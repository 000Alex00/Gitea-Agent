# Changelog


## [Unreleased] — 2026-03-26

### Features
- LLM-agnostischer Orchestrator + Multi-LLM-Routing (`7fd38df5`)
- Projekt-Profil + Feature-Flags (project.json) (closes #90) (`9caa5730`)
- --setup Wizard + --doctor Health-Check (closes #77) (`2f9eaa4d`)
- Auto-Changelog-Generator — --changelog [VERSION] (closes #39) (`84b96aba`)
- Gitea-Versionsvergleich — get_file_contents, _ast_diff, _gitea_version_compare (closes #59) (`2b9afc12`)
- context_export.sh — Slice-Schleife (plain) + Slice-Anweisung (file/gemini) (`db7365a7`)
- --get-slice loggt session.json + _warn_slices_not_requested in preconditions (`d005200f`)
- files.md Skelett-Modus — kein Volltext mehr (`a831c4ed`)
- --build-skeleton + _update_skeleton_incremental + _load_skeleton_map (`004afb86`)
- SEARCH/REPLACE Protokoll — Parser, _apply_patch, cmd_apply_patch, --apply-patch/--dry-run (closes #58) (`276417f7`)
- Diff-Validierung — Scope-Warnung bei Änderungen außerhalb AST-Skelett (closes #57) (`5747d3db`)
- AST-Skelett — ClassDef/FunctionDef-Extraktion, repo_skeleton.md, --get-slice (closes #68) (`ef9c6bf8`)
- use repo_skeleton.json for files.md truncation (`b4917d1e`)
- generate repo_skeleton.json in cmd_plan (`b663b3d3`)
- introduce find_relevant_files_advanced (`687dc46f`)
- context_export.sh + Dual-Repo --self Flag + COOKBOOK.md LLM-Workflow (closes #65) (`01bc9b5a`)
- Betriebsmodi — start_patch.sh, start_night.sh, stop_agent.sh, agent_status.sh (closes #52) (`b2c0500c`)
- Auto-Issues erst nach N aufeinanderfolgenden Passes schließen (closes #50) (`69969c26`)
- add agent_eval.template.json and tag check (`87e643a4`)
- implement patch mode and live dashboard (Issue #46) (`69eafcd3`)
- implement systematic tag failure detection in watch mode (Issue #47) (`fadeb902`)
- implement test generation via LLM prompt (Issue #44) (`74cc47e6`)
- implement performance benchmarking for eval (Issue #37) (`09a8bbed`)
- Add deterministic agent self-consistency check - Add agent_self_check.py for self checks - Run check in _check_pr_preconditions - Update COOKBOOK.md (`358a041d`)
- Kontext-Zusammenfassung als Gitea-Kommentar posten (`0e4f54b6`)
- Kontext-Loader — Kommentare in Keyword-Suche einbeziehen (`3239ae8c`)
- Issue-Kommentare in starter.md aufnehmen (## Diskussion) (`4390a8e6`)
- konfigurierbare Excludes + contexts/open/ Struktur (`018ee6f8`)
- zentrale Agent-Instanz — Pfade aus PROJECT_ROOT/agent/ lesen (closes #35) (`78ff23e1`)
- Kontext-Loader — Import-Analyse + Keyword-Suche für files.md (closes #33) (`c31e26d8`)
- Auto-Issues mit strukturiertem Kontext (closes #29) (`0a5aefec`)
- --pr prüft Server-Aktualität vor Eval + --force + --restart-before-eval (closes #30) (`5f152978`)
- _build_metadata() portiert (closes #74) (`3c180cde`)
- _build_metadata() portiert — Abschluss + Fixup Kommentare (closes #74) (`125a8aa7`)
- Szenario 2 Watch-Neustart-Trigger implementieren (closes #19) (`b5515e38`)
- _build_metadata() — aufklappbarer Block + Token-Schätzung (closes #16) (`ac811b24`)
- Abschluss-Kommentar — Eval-Ergebnis + Metadata + Session-Status (closes #13) (`4f17c02f`)
- Prioritätshinweis — technische Schranken vor Prompt-Regeln (refs #6) (`ee95c312`)
- Prozess-Enforcement — 5 Maßnahmen gegen Kontext-Drift (closes #6) (`9d8d0220`)
- STARTER_MD_PFLICHTREGELN in settings.py (Maßnahme 2+3, refs #6) (`43df45d7`)
- watch_interval_minutes aus agent_eval.json + README (closes #4) (`2d3944cf`)
- cmd_watch — generischer Hook für tools/log_analyzer.py im Watch-Loop (`7c8833cb`)
- score_history letzte 5 Einträge in PR- und Watch-Kommentare (`b3687240`)
- --score-history Anzeige-Befehl — Eval-Verlauf als Tabelle im Terminal (`a5237c4b`)
- --watch Modus + score_history.json + Auto-Issues bei Score-Verlust (`c1e0f782`)
- Baseline automatisch hochsetzen wenn Score besser wird (`47e8ace1`)
- steps-Mechanismus für mehrstufige Tests (refs #1) (`1ba15002`)
- evaluation.py — generisches Eval-System vor --pr (closes #1) (`4db5b01d`)
- --fixup, Docs-Check, Auto-Cleanup + Revert-Hinweis (`a2b56ddc`)
- Kontext-Dateien pro Issue (contexts/) (`6b1b3c10`)
- Textbausteine zentralisiert in settings.py (`b341e52a`)
- Auto-Scan + working-bot Auth + generische Konfiguration via .env (`b1ce8d0e`)
- auto-modus — Gitea-States selbständig erkennen und verarbeiten (`0a427f5d`)
- initial gitea-agent MVP — LLM-agnostischer Issue-Workflow (`09a897ce`)

### Bugfixes
- DASHBOARD_PATH in settings.py ergänzt + dashboard.py bereinigt (closes #86) (`54378fd6`)
- cmd_doctor — _api_get → gitea._request, agent_eval.json auch in agent/config/ suchen (`5b51fd21`)
- CONTEXT_DIR in .env.agent — contexts statt contexts/open (verschachtelte done/ korrigiert) (`e98cb258`)
- --build-skeleton ohne Größen-Limit (max_size_kb=10000) — agent_start.py vollständig analysiert (`2a8a5eba`)
- stufe in cmd_plan nicht definiert (NameError) (`3d04cb10`)
- evaluation.py — Commit-Hash in score_history.json (closes #55) (`e9a3a89c`)
- CONTEXT_DIR aus .env.agent lesen — --self Modus funktioniert (`4e90b93d`)
- gemini_start.sh korrekt implementiert — Kontext-Loader statt Wrapper (closes #61) (`fc577d8d`)
- _sync_closed_contexts() beim Watch-Start — verwaiste open/-Ordner nach done/ verschieben (`bc15ca67`)
- Labels bei Auto-Issue-Schließung entfernen (`a8662a53`)
- _close_resolved_auto_issues verschiebt Context nach done/ (`f8d13631`)
- Bot-Kommentar-Filter nach Username statt Keywords (`61d43b1c`)
- Kontext-Loader — Datei-Blacklist + Größen-Cap (50KB) (`b9d91bf7`)
- branch_name() — Sonderzeichen robust filtern (regex statt char-Liste) (`c68ce0f7`)
- Keyword-Suche nur in Code-Dateien (nicht .md) (`797023e1`)
- Kontext-Loader — Documentation/ excluden, AST depth=1 (`17c55edb`)
- _EXCLUDE_DIRS erweitert (Backup, vendor, contexts, .claude) (`d3db3881`)
- Path() fehlt in _resolve_path/_resolve_config — TypeError bei str project_root (`c90c0005`)
- import time fehlte — Watch-Modus crashed (NameError) (`03e05791`)
- eval_line enthält 'Score:' in allen Zweigen — COMMENT_REQUIRED_FIELDS Validierung (`e61127d1`)
- eval_line enthält Score auch bei Risikostufe-1-Skip (closes #11 Nebenfix) (`61cc1eba`)
- changed=[] Fallback vor try-Block — verhindert NameError in _neustart_required() (`73f12a5c`)
- WARN 3 — doppelte _build_metadata entfernt + Agent-Metadaten (`4bcfeaad`)
- doppelte _build_metadata() entfernt + System-Infos → Agent-Metadaten (WARN 3) (`bb0c2573`)
- baseline_score statt max_score + dynamischer Neustart-Hinweis (WARN 1+2) (`9346f4f0`)
- _save_score_history() bei baseline_created — erster Lauf in score_history (closes #25) (`6be6ca09`)
- COMMENT_REQUIRED_FIELDS[completion] — Eval + Score als Pflichtfeld (closes #24) (`211f7d50`)
- Risk-Gate in cmd_pr() — Risiko 1 überspringt Eval (closes #18) (`93414369`)
- Eval-Status und Session-Status auf separate Zeilen trennen (`2ba1e80a`)
- create_pr() gibt bestehenden PR zurück bei 409 (kein Crash bei Retry) (`61447884`)
- _format_history_block gibt Verlauf-Header zurück wenn keine History (closes #10 partial) (`dc84a582`)
- --eval-after-restart implementieren — COOKBOOK/Code Diskrepanz beheben (closes #10) (`57dee635`)
- Output-Validierung — _validate_comment() + COMMENT_REQUIRED_FIELDS (closes #8) (`a7900ba5`)
- Eval-Prüfung nur wenn agent_eval.json existiert (refs #6) (`501ac6bc`)
- sleep(2) zwischen Steps — Jetson LLM-Cooldown bei mehrstufigen Tests (`132f1a2d`)
- Eval-Fehler mit Gitea-Warnung + --summary Hinweis wenn leer (`17512cf5`)
- _ping HEAD→GET, eindeutige eval_user-ID, prompt statt message (refs #1) (`f674d0ee`)
- Label-Kette + Metadaten-Block + help-wanted Korrektur (`d39112da`)

### Refactoring
- evaluation.py von jetson-llm-chat entkoppeln (closes #89) (`75cbe428`)
- patch.py + changelog.py in plugins/ auslagern (closes #79) (`6d0c13f9`)
- Konsolenausgabe auf Statusmeldungen reduziert (`b2f49239`)
- settings.py + log.py + hardcoded Werte zentralisiert (`08748bd4`)

### Tests
- Unit-Tests für kritische reine Funktionen (closes #78) (`f5903d93`)

### Dokumentation
- Dashboard-Preview SVG + README Einbindung (`d10e6ab7`)
- Architektur-Erklärung — ein Agent, beliebig viele Projekte (`e38bb993`)
- Kapitel 1 — --setup als primärer Einstieg, manuell als Fallback (`4e4a78f6`)
- private Projektnamen anonymisiert, Legacy-Sektion entfernt (`b1894c08`)
- README + COOKBOOK — neue Features, private Daten anonymisiert (`c5b04954`)
- README aktualisiert — #77/#78/#79 in Roadmap, --generate-tests in Verwendung (`c693e4e0`)
- Gitea-Versionsvergleich (#59) in README + COOKBOOK Kapitel 18 (`5dfc9e21`)
- Codesegment-Strategie (#72) in README + COOKBOOK Kapitel 17 (`3a101c03`)
- SEARCH/REPLACE (#58) in README + COOKBOOK Kapitel 16 (`630c11da`)
- Diff-Validierung (#57) in README + COOKBOOK Kapitel 15 (`65a4e5fb`)
- AST-Skelett (#68) in README Roadmap + COOKBOOK Kapitel 14 (`74b63eed`)
- README Roadmap #65 + Gemini-Warnung; COOKBOOK Kapitel 12 Querverweise (`b0fb7b19`)
- Sitzungs-Protokoll 2026-03-23 — Änderungen + Gemini CLI Warnung (`7741c6bb`)
- README aktualisiert — Consecutive-Pass Gate, Betriebsmodi, Pflicht-Kette (`9a0c0019`)
- Feature-Übersicht in README erweitern (`d5e1e484`)
- Roadmap und Cookbook aktualisieren (`9a17348b`)
- README vollständig LLM-agnostisch formuliert (`e39474ab`)
- LLM-Anbindung (manuell vs. API) und Roadmap ausgebaut (`e821051c`)
- Feature-Übersicht mit Beschreibungen und Mini-Beispielen ausgebaut (`b518bfe8`)
- README — Intro und Feature-Übersicht ergänzt (`42809fb0`)
- konfigurierbare Excludes + contexts/open/ Struktur nachgezogen (`e37657f0`)
- agent_start.py.md + evaluation.py.md nachgezogen (Kontext-Loader Fixes, Pfade) (`1b43c7cc`)
- README + COOKBOOK — zentrale Agent-Instanz, Migrationsrezept (#35) (`5a62e026`)
- README + COOKBOOK — Kontext-Loader (Import-Analyse + grep) dokumentiert (#33) (`63635d87`)
- agent_start.py.md — Kontext-Loader dokumentiert (#33) (`fdbe29dc`)
- README + COOKBOOK — Auto-Issue strukturierter Body dokumentiert (#29) (`42997de8`)
- evaluation.py.md + agent_start.py.md — Issue #29 dokumentiert (closes #29) (`8454793e`)
- README + COOKBOOK — Server-Staleness-Check, --force, --restart-before-eval dokumentiert (`0d530d57`)
- README + COOKBOOK aktualisiert — Prozess-Enforcement, Watch-Szenario 2, log_analyzer (closes #11) (`dc7f98d9`)
- COOKBOOK.md — 10 Rezepte mit Skynet-Referenz (closes #5) (`3d2b8ebd`)
- README — Watch-Modus, score_history, Auto-Baseline, History-in-Comments (`087b96d8`)
- README — Risiko ≥ 2 Ausnahme + steps sleep(2) + Eval-Fehler-Verhalten (`496b6caa`)
- README — Eval-System Abschnitt ergänzt (evaluation.py, agent_eval.json, steps, baseline) (`1f2ccf64`)
- evaluation.py.md + README Eval-Abschnitt (refs #1) (`5f0e6dbc`)
- README komplett überarbeitet (`702e911b`)
- Problemstellung + Lösungsansätze für LLM-Trigger vollständig dokumentiert (`a4666356`)
- README Auto-Modus Diagramm + --root Fix, Docstrings vervollständigt (`e24fbb77`)

### Wartung
- Projektstruktur bereinigt — scripts/, data/, .gitignore (closes #92) (`d5c0c6de`)
- push_github.sh — Mirror-Script für GitHub (`115f83a1`)
- add gemini_start.sh for Gemini CLI workflow (closes #61) (`403f86af`)
- contexts/ und gitea-agent.log zu .gitignore hinzufügen (`5f115235`)

### Sonstiges
- Merge pull request '[04] Projekt-Profil: project.json + Feature-Flags für modularen Betrieb (closes #90)' (#98) from chore/issue-90-04-projekt-profil-project-json-fea into main (`7d9c0bef`)
- Merge pull request '[03] Refactor: evaluation.py von jetson-llm-chat entkoppeln (closes #89)' (#97) from chore/issue-89-03-refactor-evaluation-py-von-jets into main (`0e01452e`)
- Merge pull request '[02] Projektstruktur: saubere Trennung Code / Laufzeit / Config / Scripts (closes #92)' (#96) from chore/issue-92-02-projektstruktur-saubere-trennun into main (`132bb780`)
- Merge pull request 'fix: DASHBOARD_PATH korrekt in agent/data/ (closes #86)' (#93) from fix/issue-86-dashboard-path into main (`fef9fe92`)
- .env.agent aus Tracking entfernen + .gitignore ergänzt (`a2c7765c`)
- Merge pull request '--setup: Interaktiver Einrichtungs-Wizard für neue Projekte (closes #77)' (#84) from feat/issue-77-setup-wizard into main (`e884301b`)
- Merge pull request 'Inkrementelles Refactoring: patch.py + changelog.py auslagern (closes #79)' (#83) from refactor/issue-79-plugins-patch-changelog into main (`7f7edfb5`)
- Merge pull request 'Auto-Changelog-Generator (closes #39)' (#75) from feat/issue-39-auto-changelog-generator into main (`73799515`)
- Merge pull request '5 Gitea Versionsvergleich (closes #59)' (#74) from chore/issue-59-5-gitea-versionsvergleich into main (`a53138ad`)
- Merge pull request 'Flächendeckende Codesegment-Strategie — LLM lädt nur noch relevante Blöcke (closes #72)' (#73) from chore/issue-72-flaechendeckende-codesegment-strate into main (`9c94cf3e`)
- Merge pull request '4 SEARCH/REPLACE (closes #58)' (#71) from chore/issue-58-4-search-replace into main (`ce6c46a9`)
- Merge pull request '3 Diff-Validierung (closes #57)' (#70) from chore/issue-57-3-diff-validierung into main (`56126644`)
- Merge pull request 'Titel: AST-Repository-Skelett — repo_skeleton.json bei --issue generieren (closes #68)' (#69) from chore/issue-68-titel-ast-repository-skelett-repo-s into main (`f2ce6e36`)
- Merge pull request '2 AST-Skelett (closes #56)' (#67) from chore/issue-56-2-ast-skelett into main (`978b9c3d`)
- Merge pull request 'context_export.sh — LLM-agnostischer Kontext-Export + Dual-Repo-Unterstützung (closes #65)' (#66) from feat/issue-65-context-export-dual-repo into main (`6130c6fb`)
- Merge pull request 'gemini_start.sh — Gemini CLI in Agent-Workflow integrieren (closes #61)' (#63) from fix/gemini-start-correct into main (`12050f2c`)
- Merge pull request 'gemini_start.sh — Gemini CLI in Agent-Workflow integrieren (closes #61)' (#62) from chore/issue-61-gemini-start-sh-gemini-cli-in-agent into main (`d8d48224`)
- README.md aktualisiert (`7ea06675`)
- Merge pull request 'feat: Betriebsmodi — Night / Patch / Idle (closes #52)' (#53) from feat/issue-52-betriebsmodi into main (`73c2bde8`)
- Merge pull request 'feat: Auto-Issues erst nach N aufeinanderfolgenden Passes schließen (closes #50)' (#51) from feat/issue-50-consecutive-passes into main (`7691ef48`)
- README.md aktualisiert (`c6410430`)
- Merge pull request 'feat: zentrale Agent-Instanz — Pfade aus PROJECT_ROOT/agent/ lesen (closes #35)' (#36) from feat/issue-35-zentrale-agent-instanz into main (`23a3b943`)
- Merge pull request 'feat: Kontext-Loader — Import-Analyse + Keyword-Suche für files.md (closes #33)' (#34) from feat/issue-33-kontext-loader-import-analyse into main (`0cf01850`)
- Merge pull request 'feat: Auto-Issues mit strukturiertem Kontext (closes #29)' (#32) from feat/issue-29-auto-issue-strukturierter-kontext into main (`a73ecad5`)
- Merge pull request 'pr soll vor Eval prüfen ob Server-Code aktuell ist (closes #30)' (#31) from chore/issue-30-pr-soll-vor-eval-pr-fen-ob-server-c into main (`97f34766`)
- Merge pull request 'Docs: README + COOKBOOK aktualisieren — #6/#8 Details + log_analyzer Rezept (closes #11)' (#28) from docs/issue-11-docs-readme-+-cookbook-aktualisiere into main (`f8a4c2d7`)
- Merge pull request 'Audit-Fixes — 2 FAIL + 3 WARN aus Re-Audit #23 (closes #26)' (#27) from fix/issue-26-audit-fixes into main (`9d5b9c1c`)
- Merge branch 'main' into fix/issue-26-audit-fixes (`e5499fa1`)
- Merge pull request '[Audit FAIL] Szenario 2 — Watch-Neustart-Trigger nicht implementiert (closes #19)' (#21) from fix/issue-19-szenario2-watch-neustart into main (`bb446ff3`)
- Merge branch 'main' into fix/issue-19-szenario2-watch-neustart (`d4151a5f`)
- Merge pull request '[Audit FAIL] Risiko 1 — Eval wird nicht übersprungen (kein Risk-Gate) (closes #18)' (#20) from fix/issue-18-risk-gate-eval-skip into main (`f88a163f`)
- Merge branch 'main' into fix/issue-18-risk-gate-eval-skip (`5810f47e`)
- Merge pull request '_build_metadata() — Token-Schätzung + aufklappbarer Block (closes #16)' (#17) from chore/issue-16-_build_metadata-—-token-sch-tzung-+ into main (`f577cf45`)
- Merge branch 'main' into chore/issue-16-_build_metadata-—-token-sch-tzung-+ (`3b084f70`)
- Merge pull request 'Abschluss-Kommentar — Eval-Ergebnis + Metadata + Session-Status (closes #13)' (#15) from chore/issue-13-abschluss-kommentar-—-eval-ergebnis into main (`13c023f7`)
- Merge branch 'main' into chore/issue-13-abschluss-kommentar-—-eval-ergebnis (`a914faf6`)
- Merge pull request 'Fix: --eval-after-restart Diskrepanz — COOKBOOK vs agent_start.py (closes #10)' (#12) from chore/issue-10-fix-eval-after-restart-diskrepanz-— into main (`3c6a98a5`)
- Merge pull request 'fix: Output-Validierung — _validate_comment() + COMMENT_REQUIRED_FIELDS (closes #8)' (#9) from fix/issue-8-output-validierung-kommentare into main (`3813836e`)
- Merge pull request 'Prozess-Enforcement — Kontext-Drift verhindern (closes #6)' (#7) from chore/issue-6-prozess-enforcement-—-kontext-drift into main (`3adae3a6`)
- Merge pull request 'feat: evaluation.py — generisches Eval-System vor --pr (closes #1)' (#2) from feat/issue-1-evaluation-system into main (`312913a8`)

## [Unreleased] — 2026-03-26

### Features
- Autonomous Self-Healing Loop — plugins/healing.py (`7e5d1a58`)
- Projekt-Profil + Feature-Flags (project.json) (closes #90) (`9caa5730`)
- --setup Wizard + --doctor Health-Check (closes #77) (`2f9eaa4d`)
- Auto-Changelog-Generator — --changelog [VERSION] (closes #39) (`84b96aba`)
- Gitea-Versionsvergleich — get_file_contents, _ast_diff, _gitea_version_compare (closes #59) (`2b9afc12`)
- context_export.sh — Slice-Schleife (plain) + Slice-Anweisung (file/gemini) (`db7365a7`)
- --get-slice loggt session.json + _warn_slices_not_requested in preconditions (`d005200f`)
- files.md Skelett-Modus — kein Volltext mehr (`a831c4ed`)
- --build-skeleton + _update_skeleton_incremental + _load_skeleton_map (`004afb86`)
- SEARCH/REPLACE Protokoll — Parser, _apply_patch, cmd_apply_patch, --apply-patch/--dry-run (closes #58) (`276417f7`)
- Diff-Validierung — Scope-Warnung bei Änderungen außerhalb AST-Skelett (closes #57) (`5747d3db`)
- AST-Skelett — ClassDef/FunctionDef-Extraktion, repo_skeleton.md, --get-slice (closes #68) (`ef9c6bf8`)
- use repo_skeleton.json for files.md truncation (`b4917d1e`)
- generate repo_skeleton.json in cmd_plan (`b663b3d3`)
- introduce find_relevant_files_advanced (`687dc46f`)
- context_export.sh + Dual-Repo --self Flag + COOKBOOK.md LLM-Workflow (closes #65) (`01bc9b5a`)
- Betriebsmodi — start_patch.sh, start_night.sh, stop_agent.sh, agent_status.sh (closes #52) (`b2c0500c`)
- Auto-Issues erst nach N aufeinanderfolgenden Passes schließen (closes #50) (`69969c26`)
- add agent_eval.template.json and tag check (`87e643a4`)
- implement patch mode and live dashboard (Issue #46) (`69eafcd3`)
- implement systematic tag failure detection in watch mode (Issue #47) (`fadeb902`)
- implement test generation via LLM prompt (Issue #44) (`74cc47e6`)
- implement performance benchmarking for eval (Issue #37) (`09a8bbed`)
- Add deterministic agent self-consistency check - Add agent_self_check.py for self checks - Run check in _check_pr_preconditions - Update COOKBOOK.md (`358a041d`)
- Kontext-Zusammenfassung als Gitea-Kommentar posten (`0e4f54b6`)
- Kontext-Loader — Kommentare in Keyword-Suche einbeziehen (`3239ae8c`)
- Issue-Kommentare in starter.md aufnehmen (## Diskussion) (`4390a8e6`)
- konfigurierbare Excludes + contexts/open/ Struktur (`018ee6f8`)
- zentrale Agent-Instanz — Pfade aus PROJECT_ROOT/agent/ lesen (closes #35) (`78ff23e1`)
- Kontext-Loader — Import-Analyse + Keyword-Suche für files.md (closes #33) (`c31e26d8`)
- Auto-Issues mit strukturiertem Kontext (closes #29) (`0a5aefec`)
- --pr prüft Server-Aktualität vor Eval + --force + --restart-before-eval (closes #30) (`5f152978`)
- _build_metadata() portiert (closes #74) (`3c180cde`)
- _build_metadata() portiert — Abschluss + Fixup Kommentare (closes #74) (`125a8aa7`)
- Szenario 2 Watch-Neustart-Trigger implementieren (closes #19) (`b5515e38`)
- _build_metadata() — aufklappbarer Block + Token-Schätzung (closes #16) (`ac811b24`)
- Abschluss-Kommentar — Eval-Ergebnis + Metadata + Session-Status (closes #13) (`4f17c02f`)
- Prioritätshinweis — technische Schranken vor Prompt-Regeln (refs #6) (`ee95c312`)
- Prozess-Enforcement — 5 Maßnahmen gegen Kontext-Drift (closes #6) (`9d8d0220`)
- STARTER_MD_PFLICHTREGELN in settings.py (Maßnahme 2+3, refs #6) (`43df45d7`)
- watch_interval_minutes aus agent_eval.json + README (closes #4) (`2d3944cf`)
- cmd_watch — generischer Hook für tools/log_analyzer.py im Watch-Loop (`7c8833cb`)
- score_history letzte 5 Einträge in PR- und Watch-Kommentare (`b3687240`)
- --score-history Anzeige-Befehl — Eval-Verlauf als Tabelle im Terminal (`a5237c4b`)
- --watch Modus + score_history.json + Auto-Issues bei Score-Verlust (`c1e0f782`)
- Baseline automatisch hochsetzen wenn Score besser wird (`47e8ace1`)
- steps-Mechanismus für mehrstufige Tests (refs #1) (`1ba15002`)
- evaluation.py — generisches Eval-System vor --pr (closes #1) (`4db5b01d`)
- --fixup, Docs-Check, Auto-Cleanup + Revert-Hinweis (`a2b56ddc`)
- Kontext-Dateien pro Issue (contexts/) (`6b1b3c10`)
- Textbausteine zentralisiert in settings.py (`b341e52a`)
- Auto-Scan + working-bot Auth + generische Konfiguration via .env (`b1ce8d0e`)
- auto-modus — Gitea-States selbständig erkennen und verarbeiten (`0a427f5d`)
- initial gitea-agent MVP — LLM-agnostischer Issue-Workflow (`09a897ce`)

### Bugfixes
- DASHBOARD_PATH in settings.py ergänzt + dashboard.py bereinigt (closes #86) (`54378fd6`)
- cmd_doctor — _api_get → gitea._request, agent_eval.json auch in agent/config/ suchen (`5b51fd21`)
- CONTEXT_DIR in .env.agent — contexts statt contexts/open (verschachtelte done/ korrigiert) (`e98cb258`)
- --build-skeleton ohne Größen-Limit (max_size_kb=10000) — agent_start.py vollständig analysiert (`2a8a5eba`)
- stufe in cmd_plan nicht definiert (NameError) (`3d04cb10`)
- evaluation.py — Commit-Hash in score_history.json (closes #55) (`e9a3a89c`)
- CONTEXT_DIR aus .env.agent lesen — --self Modus funktioniert (`4e90b93d`)
- gemini_start.sh korrekt implementiert — Kontext-Loader statt Wrapper (closes #61) (`fc577d8d`)
- _sync_closed_contexts() beim Watch-Start — verwaiste open/-Ordner nach done/ verschieben (`bc15ca67`)
- Labels bei Auto-Issue-Schließung entfernen (`a8662a53`)
- _close_resolved_auto_issues verschiebt Context nach done/ (`f8d13631`)
- Bot-Kommentar-Filter nach Username statt Keywords (`61d43b1c`)
- Kontext-Loader — Datei-Blacklist + Größen-Cap (50KB) (`b9d91bf7`)
- branch_name() — Sonderzeichen robust filtern (regex statt char-Liste) (`c68ce0f7`)
- Keyword-Suche nur in Code-Dateien (nicht .md) (`797023e1`)
- Kontext-Loader — Documentation/ excluden, AST depth=1 (`17c55edb`)
- _EXCLUDE_DIRS erweitert (Backup, vendor, contexts, .claude) (`d3db3881`)
- Path() fehlt in _resolve_path/_resolve_config — TypeError bei str project_root (`c90c0005`)
- import time fehlte — Watch-Modus crashed (NameError) (`03e05791`)
- eval_line enthält 'Score:' in allen Zweigen — COMMENT_REQUIRED_FIELDS Validierung (`e61127d1`)
- eval_line enthält Score auch bei Risikostufe-1-Skip (closes #11 Nebenfix) (`61cc1eba`)
- changed=[] Fallback vor try-Block — verhindert NameError in _neustart_required() (`73f12a5c`)
- WARN 3 — doppelte _build_metadata entfernt + Agent-Metadaten (`4bcfeaad`)
- doppelte _build_metadata() entfernt + System-Infos → Agent-Metadaten (WARN 3) (`bb0c2573`)
- baseline_score statt max_score + dynamischer Neustart-Hinweis (WARN 1+2) (`9346f4f0`)
- _save_score_history() bei baseline_created — erster Lauf in score_history (closes #25) (`6be6ca09`)
- COMMENT_REQUIRED_FIELDS[completion] — Eval + Score als Pflichtfeld (closes #24) (`211f7d50`)
- Risk-Gate in cmd_pr() — Risiko 1 überspringt Eval (closes #18) (`93414369`)
- Eval-Status und Session-Status auf separate Zeilen trennen (`2ba1e80a`)
- create_pr() gibt bestehenden PR zurück bei 409 (kein Crash bei Retry) (`61447884`)
- _format_history_block gibt Verlauf-Header zurück wenn keine History (closes #10 partial) (`dc84a582`)
- --eval-after-restart implementieren — COOKBOOK/Code Diskrepanz beheben (closes #10) (`57dee635`)
- Output-Validierung — _validate_comment() + COMMENT_REQUIRED_FIELDS (closes #8) (`a7900ba5`)
- Eval-Prüfung nur wenn agent_eval.json existiert (refs #6) (`501ac6bc`)
- sleep(2) zwischen Steps — Jetson LLM-Cooldown bei mehrstufigen Tests (`132f1a2d`)
- Eval-Fehler mit Gitea-Warnung + --summary Hinweis wenn leer (`17512cf5`)
- _ping HEAD→GET, eindeutige eval_user-ID, prompt statt message (refs #1) (`f674d0ee`)
- Label-Kette + Metadaten-Block + help-wanted Korrektur (`d39112da`)

### Refactoring
- evaluation.py von jetson-llm-chat entkoppeln (closes #89) (`75cbe428`)
- patch.py + changelog.py in plugins/ auslagern (closes #79) (`6d0c13f9`)
- Konsolenausgabe auf Statusmeldungen reduziert (`b2f49239`)
- settings.py + log.py + hardcoded Werte zentralisiert (`08748bd4`)

### Tests
- Unit-Tests für kritische reine Funktionen (closes #78) (`f5903d93`)

### Dokumentation
- Dashboard-Preview SVG + README Einbindung (`d10e6ab7`)
- Architektur-Erklärung — ein Agent, beliebig viele Projekte (`e38bb993`)
- Kapitel 1 — --setup als primärer Einstieg, manuell als Fallback (`4e4a78f6`)
- private Projektnamen anonymisiert, Legacy-Sektion entfernt (`b1894c08`)
- README + COOKBOOK — neue Features, private Daten anonymisiert (`c5b04954`)
- README aktualisiert — #77/#78/#79 in Roadmap, --generate-tests in Verwendung (`c693e4e0`)
- Gitea-Versionsvergleich (#59) in README + COOKBOOK Kapitel 18 (`5dfc9e21`)
- Codesegment-Strategie (#72) in README + COOKBOOK Kapitel 17 (`3a101c03`)
- SEARCH/REPLACE (#58) in README + COOKBOOK Kapitel 16 (`630c11da`)
- Diff-Validierung (#57) in README + COOKBOOK Kapitel 15 (`65a4e5fb`)
- AST-Skelett (#68) in README Roadmap + COOKBOOK Kapitel 14 (`74b63eed`)
- README Roadmap #65 + Gemini-Warnung; COOKBOOK Kapitel 12 Querverweise (`b0fb7b19`)
- Sitzungs-Protokoll 2026-03-23 — Änderungen + Gemini CLI Warnung (`7741c6bb`)
- README aktualisiert — Consecutive-Pass Gate, Betriebsmodi, Pflicht-Kette (`9a0c0019`)
- Feature-Übersicht in README erweitern (`d5e1e484`)
- Roadmap und Cookbook aktualisieren (`9a17348b`)
- README vollständig LLM-agnostisch formuliert (`e39474ab`)
- LLM-Anbindung (manuell vs. API) und Roadmap ausgebaut (`e821051c`)
- Feature-Übersicht mit Beschreibungen und Mini-Beispielen ausgebaut (`b518bfe8`)
- README — Intro und Feature-Übersicht ergänzt (`42809fb0`)
- konfigurierbare Excludes + contexts/open/ Struktur nachgezogen (`e37657f0`)
- agent_start.py.md + evaluation.py.md nachgezogen (Kontext-Loader Fixes, Pfade) (`1b43c7cc`)
- README + COOKBOOK — zentrale Agent-Instanz, Migrationsrezept (#35) (`5a62e026`)
- README + COOKBOOK — Kontext-Loader (Import-Analyse + grep) dokumentiert (#33) (`63635d87`)
- agent_start.py.md — Kontext-Loader dokumentiert (#33) (`fdbe29dc`)
- README + COOKBOOK — Auto-Issue strukturierter Body dokumentiert (#29) (`42997de8`)
- evaluation.py.md + agent_start.py.md — Issue #29 dokumentiert (closes #29) (`8454793e`)
- README + COOKBOOK — Server-Staleness-Check, --force, --restart-before-eval dokumentiert (`0d530d57`)
- README + COOKBOOK aktualisiert — Prozess-Enforcement, Watch-Szenario 2, log_analyzer (closes #11) (`dc7f98d9`)
- COOKBOOK.md — 10 Rezepte mit Skynet-Referenz (closes #5) (`3d2b8ebd`)
- README — Watch-Modus, score_history, Auto-Baseline, History-in-Comments (`087b96d8`)
- README — Risiko ≥ 2 Ausnahme + steps sleep(2) + Eval-Fehler-Verhalten (`496b6caa`)
- README — Eval-System Abschnitt ergänzt (evaluation.py, agent_eval.json, steps, baseline) (`1f2ccf64`)
- evaluation.py.md + README Eval-Abschnitt (refs #1) (`5f0e6dbc`)
- README komplett überarbeitet (`702e911b`)
- Problemstellung + Lösungsansätze für LLM-Trigger vollständig dokumentiert (`a4666356`)
- README Auto-Modus Diagramm + --root Fix, Docstrings vervollständigt (`e24fbb77`)

### Wartung
- Projektstruktur bereinigt — scripts/, data/, .gitignore (closes #92) (`d5c0c6de`)
- push_github.sh — Mirror-Script für GitHub (`115f83a1`)
- add gemini_start.sh for Gemini CLI workflow (closes #61) (`403f86af`)
- contexts/ und gitea-agent.log zu .gitignore hinzufügen (`5f115235`)

### Sonstiges
- Merge pull request '[04] Projekt-Profil: project.json + Feature-Flags für modularen Betrieb (closes #90)' (#98) from chore/issue-90-04-projekt-profil-project-json-fea into main (`7d9c0bef`)
- Merge pull request '[03] Refactor: evaluation.py von jetson-llm-chat entkoppeln (closes #89)' (#97) from chore/issue-89-03-refactor-evaluation-py-von-jets into main (`0e01452e`)
- Merge pull request '[02] Projektstruktur: saubere Trennung Code / Laufzeit / Config / Scripts (closes #92)' (#96) from chore/issue-92-02-projektstruktur-saubere-trennun into main (`132bb780`)
- Merge pull request 'fix: DASHBOARD_PATH korrekt in agent/data/ (closes #86)' (#93) from fix/issue-86-dashboard-path into main (`fef9fe92`)
- .env.agent aus Tracking entfernen + .gitignore ergänzt (`a2c7765c`)
- Merge pull request '--setup: Interaktiver Einrichtungs-Wizard für neue Projekte (closes #77)' (#84) from feat/issue-77-setup-wizard into main (`e884301b`)
- Merge pull request 'Inkrementelles Refactoring: patch.py + changelog.py auslagern (closes #79)' (#83) from refactor/issue-79-plugins-patch-changelog into main (`7f7edfb5`)
- Merge pull request 'Auto-Changelog-Generator (closes #39)' (#75) from feat/issue-39-auto-changelog-generator into main (`73799515`)
- Merge pull request '5 Gitea Versionsvergleich (closes #59)' (#74) from chore/issue-59-5-gitea-versionsvergleich into main (`a53138ad`)
- Merge pull request 'Flächendeckende Codesegment-Strategie — LLM lädt nur noch relevante Blöcke (closes #72)' (#73) from chore/issue-72-flaechendeckende-codesegment-strate into main (`9c94cf3e`)
- Merge pull request '4 SEARCH/REPLACE (closes #58)' (#71) from chore/issue-58-4-search-replace into main (`ce6c46a9`)
- Merge pull request '3 Diff-Validierung (closes #57)' (#70) from chore/issue-57-3-diff-validierung into main (`56126644`)
- Merge pull request 'Titel: AST-Repository-Skelett — repo_skeleton.json bei --issue generieren (closes #68)' (#69) from chore/issue-68-titel-ast-repository-skelett-repo-s into main (`f2ce6e36`)
- Merge pull request '2 AST-Skelett (closes #56)' (#67) from chore/issue-56-2-ast-skelett into main (`978b9c3d`)
- Merge pull request 'context_export.sh — LLM-agnostischer Kontext-Export + Dual-Repo-Unterstützung (closes #65)' (#66) from feat/issue-65-context-export-dual-repo into main (`6130c6fb`)
- Merge pull request 'gemini_start.sh — Gemini CLI in Agent-Workflow integrieren (closes #61)' (#63) from fix/gemini-start-correct into main (`12050f2c`)
- Merge pull request 'gemini_start.sh — Gemini CLI in Agent-Workflow integrieren (closes #61)' (#62) from chore/issue-61-gemini-start-sh-gemini-cli-in-agent into main (`d8d48224`)
- README.md aktualisiert (`7ea06675`)
- Merge pull request 'feat: Betriebsmodi — Night / Patch / Idle (closes #52)' (#53) from feat/issue-52-betriebsmodi into main (`73c2bde8`)
- Merge pull request 'feat: Auto-Issues erst nach N aufeinanderfolgenden Passes schließen (closes #50)' (#51) from feat/issue-50-consecutive-passes into main (`7691ef48`)
- README.md aktualisiert (`c6410430`)
- Merge pull request 'feat: zentrale Agent-Instanz — Pfade aus PROJECT_ROOT/agent/ lesen (closes #35)' (#36) from feat/issue-35-zentrale-agent-instanz into main (`23a3b943`)
- Merge pull request 'feat: Kontext-Loader — Import-Analyse + Keyword-Suche für files.md (closes #33)' (#34) from feat/issue-33-kontext-loader-import-analyse into main (`0cf01850`)
- Merge pull request 'feat: Auto-Issues mit strukturiertem Kontext (closes #29)' (#32) from feat/issue-29-auto-issue-strukturierter-kontext into main (`a73ecad5`)
- Merge pull request 'pr soll vor Eval prüfen ob Server-Code aktuell ist (closes #30)' (#31) from chore/issue-30-pr-soll-vor-eval-pr-fen-ob-server-c into main (`97f34766`)
- Merge pull request 'Docs: README + COOKBOOK aktualisieren — #6/#8 Details + log_analyzer Rezept (closes #11)' (#28) from docs/issue-11-docs-readme-+-cookbook-aktualisiere into main (`f8a4c2d7`)
- Merge pull request 'Audit-Fixes — 2 FAIL + 3 WARN aus Re-Audit #23 (closes #26)' (#27) from fix/issue-26-audit-fixes into main (`9d5b9c1c`)
- Merge branch 'main' into fix/issue-26-audit-fixes (`e5499fa1`)
- Merge pull request '[Audit FAIL] Szenario 2 — Watch-Neustart-Trigger nicht implementiert (closes #19)' (#21) from fix/issue-19-szenario2-watch-neustart into main (`bb446ff3`)
- Merge branch 'main' into fix/issue-19-szenario2-watch-neustart (`d4151a5f`)
- Merge pull request '[Audit FAIL] Risiko 1 — Eval wird nicht übersprungen (kein Risk-Gate) (closes #18)' (#20) from fix/issue-18-risk-gate-eval-skip into main (`f88a163f`)
- Merge branch 'main' into fix/issue-18-risk-gate-eval-skip (`5810f47e`)
- Merge pull request '_build_metadata() — Token-Schätzung + aufklappbarer Block (closes #16)' (#17) from chore/issue-16-_build_metadata-—-token-sch-tzung-+ into main (`f577cf45`)
- Merge branch 'main' into chore/issue-16-_build_metadata-—-token-sch-tzung-+ (`3b084f70`)
- Merge pull request 'Abschluss-Kommentar — Eval-Ergebnis + Metadata + Session-Status (closes #13)' (#15) from chore/issue-13-abschluss-kommentar-—-eval-ergebnis into main (`13c023f7`)
- Merge branch 'main' into chore/issue-13-abschluss-kommentar-—-eval-ergebnis (`a914faf6`)
- Merge pull request 'Fix: --eval-after-restart Diskrepanz — COOKBOOK vs agent_start.py (closes #10)' (#12) from chore/issue-10-fix-eval-after-restart-diskrepanz-— into main (`3c6a98a5`)
- Merge pull request 'fix: Output-Validierung — _validate_comment() + COMMENT_REQUIRED_FIELDS (closes #8)' (#9) from fix/issue-8-output-validierung-kommentare into main (`3813836e`)
- Merge pull request 'Prozess-Enforcement — Kontext-Drift verhindern (closes #6)' (#7) from chore/issue-6-prozess-enforcement-—-kontext-drift into main (`3adae3a6`)
- Merge pull request 'feat: evaluation.py — generisches Eval-System vor --pr (closes #1)' (#2) from feat/issue-1-evaluation-system into main (`312913a8`)

## [Unreleased] — 2026-03-26

### Features
- Autonomous Self-Healing Loop — plugins/healing.py (`7e5d1a58`)
- Projekt-Profil + Feature-Flags (project.json) (closes #90) (`9caa5730`)
- --setup Wizard + --doctor Health-Check (closes #77) (`2f9eaa4d`)
- Auto-Changelog-Generator — --changelog [VERSION] (closes #39) (`84b96aba`)
- Gitea-Versionsvergleich — get_file_contents, _ast_diff, _gitea_version_compare (closes #59) (`2b9afc12`)
- context_export.sh — Slice-Schleife (plain) + Slice-Anweisung (file/gemini) (`db7365a7`)
- --get-slice loggt session.json + _warn_slices_not_requested in preconditions (`d005200f`)
- files.md Skelett-Modus — kein Volltext mehr (`a831c4ed`)
- --build-skeleton + _update_skeleton_incremental + _load_skeleton_map (`004afb86`)
- SEARCH/REPLACE Protokoll — Parser, _apply_patch, cmd_apply_patch, --apply-patch/--dry-run (closes #58) (`276417f7`)
- Diff-Validierung — Scope-Warnung bei Änderungen außerhalb AST-Skelett (closes #57) (`5747d3db`)
- AST-Skelett — ClassDef/FunctionDef-Extraktion, repo_skeleton.md, --get-slice (closes #68) (`ef9c6bf8`)
- use repo_skeleton.json for files.md truncation (`b4917d1e`)
- generate repo_skeleton.json in cmd_plan (`b663b3d3`)
- introduce find_relevant_files_advanced (`687dc46f`)
- context_export.sh + Dual-Repo --self Flag + COOKBOOK.md LLM-Workflow (closes #65) (`01bc9b5a`)
- Betriebsmodi — start_patch.sh, start_night.sh, stop_agent.sh, agent_status.sh (closes #52) (`b2c0500c`)
- Auto-Issues erst nach N aufeinanderfolgenden Passes schließen (closes #50) (`69969c26`)
- add agent_eval.template.json and tag check (`87e643a4`)
- implement patch mode and live dashboard (Issue #46) (`69eafcd3`)
- implement systematic tag failure detection in watch mode (Issue #47) (`fadeb902`)
- implement test generation via LLM prompt (Issue #44) (`74cc47e6`)
- implement performance benchmarking for eval (Issue #37) (`09a8bbed`)
- Add deterministic agent self-consistency check - Add agent_self_check.py for self checks - Run check in _check_pr_preconditions - Update COOKBOOK.md (`358a041d`)
- Kontext-Zusammenfassung als Gitea-Kommentar posten (`0e4f54b6`)
- Kontext-Loader — Kommentare in Keyword-Suche einbeziehen (`3239ae8c`)
- Issue-Kommentare in starter.md aufnehmen (## Diskussion) (`4390a8e6`)
- konfigurierbare Excludes + contexts/open/ Struktur (`018ee6f8`)
- zentrale Agent-Instanz — Pfade aus PROJECT_ROOT/agent/ lesen (closes #35) (`78ff23e1`)
- Kontext-Loader — Import-Analyse + Keyword-Suche für files.md (closes #33) (`c31e26d8`)
- Auto-Issues mit strukturiertem Kontext (closes #29) (`0a5aefec`)
- --pr prüft Server-Aktualität vor Eval + --force + --restart-before-eval (closes #30) (`5f152978`)
- _build_metadata() portiert (closes #74) (`3c180cde`)
- _build_metadata() portiert — Abschluss + Fixup Kommentare (closes #74) (`125a8aa7`)
- Szenario 2 Watch-Neustart-Trigger implementieren (closes #19) (`b5515e38`)
- _build_metadata() — aufklappbarer Block + Token-Schätzung (closes #16) (`ac811b24`)
- Abschluss-Kommentar — Eval-Ergebnis + Metadata + Session-Status (closes #13) (`4f17c02f`)
- Prioritätshinweis — technische Schranken vor Prompt-Regeln (refs #6) (`ee95c312`)
- Prozess-Enforcement — 5 Maßnahmen gegen Kontext-Drift (closes #6) (`9d8d0220`)
- STARTER_MD_PFLICHTREGELN in settings.py (Maßnahme 2+3, refs #6) (`43df45d7`)
- watch_interval_minutes aus agent_eval.json + README (closes #4) (`2d3944cf`)
- cmd_watch — generischer Hook für tools/log_analyzer.py im Watch-Loop (`7c8833cb`)
- score_history letzte 5 Einträge in PR- und Watch-Kommentare (`b3687240`)
- --score-history Anzeige-Befehl — Eval-Verlauf als Tabelle im Terminal (`a5237c4b`)
- --watch Modus + score_history.json + Auto-Issues bei Score-Verlust (`c1e0f782`)
- Baseline automatisch hochsetzen wenn Score besser wird (`47e8ace1`)
- steps-Mechanismus für mehrstufige Tests (refs #1) (`1ba15002`)
- evaluation.py — generisches Eval-System vor --pr (closes #1) (`4db5b01d`)
- --fixup, Docs-Check, Auto-Cleanup + Revert-Hinweis (`a2b56ddc`)
- Kontext-Dateien pro Issue (contexts/) (`6b1b3c10`)
- Textbausteine zentralisiert in settings.py (`b341e52a`)
- Auto-Scan + working-bot Auth + generische Konfiguration via .env (`b1ce8d0e`)
- auto-modus — Gitea-States selbständig erkennen und verarbeiten (`0a427f5d`)
- initial gitea-agent MVP — LLM-agnostischer Issue-Workflow (`09a897ce`)

### Bugfixes
- DASHBOARD_PATH in settings.py ergänzt + dashboard.py bereinigt (closes #86) (`54378fd6`)
- cmd_doctor — _api_get → gitea._request, agent_eval.json auch in agent/config/ suchen (`5b51fd21`)
- CONTEXT_DIR in .env.agent — contexts statt contexts/open (verschachtelte done/ korrigiert) (`e98cb258`)
- --build-skeleton ohne Größen-Limit (max_size_kb=10000) — agent_start.py vollständig analysiert (`2a8a5eba`)
- stufe in cmd_plan nicht definiert (NameError) (`3d04cb10`)
- evaluation.py — Commit-Hash in score_history.json (closes #55) (`e9a3a89c`)
- CONTEXT_DIR aus .env.agent lesen — --self Modus funktioniert (`4e90b93d`)
- gemini_start.sh korrekt implementiert — Kontext-Loader statt Wrapper (closes #61) (`fc577d8d`)
- _sync_closed_contexts() beim Watch-Start — verwaiste open/-Ordner nach done/ verschieben (`bc15ca67`)
- Labels bei Auto-Issue-Schließung entfernen (`a8662a53`)
- _close_resolved_auto_issues verschiebt Context nach done/ (`f8d13631`)
- Bot-Kommentar-Filter nach Username statt Keywords (`61d43b1c`)
- Kontext-Loader — Datei-Blacklist + Größen-Cap (50KB) (`b9d91bf7`)
- branch_name() — Sonderzeichen robust filtern (regex statt char-Liste) (`c68ce0f7`)
- Keyword-Suche nur in Code-Dateien (nicht .md) (`797023e1`)
- Kontext-Loader — Documentation/ excluden, AST depth=1 (`17c55edb`)
- _EXCLUDE_DIRS erweitert (Backup, vendor, contexts, .claude) (`d3db3881`)
- Path() fehlt in _resolve_path/_resolve_config — TypeError bei str project_root (`c90c0005`)
- import time fehlte — Watch-Modus crashed (NameError) (`03e05791`)
- eval_line enthält 'Score:' in allen Zweigen — COMMENT_REQUIRED_FIELDS Validierung (`e61127d1`)
- eval_line enthält Score auch bei Risikostufe-1-Skip (closes #11 Nebenfix) (`61cc1eba`)
- changed=[] Fallback vor try-Block — verhindert NameError in _neustart_required() (`73f12a5c`)
- WARN 3 — doppelte _build_metadata entfernt + Agent-Metadaten (`4bcfeaad`)
- doppelte _build_metadata() entfernt + System-Infos → Agent-Metadaten (WARN 3) (`bb0c2573`)
- baseline_score statt max_score + dynamischer Neustart-Hinweis (WARN 1+2) (`9346f4f0`)
- _save_score_history() bei baseline_created — erster Lauf in score_history (closes #25) (`6be6ca09`)
- COMMENT_REQUIRED_FIELDS[completion] — Eval + Score als Pflichtfeld (closes #24) (`211f7d50`)
- Risk-Gate in cmd_pr() — Risiko 1 überspringt Eval (closes #18) (`93414369`)
- Eval-Status und Session-Status auf separate Zeilen trennen (`2ba1e80a`)
- create_pr() gibt bestehenden PR zurück bei 409 (kein Crash bei Retry) (`61447884`)
- _format_history_block gibt Verlauf-Header zurück wenn keine History (closes #10 partial) (`dc84a582`)
- --eval-after-restart implementieren — COOKBOOK/Code Diskrepanz beheben (closes #10) (`57dee635`)
- Output-Validierung — _validate_comment() + COMMENT_REQUIRED_FIELDS (closes #8) (`a7900ba5`)
- Eval-Prüfung nur wenn agent_eval.json existiert (refs #6) (`501ac6bc`)
- sleep(2) zwischen Steps — Jetson LLM-Cooldown bei mehrstufigen Tests (`132f1a2d`)
- Eval-Fehler mit Gitea-Warnung + --summary Hinweis wenn leer (`17512cf5`)
- _ping HEAD→GET, eindeutige eval_user-ID, prompt statt message (refs #1) (`f674d0ee`)
- Label-Kette + Metadaten-Block + help-wanted Korrektur (`d39112da`)

### Refactoring
- evaluation.py von jetson-llm-chat entkoppeln (closes #89) (`75cbe428`)
- patch.py + changelog.py in plugins/ auslagern (closes #79) (`6d0c13f9`)
- Konsolenausgabe auf Statusmeldungen reduziert (`b2f49239`)
- settings.py + log.py + hardcoded Werte zentralisiert (`08748bd4`)

### Tests
- Unit-Tests für kritische reine Funktionen (closes #78) (`f5903d93`)

### Dokumentation
- Dashboard-Preview SVG + README Einbindung (`d10e6ab7`)
- Architektur-Erklärung — ein Agent, beliebig viele Projekte (`e38bb993`)
- Kapitel 1 — --setup als primärer Einstieg, manuell als Fallback (`4e4a78f6`)
- private Projektnamen anonymisiert, Legacy-Sektion entfernt (`b1894c08`)
- README + COOKBOOK — neue Features, private Daten anonymisiert (`c5b04954`)
- README aktualisiert — #77/#78/#79 in Roadmap, --generate-tests in Verwendung (`c693e4e0`)
- Gitea-Versionsvergleich (#59) in README + COOKBOOK Kapitel 18 (`5dfc9e21`)
- Codesegment-Strategie (#72) in README + COOKBOOK Kapitel 17 (`3a101c03`)
- SEARCH/REPLACE (#58) in README + COOKBOOK Kapitel 16 (`630c11da`)
- Diff-Validierung (#57) in README + COOKBOOK Kapitel 15 (`65a4e5fb`)
- AST-Skelett (#68) in README Roadmap + COOKBOOK Kapitel 14 (`74b63eed`)
- README Roadmap #65 + Gemini-Warnung; COOKBOOK Kapitel 12 Querverweise (`b0fb7b19`)
- Sitzungs-Protokoll 2026-03-23 — Änderungen + Gemini CLI Warnung (`7741c6bb`)
- README aktualisiert — Consecutive-Pass Gate, Betriebsmodi, Pflicht-Kette (`9a0c0019`)
- Feature-Übersicht in README erweitern (`d5e1e484`)
- Roadmap und Cookbook aktualisieren (`9a17348b`)
- README vollständig LLM-agnostisch formuliert (`e39474ab`)
- LLM-Anbindung (manuell vs. API) und Roadmap ausgebaut (`e821051c`)
- Feature-Übersicht mit Beschreibungen und Mini-Beispielen ausgebaut (`b518bfe8`)
- README — Intro und Feature-Übersicht ergänzt (`42809fb0`)
- konfigurierbare Excludes + contexts/open/ Struktur nachgezogen (`e37657f0`)
- agent_start.py.md + evaluation.py.md nachgezogen (Kontext-Loader Fixes, Pfade) (`1b43c7cc`)
- README + COOKBOOK — zentrale Agent-Instanz, Migrationsrezept (#35) (`5a62e026`)
- README + COOKBOOK — Kontext-Loader (Import-Analyse + grep) dokumentiert (#33) (`63635d87`)
- agent_start.py.md — Kontext-Loader dokumentiert (#33) (`fdbe29dc`)
- README + COOKBOOK — Auto-Issue strukturierter Body dokumentiert (#29) (`42997de8`)
- evaluation.py.md + agent_start.py.md — Issue #29 dokumentiert (closes #29) (`8454793e`)
- README + COOKBOOK — Server-Staleness-Check, --force, --restart-before-eval dokumentiert (`0d530d57`)
- README + COOKBOOK aktualisiert — Prozess-Enforcement, Watch-Szenario 2, log_analyzer (closes #11) (`dc7f98d9`)
- COOKBOOK.md — 10 Rezepte mit Skynet-Referenz (closes #5) (`3d2b8ebd`)
- README — Watch-Modus, score_history, Auto-Baseline, History-in-Comments (`087b96d8`)
- README — Risiko ≥ 2 Ausnahme + steps sleep(2) + Eval-Fehler-Verhalten (`496b6caa`)
- README — Eval-System Abschnitt ergänzt (evaluation.py, agent_eval.json, steps, baseline) (`1f2ccf64`)
- evaluation.py.md + README Eval-Abschnitt (refs #1) (`5f0e6dbc`)
- README komplett überarbeitet (`702e911b`)
- Problemstellung + Lösungsansätze für LLM-Trigger vollständig dokumentiert (`a4666356`)
- README Auto-Modus Diagramm + --root Fix, Docstrings vervollständigt (`e24fbb77`)

### Wartung
- Projektstruktur bereinigt — scripts/, data/, .gitignore (closes #92) (`d5c0c6de`)
- push_github.sh — Mirror-Script für GitHub (`115f83a1`)
- add gemini_start.sh for Gemini CLI workflow (closes #61) (`403f86af`)
- contexts/ und gitea-agent.log zu .gitignore hinzufügen (`5f115235`)

### Sonstiges
- Merge pull request '[04] Projekt-Profil: project.json + Feature-Flags für modularen Betrieb (closes #90)' (#98) from chore/issue-90-04-projekt-profil-project-json-fea into main (`7d9c0bef`)
- Merge pull request '[03] Refactor: evaluation.py von jetson-llm-chat entkoppeln (closes #89)' (#97) from chore/issue-89-03-refactor-evaluation-py-von-jets into main (`0e01452e`)
- Merge pull request '[02] Projektstruktur: saubere Trennung Code / Laufzeit / Config / Scripts (closes #92)' (#96) from chore/issue-92-02-projektstruktur-saubere-trennun into main (`132bb780`)
- Merge pull request 'fix: DASHBOARD_PATH korrekt in agent/data/ (closes #86)' (#93) from fix/issue-86-dashboard-path into main (`fef9fe92`)
- .env.agent aus Tracking entfernen + .gitignore ergänzt (`a2c7765c`)
- Merge pull request '--setup: Interaktiver Einrichtungs-Wizard für neue Projekte (closes #77)' (#84) from feat/issue-77-setup-wizard into main (`e884301b`)
- Merge pull request 'Inkrementelles Refactoring: patch.py + changelog.py auslagern (closes #79)' (#83) from refactor/issue-79-plugins-patch-changelog into main (`7f7edfb5`)
- Merge pull request 'Auto-Changelog-Generator (closes #39)' (#75) from feat/issue-39-auto-changelog-generator into main (`73799515`)
- Merge pull request '5 Gitea Versionsvergleich (closes #59)' (#74) from chore/issue-59-5-gitea-versionsvergleich into main (`a53138ad`)
- Merge pull request 'Flächendeckende Codesegment-Strategie — LLM lädt nur noch relevante Blöcke (closes #72)' (#73) from chore/issue-72-flaechendeckende-codesegment-strate into main (`9c94cf3e`)
- Merge pull request '4 SEARCH/REPLACE (closes #58)' (#71) from chore/issue-58-4-search-replace into main (`ce6c46a9`)
- Merge pull request '3 Diff-Validierung (closes #57)' (#70) from chore/issue-57-3-diff-validierung into main (`56126644`)
- Merge pull request 'Titel: AST-Repository-Skelett — repo_skeleton.json bei --issue generieren (closes #68)' (#69) from chore/issue-68-titel-ast-repository-skelett-repo-s into main (`f2ce6e36`)
- Merge pull request '2 AST-Skelett (closes #56)' (#67) from chore/issue-56-2-ast-skelett into main (`978b9c3d`)
- Merge pull request 'context_export.sh — LLM-agnostischer Kontext-Export + Dual-Repo-Unterstützung (closes #65)' (#66) from feat/issue-65-context-export-dual-repo into main (`6130c6fb`)
- Merge pull request 'gemini_start.sh — Gemini CLI in Agent-Workflow integrieren (closes #61)' (#63) from fix/gemini-start-correct into main (`12050f2c`)
- Merge pull request 'gemini_start.sh — Gemini CLI in Agent-Workflow integrieren (closes #61)' (#62) from chore/issue-61-gemini-start-sh-gemini-cli-in-agent into main (`d8d48224`)
- README.md aktualisiert (`7ea06675`)
- Merge pull request 'feat: Betriebsmodi — Night / Patch / Idle (closes #52)' (#53) from feat/issue-52-betriebsmodi into main (`73c2bde8`)
- Merge pull request 'feat: Auto-Issues erst nach N aufeinanderfolgenden Passes schließen (closes #50)' (#51) from feat/issue-50-consecutive-passes into main (`7691ef48`)
- README.md aktualisiert (`c6410430`)
- Merge pull request 'feat: zentrale Agent-Instanz — Pfade aus PROJECT_ROOT/agent/ lesen (closes #35)' (#36) from feat/issue-35-zentrale-agent-instanz into main (`23a3b943`)
- Merge pull request 'feat: Kontext-Loader — Import-Analyse + Keyword-Suche für files.md (closes #33)' (#34) from feat/issue-33-kontext-loader-import-analyse into main (`0cf01850`)
- Merge pull request 'feat: Auto-Issues mit strukturiertem Kontext (closes #29)' (#32) from feat/issue-29-auto-issue-strukturierter-kontext into main (`a73ecad5`)
- Merge pull request 'pr soll vor Eval prüfen ob Server-Code aktuell ist (closes #30)' (#31) from chore/issue-30-pr-soll-vor-eval-pr-fen-ob-server-c into main (`97f34766`)
- Merge pull request 'Docs: README + COOKBOOK aktualisieren — #6/#8 Details + log_analyzer Rezept (closes #11)' (#28) from docs/issue-11-docs-readme-+-cookbook-aktualisiere into main (`f8a4c2d7`)
- Merge pull request 'Audit-Fixes — 2 FAIL + 3 WARN aus Re-Audit #23 (closes #26)' (#27) from fix/issue-26-audit-fixes into main (`9d5b9c1c`)
- Merge branch 'main' into fix/issue-26-audit-fixes (`e5499fa1`)
- Merge pull request '[Audit FAIL] Szenario 2 — Watch-Neustart-Trigger nicht implementiert (closes #19)' (#21) from fix/issue-19-szenario2-watch-neustart into main (`bb446ff3`)
- Merge branch 'main' into fix/issue-19-szenario2-watch-neustart (`d4151a5f`)
- Merge pull request '[Audit FAIL] Risiko 1 — Eval wird nicht übersprungen (kein Risk-Gate) (closes #18)' (#20) from fix/issue-18-risk-gate-eval-skip into main (`f88a163f`)
- Merge branch 'main' into fix/issue-18-risk-gate-eval-skip (`5810f47e`)
- Merge pull request '_build_metadata() — Token-Schätzung + aufklappbarer Block (closes #16)' (#17) from chore/issue-16-_build_metadata-—-token-sch-tzung-+ into main (`f577cf45`)
- Merge branch 'main' into chore/issue-16-_build_metadata-—-token-sch-tzung-+ (`3b084f70`)
- Merge pull request 'Abschluss-Kommentar — Eval-Ergebnis + Metadata + Session-Status (closes #13)' (#15) from chore/issue-13-abschluss-kommentar-—-eval-ergebnis into main (`13c023f7`)
- Merge branch 'main' into chore/issue-13-abschluss-kommentar-—-eval-ergebnis (`a914faf6`)
- Merge pull request 'Fix: --eval-after-restart Diskrepanz — COOKBOOK vs agent_start.py (closes #10)' (#12) from chore/issue-10-fix-eval-after-restart-diskrepanz-— into main (`3c6a98a5`)
- Merge pull request 'fix: Output-Validierung — _validate_comment() + COMMENT_REQUIRED_FIELDS (closes #8)' (#9) from fix/issue-8-output-validierung-kommentare into main (`3813836e`)
- Merge pull request 'Prozess-Enforcement — Kontext-Drift verhindern (closes #6)' (#7) from chore/issue-6-prozess-enforcement-—-kontext-drift into main (`3adae3a6`)
- Merge pull request 'feat: evaluation.py — generisches Eval-System vor --pr (closes #1)' (#2) from feat/issue-1-evaluation-system into main (`312913a8`)

## [Unreleased] — 2026-03-26

### Features
- Autonomous Self-Healing Loop — plugins/healing.py (`7e5d1a58`)
- Projekt-Profil + Feature-Flags (project.json) (closes #90) (`9caa5730`)
- --setup Wizard + --doctor Health-Check (closes #77) (`2f9eaa4d`)
- Auto-Changelog-Generator — --changelog [VERSION] (closes #39) (`84b96aba`)
- Gitea-Versionsvergleich — get_file_contents, _ast_diff, _gitea_version_compare (closes #59) (`2b9afc12`)
- context_export.sh — Slice-Schleife (plain) + Slice-Anweisung (file/gemini) (`db7365a7`)
- --get-slice loggt session.json + _warn_slices_not_requested in preconditions (`d005200f`)
- files.md Skelett-Modus — kein Volltext mehr (`a831c4ed`)
- --build-skeleton + _update_skeleton_incremental + _load_skeleton_map (`004afb86`)
- SEARCH/REPLACE Protokoll — Parser, _apply_patch, cmd_apply_patch, --apply-patch/--dry-run (closes #58) (`276417f7`)
- Diff-Validierung — Scope-Warnung bei Änderungen außerhalb AST-Skelett (closes #57) (`5747d3db`)
- AST-Skelett — ClassDef/FunctionDef-Extraktion, repo_skeleton.md, --get-slice (closes #68) (`ef9c6bf8`)
- use repo_skeleton.json for files.md truncation (`b4917d1e`)
- generate repo_skeleton.json in cmd_plan (`b663b3d3`)
- introduce find_relevant_files_advanced (`687dc46f`)
- context_export.sh + Dual-Repo --self Flag + COOKBOOK.md LLM-Workflow (closes #65) (`01bc9b5a`)
- Betriebsmodi — start_patch.sh, start_night.sh, stop_agent.sh, agent_status.sh (closes #52) (`b2c0500c`)
- Auto-Issues erst nach N aufeinanderfolgenden Passes schließen (closes #50) (`69969c26`)
- add agent_eval.template.json and tag check (`87e643a4`)
- implement patch mode and live dashboard (Issue #46) (`69eafcd3`)
- implement systematic tag failure detection in watch mode (Issue #47) (`fadeb902`)
- implement test generation via LLM prompt (Issue #44) (`74cc47e6`)
- implement performance benchmarking for eval (Issue #37) (`09a8bbed`)
- Add deterministic agent self-consistency check - Add agent_self_check.py for self checks - Run check in _check_pr_preconditions - Update COOKBOOK.md (`358a041d`)
- Kontext-Zusammenfassung als Gitea-Kommentar posten (`0e4f54b6`)
- Kontext-Loader — Kommentare in Keyword-Suche einbeziehen (`3239ae8c`)
- Issue-Kommentare in starter.md aufnehmen (## Diskussion) (`4390a8e6`)
- konfigurierbare Excludes + contexts/open/ Struktur (`018ee6f8`)
- zentrale Agent-Instanz — Pfade aus PROJECT_ROOT/agent/ lesen (closes #35) (`78ff23e1`)
- Kontext-Loader — Import-Analyse + Keyword-Suche für files.md (closes #33) (`c31e26d8`)
- Auto-Issues mit strukturiertem Kontext (closes #29) (`0a5aefec`)
- --pr prüft Server-Aktualität vor Eval + --force + --restart-before-eval (closes #30) (`5f152978`)
- _build_metadata() portiert (closes #74) (`3c180cde`)
- _build_metadata() portiert — Abschluss + Fixup Kommentare (closes #74) (`125a8aa7`)
- Szenario 2 Watch-Neustart-Trigger implementieren (closes #19) (`b5515e38`)
- _build_metadata() — aufklappbarer Block + Token-Schätzung (closes #16) (`ac811b24`)
- Abschluss-Kommentar — Eval-Ergebnis + Metadata + Session-Status (closes #13) (`4f17c02f`)
- Prioritätshinweis — technische Schranken vor Prompt-Regeln (refs #6) (`ee95c312`)
- Prozess-Enforcement — 5 Maßnahmen gegen Kontext-Drift (closes #6) (`9d8d0220`)
- STARTER_MD_PFLICHTREGELN in settings.py (Maßnahme 2+3, refs #6) (`43df45d7`)
- watch_interval_minutes aus agent_eval.json + README (closes #4) (`2d3944cf`)
- cmd_watch — generischer Hook für tools/log_analyzer.py im Watch-Loop (`7c8833cb`)
- score_history letzte 5 Einträge in PR- und Watch-Kommentare (`b3687240`)
- --score-history Anzeige-Befehl — Eval-Verlauf als Tabelle im Terminal (`a5237c4b`)
- --watch Modus + score_history.json + Auto-Issues bei Score-Verlust (`c1e0f782`)
- Baseline automatisch hochsetzen wenn Score besser wird (`47e8ace1`)
- steps-Mechanismus für mehrstufige Tests (refs #1) (`1ba15002`)
- evaluation.py — generisches Eval-System vor --pr (closes #1) (`4db5b01d`)
- --fixup, Docs-Check, Auto-Cleanup + Revert-Hinweis (`a2b56ddc`)
- Kontext-Dateien pro Issue (contexts/) (`6b1b3c10`)
- Textbausteine zentralisiert in settings.py (`b341e52a`)
- Auto-Scan + working-bot Auth + generische Konfiguration via .env (`b1ce8d0e`)
- auto-modus — Gitea-States selbständig erkennen und verarbeiten (`0a427f5d`)
- initial gitea-agent MVP — LLM-agnostischer Issue-Workflow (`09a897ce`)

### Bugfixes
- DASHBOARD_PATH in settings.py ergänzt + dashboard.py bereinigt (closes #86) (`54378fd6`)
- cmd_doctor — _api_get → gitea._request, agent_eval.json auch in agent/config/ suchen (`5b51fd21`)
- CONTEXT_DIR in .env.agent — contexts statt contexts/open (verschachtelte done/ korrigiert) (`e98cb258`)
- --build-skeleton ohne Größen-Limit (max_size_kb=10000) — agent_start.py vollständig analysiert (`2a8a5eba`)
- stufe in cmd_plan nicht definiert (NameError) (`3d04cb10`)
- evaluation.py — Commit-Hash in score_history.json (closes #55) (`e9a3a89c`)
- CONTEXT_DIR aus .env.agent lesen — --self Modus funktioniert (`4e90b93d`)
- gemini_start.sh korrekt implementiert — Kontext-Loader statt Wrapper (closes #61) (`fc577d8d`)
- _sync_closed_contexts() beim Watch-Start — verwaiste open/-Ordner nach done/ verschieben (`bc15ca67`)
- Labels bei Auto-Issue-Schließung entfernen (`a8662a53`)
- _close_resolved_auto_issues verschiebt Context nach done/ (`f8d13631`)
- Bot-Kommentar-Filter nach Username statt Keywords (`61d43b1c`)
- Kontext-Loader — Datei-Blacklist + Größen-Cap (50KB) (`b9d91bf7`)
- branch_name() — Sonderzeichen robust filtern (regex statt char-Liste) (`c68ce0f7`)
- Keyword-Suche nur in Code-Dateien (nicht .md) (`797023e1`)
- Kontext-Loader — Documentation/ excluden, AST depth=1 (`17c55edb`)
- _EXCLUDE_DIRS erweitert (Backup, vendor, contexts, .claude) (`d3db3881`)
- Path() fehlt in _resolve_path/_resolve_config — TypeError bei str project_root (`c90c0005`)
- import time fehlte — Watch-Modus crashed (NameError) (`03e05791`)
- eval_line enthält 'Score:' in allen Zweigen — COMMENT_REQUIRED_FIELDS Validierung (`e61127d1`)
- eval_line enthält Score auch bei Risikostufe-1-Skip (closes #11 Nebenfix) (`61cc1eba`)
- changed=[] Fallback vor try-Block — verhindert NameError in _neustart_required() (`73f12a5c`)
- WARN 3 — doppelte _build_metadata entfernt + Agent-Metadaten (`4bcfeaad`)
- doppelte _build_metadata() entfernt + System-Infos → Agent-Metadaten (WARN 3) (`bb0c2573`)
- baseline_score statt max_score + dynamischer Neustart-Hinweis (WARN 1+2) (`9346f4f0`)
- _save_score_history() bei baseline_created — erster Lauf in score_history (closes #25) (`6be6ca09`)
- COMMENT_REQUIRED_FIELDS[completion] — Eval + Score als Pflichtfeld (closes #24) (`211f7d50`)
- Risk-Gate in cmd_pr() — Risiko 1 überspringt Eval (closes #18) (`93414369`)
- Eval-Status und Session-Status auf separate Zeilen trennen (`2ba1e80a`)
- create_pr() gibt bestehenden PR zurück bei 409 (kein Crash bei Retry) (`61447884`)
- _format_history_block gibt Verlauf-Header zurück wenn keine History (closes #10 partial) (`dc84a582`)
- --eval-after-restart implementieren — COOKBOOK/Code Diskrepanz beheben (closes #10) (`57dee635`)
- Output-Validierung — _validate_comment() + COMMENT_REQUIRED_FIELDS (closes #8) (`a7900ba5`)
- Eval-Prüfung nur wenn agent_eval.json existiert (refs #6) (`501ac6bc`)
- sleep(2) zwischen Steps — Jetson LLM-Cooldown bei mehrstufigen Tests (`132f1a2d`)
- Eval-Fehler mit Gitea-Warnung + --summary Hinweis wenn leer (`17512cf5`)
- _ping HEAD→GET, eindeutige eval_user-ID, prompt statt message (refs #1) (`f674d0ee`)
- Label-Kette + Metadaten-Block + help-wanted Korrektur (`d39112da`)

### Refactoring
- evaluation.py von jetson-llm-chat entkoppeln (closes #89) (`75cbe428`)
- patch.py + changelog.py in plugins/ auslagern (closes #79) (`6d0c13f9`)
- Konsolenausgabe auf Statusmeldungen reduziert (`b2f49239`)
- settings.py + log.py + hardcoded Werte zentralisiert (`08748bd4`)

### Tests
- Unit-Tests für kritische reine Funktionen (closes #78) (`f5903d93`)

### Dokumentation
- Dashboard-Preview SVG + README Einbindung (`d10e6ab7`)
- Architektur-Erklärung — ein Agent, beliebig viele Projekte (`e38bb993`)
- Kapitel 1 — --setup als primärer Einstieg, manuell als Fallback (`4e4a78f6`)
- private Projektnamen anonymisiert, Legacy-Sektion entfernt (`b1894c08`)
- README + COOKBOOK — neue Features, private Daten anonymisiert (`c5b04954`)
- README aktualisiert — #77/#78/#79 in Roadmap, --generate-tests in Verwendung (`c693e4e0`)
- Gitea-Versionsvergleich (#59) in README + COOKBOOK Kapitel 18 (`5dfc9e21`)
- Codesegment-Strategie (#72) in README + COOKBOOK Kapitel 17 (`3a101c03`)
- SEARCH/REPLACE (#58) in README + COOKBOOK Kapitel 16 (`630c11da`)
- Diff-Validierung (#57) in README + COOKBOOK Kapitel 15 (`65a4e5fb`)
- AST-Skelett (#68) in README Roadmap + COOKBOOK Kapitel 14 (`74b63eed`)
- README Roadmap #65 + Gemini-Warnung; COOKBOOK Kapitel 12 Querverweise (`b0fb7b19`)
- Sitzungs-Protokoll 2026-03-23 — Änderungen + Gemini CLI Warnung (`7741c6bb`)
- README aktualisiert — Consecutive-Pass Gate, Betriebsmodi, Pflicht-Kette (`9a0c0019`)
- Feature-Übersicht in README erweitern (`d5e1e484`)
- Roadmap und Cookbook aktualisieren (`9a17348b`)
- README vollständig LLM-agnostisch formuliert (`e39474ab`)
- LLM-Anbindung (manuell vs. API) und Roadmap ausgebaut (`e821051c`)
- Feature-Übersicht mit Beschreibungen und Mini-Beispielen ausgebaut (`b518bfe8`)
- README — Intro und Feature-Übersicht ergänzt (`42809fb0`)
- konfigurierbare Excludes + contexts/open/ Struktur nachgezogen (`e37657f0`)
- agent_start.py.md + evaluation.py.md nachgezogen (Kontext-Loader Fixes, Pfade) (`1b43c7cc`)
- README + COOKBOOK — zentrale Agent-Instanz, Migrationsrezept (#35) (`5a62e026`)
- README + COOKBOOK — Kontext-Loader (Import-Analyse + grep) dokumentiert (#33) (`63635d87`)
- agent_start.py.md — Kontext-Loader dokumentiert (#33) (`fdbe29dc`)
- README + COOKBOOK — Auto-Issue strukturierter Body dokumentiert (#29) (`42997de8`)
- evaluation.py.md + agent_start.py.md — Issue #29 dokumentiert (closes #29) (`8454793e`)
- README + COOKBOOK — Server-Staleness-Check, --force, --restart-before-eval dokumentiert (`0d530d57`)
- README + COOKBOOK aktualisiert — Prozess-Enforcement, Watch-Szenario 2, log_analyzer (closes #11) (`dc7f98d9`)
- COOKBOOK.md — 10 Rezepte mit Skynet-Referenz (closes #5) (`3d2b8ebd`)
- README — Watch-Modus, score_history, Auto-Baseline, History-in-Comments (`087b96d8`)
- README — Risiko ≥ 2 Ausnahme + steps sleep(2) + Eval-Fehler-Verhalten (`496b6caa`)
- README — Eval-System Abschnitt ergänzt (evaluation.py, agent_eval.json, steps, baseline) (`1f2ccf64`)
- evaluation.py.md + README Eval-Abschnitt (refs #1) (`5f0e6dbc`)
- README komplett überarbeitet (`702e911b`)
- Problemstellung + Lösungsansätze für LLM-Trigger vollständig dokumentiert (`a4666356`)
- README Auto-Modus Diagramm + --root Fix, Docstrings vervollständigt (`e24fbb77`)

### Wartung
- Projektstruktur bereinigt — scripts/, data/, .gitignore (closes #92) (`d5c0c6de`)
- push_github.sh — Mirror-Script für GitHub (`115f83a1`)
- add gemini_start.sh for Gemini CLI workflow (closes #61) (`403f86af`)
- contexts/ und gitea-agent.log zu .gitignore hinzufügen (`5f115235`)

### Sonstiges
- Merge pull request '[04] Projekt-Profil: project.json + Feature-Flags für modularen Betrieb (closes #90)' (#98) from chore/issue-90-04-projekt-profil-project-json-fea into main (`7d9c0bef`)
- Merge pull request '[03] Refactor: evaluation.py von jetson-llm-chat entkoppeln (closes #89)' (#97) from chore/issue-89-03-refactor-evaluation-py-von-jets into main (`0e01452e`)
- Merge pull request '[02] Projektstruktur: saubere Trennung Code / Laufzeit / Config / Scripts (closes #92)' (#96) from chore/issue-92-02-projektstruktur-saubere-trennun into main (`132bb780`)
- Merge pull request 'fix: DASHBOARD_PATH korrekt in agent/data/ (closes #86)' (#93) from fix/issue-86-dashboard-path into main (`fef9fe92`)
- .env.agent aus Tracking entfernen + .gitignore ergänzt (`a2c7765c`)
- Merge pull request '--setup: Interaktiver Einrichtungs-Wizard für neue Projekte (closes #77)' (#84) from feat/issue-77-setup-wizard into main (`e884301b`)
- Merge pull request 'Inkrementelles Refactoring: patch.py + changelog.py auslagern (closes #79)' (#83) from refactor/issue-79-plugins-patch-changelog into main (`7f7edfb5`)
- Merge pull request 'Auto-Changelog-Generator (closes #39)' (#75) from feat/issue-39-auto-changelog-generator into main (`73799515`)
- Merge pull request '5 Gitea Versionsvergleich (closes #59)' (#74) from chore/issue-59-5-gitea-versionsvergleich into main (`a53138ad`)
- Merge pull request 'Flächendeckende Codesegment-Strategie — LLM lädt nur noch relevante Blöcke (closes #72)' (#73) from chore/issue-72-flaechendeckende-codesegment-strate into main (`9c94cf3e`)
- Merge pull request '4 SEARCH/REPLACE (closes #58)' (#71) from chore/issue-58-4-search-replace into main (`ce6c46a9`)
- Merge pull request '3 Diff-Validierung (closes #57)' (#70) from chore/issue-57-3-diff-validierung into main (`56126644`)
- Merge pull request 'Titel: AST-Repository-Skelett — repo_skeleton.json bei --issue generieren (closes #68)' (#69) from chore/issue-68-titel-ast-repository-skelett-repo-s into main (`f2ce6e36`)
- Merge pull request '2 AST-Skelett (closes #56)' (#67) from chore/issue-56-2-ast-skelett into main (`978b9c3d`)
- Merge pull request 'context_export.sh — LLM-agnostischer Kontext-Export + Dual-Repo-Unterstützung (closes #65)' (#66) from feat/issue-65-context-export-dual-repo into main (`6130c6fb`)
- Merge pull request 'gemini_start.sh — Gemini CLI in Agent-Workflow integrieren (closes #61)' (#63) from fix/gemini-start-correct into main (`12050f2c`)
- Merge pull request 'gemini_start.sh — Gemini CLI in Agent-Workflow integrieren (closes #61)' (#62) from chore/issue-61-gemini-start-sh-gemini-cli-in-agent into main (`d8d48224`)
- README.md aktualisiert (`7ea06675`)
- Merge pull request 'feat: Betriebsmodi — Night / Patch / Idle (closes #52)' (#53) from feat/issue-52-betriebsmodi into main (`73c2bde8`)
- Merge pull request 'feat: Auto-Issues erst nach N aufeinanderfolgenden Passes schließen (closes #50)' (#51) from feat/issue-50-consecutive-passes into main (`7691ef48`)
- README.md aktualisiert (`c6410430`)
- Merge pull request 'feat: zentrale Agent-Instanz — Pfade aus PROJECT_ROOT/agent/ lesen (closes #35)' (#36) from feat/issue-35-zentrale-agent-instanz into main (`23a3b943`)
- Merge pull request 'feat: Kontext-Loader — Import-Analyse + Keyword-Suche für files.md (closes #33)' (#34) from feat/issue-33-kontext-loader-import-analyse into main (`0cf01850`)
- Merge pull request 'feat: Auto-Issues mit strukturiertem Kontext (closes #29)' (#32) from feat/issue-29-auto-issue-strukturierter-kontext into main (`a73ecad5`)
- Merge pull request 'pr soll vor Eval prüfen ob Server-Code aktuell ist (closes #30)' (#31) from chore/issue-30-pr-soll-vor-eval-pr-fen-ob-server-c into main (`97f34766`)
- Merge pull request 'Docs: README + COOKBOOK aktualisieren — #6/#8 Details + log_analyzer Rezept (closes #11)' (#28) from docs/issue-11-docs-readme-+-cookbook-aktualisiere into main (`f8a4c2d7`)
- Merge pull request 'Audit-Fixes — 2 FAIL + 3 WARN aus Re-Audit #23 (closes #26)' (#27) from fix/issue-26-audit-fixes into main (`9d5b9c1c`)
- Merge branch 'main' into fix/issue-26-audit-fixes (`e5499fa1`)
- Merge pull request '[Audit FAIL] Szenario 2 — Watch-Neustart-Trigger nicht implementiert (closes #19)' (#21) from fix/issue-19-szenario2-watch-neustart into main (`bb446ff3`)
- Merge branch 'main' into fix/issue-19-szenario2-watch-neustart (`d4151a5f`)
- Merge pull request '[Audit FAIL] Risiko 1 — Eval wird nicht übersprungen (kein Risk-Gate) (closes #18)' (#20) from fix/issue-18-risk-gate-eval-skip into main (`f88a163f`)
- Merge branch 'main' into fix/issue-18-risk-gate-eval-skip (`5810f47e`)
- Merge pull request '_build_metadata() — Token-Schätzung + aufklappbarer Block (closes #16)' (#17) from chore/issue-16-_build_metadata-—-token-sch-tzung-+ into main (`f577cf45`)
- Merge branch 'main' into chore/issue-16-_build_metadata-—-token-sch-tzung-+ (`3b084f70`)
- Merge pull request 'Abschluss-Kommentar — Eval-Ergebnis + Metadata + Session-Status (closes #13)' (#15) from chore/issue-13-abschluss-kommentar-—-eval-ergebnis into main (`13c023f7`)
- Merge branch 'main' into chore/issue-13-abschluss-kommentar-—-eval-ergebnis (`a914faf6`)
- Merge pull request 'Fix: --eval-after-restart Diskrepanz — COOKBOOK vs agent_start.py (closes #10)' (#12) from chore/issue-10-fix-eval-after-restart-diskrepanz-— into main (`3c6a98a5`)
- Merge pull request 'fix: Output-Validierung — _validate_comment() + COMMENT_REQUIRED_FIELDS (closes #8)' (#9) from fix/issue-8-output-validierung-kommentare into main (`3813836e`)
- Merge pull request 'Prozess-Enforcement — Kontext-Drift verhindern (closes #6)' (#7) from chore/issue-6-prozess-enforcement-—-kontext-drift into main (`3adae3a6`)
- Merge pull request 'feat: evaluation.py — generisches Eval-System vor --pr (closes #1)' (#2) from feat/issue-1-evaluation-system into main (`312913a8`)

## [Unreleased] — 2026-03-26

### Features
- Projekt-Profil + Feature-Flags (project.json) (closes #90) (`9caa5730`)
- --setup Wizard + --doctor Health-Check (closes #77) (`2f9eaa4d`)
- Auto-Changelog-Generator — --changelog [VERSION] (closes #39) (`84b96aba`)
- Gitea-Versionsvergleich — get_file_contents, _ast_diff, _gitea_version_compare (closes #59) (`2b9afc12`)
- context_export.sh — Slice-Schleife (plain) + Slice-Anweisung (file/gemini) (`db7365a7`)
- --get-slice loggt session.json + _warn_slices_not_requested in preconditions (`d005200f`)
- files.md Skelett-Modus — kein Volltext mehr (`a831c4ed`)
- --build-skeleton + _update_skeleton_incremental + _load_skeleton_map (`004afb86`)
- SEARCH/REPLACE Protokoll — Parser, _apply_patch, cmd_apply_patch, --apply-patch/--dry-run (closes #58) (`276417f7`)
- Diff-Validierung — Scope-Warnung bei Änderungen außerhalb AST-Skelett (closes #57) (`5747d3db`)
- AST-Skelett — ClassDef/FunctionDef-Extraktion, repo_skeleton.md, --get-slice (closes #68) (`ef9c6bf8`)
- use repo_skeleton.json for files.md truncation (`b4917d1e`)
- generate repo_skeleton.json in cmd_plan (`b663b3d3`)
- introduce find_relevant_files_advanced (`687dc46f`)
- context_export.sh + Dual-Repo --self Flag + COOKBOOK.md LLM-Workflow (closes #65) (`01bc9b5a`)
- Betriebsmodi — start_patch.sh, start_night.sh, stop_agent.sh, agent_status.sh (closes #52) (`b2c0500c`)
- Auto-Issues erst nach N aufeinanderfolgenden Passes schließen (closes #50) (`69969c26`)
- add agent_eval.template.json and tag check (`87e643a4`)
- implement patch mode and live dashboard (Issue #46) (`69eafcd3`)
- implement systematic tag failure detection in watch mode (Issue #47) (`fadeb902`)
- implement test generation via LLM prompt (Issue #44) (`74cc47e6`)
- implement performance benchmarking for eval (Issue #37) (`09a8bbed`)
- Add deterministic agent self-consistency check - Add agent_self_check.py for self checks - Run check in _check_pr_preconditions - Update COOKBOOK.md (`358a041d`)
- Kontext-Zusammenfassung als Gitea-Kommentar posten (`0e4f54b6`)
- Kontext-Loader — Kommentare in Keyword-Suche einbeziehen (`3239ae8c`)
- Issue-Kommentare in starter.md aufnehmen (## Diskussion) (`4390a8e6`)
- konfigurierbare Excludes + contexts/open/ Struktur (`018ee6f8`)
- zentrale Agent-Instanz — Pfade aus PROJECT_ROOT/agent/ lesen (closes #35) (`78ff23e1`)
- Kontext-Loader — Import-Analyse + Keyword-Suche für files.md (closes #33) (`c31e26d8`)
- Auto-Issues mit strukturiertem Kontext (closes #29) (`0a5aefec`)
- --pr prüft Server-Aktualität vor Eval + --force + --restart-before-eval (closes #30) (`5f152978`)
- _build_metadata() portiert (closes #74) (`3c180cde`)
- _build_metadata() portiert — Abschluss + Fixup Kommentare (closes #74) (`125a8aa7`)
- Szenario 2 Watch-Neustart-Trigger implementieren (closes #19) (`b5515e38`)
- _build_metadata() — aufklappbarer Block + Token-Schätzung (closes #16) (`ac811b24`)
- Abschluss-Kommentar — Eval-Ergebnis + Metadata + Session-Status (closes #13) (`4f17c02f`)
- Prioritätshinweis — technische Schranken vor Prompt-Regeln (refs #6) (`ee95c312`)
- Prozess-Enforcement — 5 Maßnahmen gegen Kontext-Drift (closes #6) (`9d8d0220`)
- STARTER_MD_PFLICHTREGELN in settings.py (Maßnahme 2+3, refs #6) (`43df45d7`)
- watch_interval_minutes aus agent_eval.json + README (closes #4) (`2d3944cf`)
- cmd_watch — generischer Hook für tools/log_analyzer.py im Watch-Loop (`7c8833cb`)
- score_history letzte 5 Einträge in PR- und Watch-Kommentare (`b3687240`)
- --score-history Anzeige-Befehl — Eval-Verlauf als Tabelle im Terminal (`a5237c4b`)
- --watch Modus + score_history.json + Auto-Issues bei Score-Verlust (`c1e0f782`)
- Baseline automatisch hochsetzen wenn Score besser wird (`47e8ace1`)
- steps-Mechanismus für mehrstufige Tests (refs #1) (`1ba15002`)
- evaluation.py — generisches Eval-System vor --pr (closes #1) (`4db5b01d`)
- --fixup, Docs-Check, Auto-Cleanup + Revert-Hinweis (`a2b56ddc`)
- Kontext-Dateien pro Issue (contexts/) (`6b1b3c10`)
- Textbausteine zentralisiert in settings.py (`b341e52a`)
- Auto-Scan + working-bot Auth + generische Konfiguration via .env (`b1ce8d0e`)
- auto-modus — Gitea-States selbständig erkennen und verarbeiten (`0a427f5d`)
- initial gitea-agent MVP — LLM-agnostischer Issue-Workflow (`09a897ce`)

### Bugfixes
- Slice-Gate — Schlupflöcher-Kontrolle verschärfen (`6840f48d`)
- DASHBOARD_PATH in settings.py ergänzt + dashboard.py bereinigt (closes #86) (`54378fd6`)
- cmd_doctor — _api_get → gitea._request, agent_eval.json auch in agent/config/ suchen (`5b51fd21`)
- CONTEXT_DIR in .env.agent — contexts statt contexts/open (verschachtelte done/ korrigiert) (`e98cb258`)
- --build-skeleton ohne Größen-Limit (max_size_kb=10000) — agent_start.py vollständig analysiert (`2a8a5eba`)
- stufe in cmd_plan nicht definiert (NameError) (`3d04cb10`)
- evaluation.py — Commit-Hash in score_history.json (closes #55) (`e9a3a89c`)
- CONTEXT_DIR aus .env.agent lesen — --self Modus funktioniert (`4e90b93d`)
- gemini_start.sh korrekt implementiert — Kontext-Loader statt Wrapper (closes #61) (`fc577d8d`)
- _sync_closed_contexts() beim Watch-Start — verwaiste open/-Ordner nach done/ verschieben (`bc15ca67`)
- Labels bei Auto-Issue-Schließung entfernen (`a8662a53`)
- _close_resolved_auto_issues verschiebt Context nach done/ (`f8d13631`)
- Bot-Kommentar-Filter nach Username statt Keywords (`61d43b1c`)
- Kontext-Loader — Datei-Blacklist + Größen-Cap (50KB) (`b9d91bf7`)
- branch_name() — Sonderzeichen robust filtern (regex statt char-Liste) (`c68ce0f7`)
- Keyword-Suche nur in Code-Dateien (nicht .md) (`797023e1`)
- Kontext-Loader — Documentation/ excluden, AST depth=1 (`17c55edb`)
- _EXCLUDE_DIRS erweitert (Backup, vendor, contexts, .claude) (`d3db3881`)
- Path() fehlt in _resolve_path/_resolve_config — TypeError bei str project_root (`c90c0005`)
- import time fehlte — Watch-Modus crashed (NameError) (`03e05791`)
- eval_line enthält 'Score:' in allen Zweigen — COMMENT_REQUIRED_FIELDS Validierung (`e61127d1`)
- eval_line enthält Score auch bei Risikostufe-1-Skip (closes #11 Nebenfix) (`61cc1eba`)
- changed=[] Fallback vor try-Block — verhindert NameError in _neustart_required() (`73f12a5c`)
- WARN 3 — doppelte _build_metadata entfernt + Agent-Metadaten (`4bcfeaad`)
- doppelte _build_metadata() entfernt + System-Infos → Agent-Metadaten (WARN 3) (`bb0c2573`)
- baseline_score statt max_score + dynamischer Neustart-Hinweis (WARN 1+2) (`9346f4f0`)
- _save_score_history() bei baseline_created — erster Lauf in score_history (closes #25) (`6be6ca09`)
- COMMENT_REQUIRED_FIELDS[completion] — Eval + Score als Pflichtfeld (closes #24) (`211f7d50`)
- Risk-Gate in cmd_pr() — Risiko 1 überspringt Eval (closes #18) (`93414369`)
- Eval-Status und Session-Status auf separate Zeilen trennen (`2ba1e80a`)
- create_pr() gibt bestehenden PR zurück bei 409 (kein Crash bei Retry) (`61447884`)
- _format_history_block gibt Verlauf-Header zurück wenn keine History (closes #10 partial) (`dc84a582`)
- --eval-after-restart implementieren — COOKBOOK/Code Diskrepanz beheben (closes #10) (`57dee635`)
- Output-Validierung — _validate_comment() + COMMENT_REQUIRED_FIELDS (closes #8) (`a7900ba5`)
- Eval-Prüfung nur wenn agent_eval.json existiert (refs #6) (`501ac6bc`)
- sleep(2) zwischen Steps — Jetson LLM-Cooldown bei mehrstufigen Tests (`132f1a2d`)
- Eval-Fehler mit Gitea-Warnung + --summary Hinweis wenn leer (`17512cf5`)
- _ping HEAD→GET, eindeutige eval_user-ID, prompt statt message (refs #1) (`f674d0ee`)
- Label-Kette + Metadaten-Block + help-wanted Korrektur (`d39112da`)

### Refactoring
- evaluation.py von jetson-llm-chat entkoppeln (closes #89) (`75cbe428`)
- patch.py + changelog.py in plugins/ auslagern (closes #79) (`6d0c13f9`)
- Konsolenausgabe auf Statusmeldungen reduziert (`b2f49239`)
- settings.py + log.py + hardcoded Werte zentralisiert (`08748bd4`)

### Tests
- Unit-Tests für kritische reine Funktionen (closes #78) (`f5903d93`)

### Dokumentation
- Dashboard-Preview SVG + README Einbindung (`d10e6ab7`)
- Architektur-Erklärung — ein Agent, beliebig viele Projekte (`e38bb993`)
- Kapitel 1 — --setup als primärer Einstieg, manuell als Fallback (`4e4a78f6`)
- private Projektnamen anonymisiert, Legacy-Sektion entfernt (`b1894c08`)
- README + COOKBOOK — neue Features, private Daten anonymisiert (`c5b04954`)
- README aktualisiert — #77/#78/#79 in Roadmap, --generate-tests in Verwendung (`c693e4e0`)
- Gitea-Versionsvergleich (#59) in README + COOKBOOK Kapitel 18 (`5dfc9e21`)
- Codesegment-Strategie (#72) in README + COOKBOOK Kapitel 17 (`3a101c03`)
- SEARCH/REPLACE (#58) in README + COOKBOOK Kapitel 16 (`630c11da`)
- Diff-Validierung (#57) in README + COOKBOOK Kapitel 15 (`65a4e5fb`)
- AST-Skelett (#68) in README Roadmap + COOKBOOK Kapitel 14 (`74b63eed`)
- README Roadmap #65 + Gemini-Warnung; COOKBOOK Kapitel 12 Querverweise (`b0fb7b19`)
- Sitzungs-Protokoll 2026-03-23 — Änderungen + Gemini CLI Warnung (`7741c6bb`)
- README aktualisiert — Consecutive-Pass Gate, Betriebsmodi, Pflicht-Kette (`9a0c0019`)
- Feature-Übersicht in README erweitern (`d5e1e484`)
- Roadmap und Cookbook aktualisieren (`9a17348b`)
- README vollständig LLM-agnostisch formuliert (`e39474ab`)
- LLM-Anbindung (manuell vs. API) und Roadmap ausgebaut (`e821051c`)
- Feature-Übersicht mit Beschreibungen und Mini-Beispielen ausgebaut (`b518bfe8`)
- README — Intro und Feature-Übersicht ergänzt (`42809fb0`)
- konfigurierbare Excludes + contexts/open/ Struktur nachgezogen (`e37657f0`)
- agent_start.py.md + evaluation.py.md nachgezogen (Kontext-Loader Fixes, Pfade) (`1b43c7cc`)
- README + COOKBOOK — zentrale Agent-Instanz, Migrationsrezept (#35) (`5a62e026`)
- README + COOKBOOK — Kontext-Loader (Import-Analyse + grep) dokumentiert (#33) (`63635d87`)
- agent_start.py.md — Kontext-Loader dokumentiert (#33) (`fdbe29dc`)
- README + COOKBOOK — Auto-Issue strukturierter Body dokumentiert (#29) (`42997de8`)
- evaluation.py.md + agent_start.py.md — Issue #29 dokumentiert (closes #29) (`8454793e`)
- README + COOKBOOK — Server-Staleness-Check, --force, --restart-before-eval dokumentiert (`0d530d57`)
- README + COOKBOOK aktualisiert — Prozess-Enforcement, Watch-Szenario 2, log_analyzer (closes #11) (`dc7f98d9`)
- COOKBOOK.md — 10 Rezepte mit Skynet-Referenz (closes #5) (`3d2b8ebd`)
- README — Watch-Modus, score_history, Auto-Baseline, History-in-Comments (`087b96d8`)
- README — Risiko ≥ 2 Ausnahme + steps sleep(2) + Eval-Fehler-Verhalten (`496b6caa`)
- README — Eval-System Abschnitt ergänzt (evaluation.py, agent_eval.json, steps, baseline) (`1f2ccf64`)
- evaluation.py.md + README Eval-Abschnitt (refs #1) (`5f0e6dbc`)
- README komplett überarbeitet (`702e911b`)
- Problemstellung + Lösungsansätze für LLM-Trigger vollständig dokumentiert (`a4666356`)
- README Auto-Modus Diagramm + --root Fix, Docstrings vervollständigt (`e24fbb77`)

### Wartung
- Projektstruktur bereinigt — scripts/, data/, .gitignore (closes #92) (`d5c0c6de`)
- push_github.sh — Mirror-Script für GitHub (`115f83a1`)
- add gemini_start.sh for Gemini CLI workflow (closes #61) (`403f86af`)
- contexts/ und gitea-agent.log zu .gitignore hinzufügen (`5f115235`)

### Sonstiges
- Merge pull request '[04] Projekt-Profil: project.json + Feature-Flags für modularen Betrieb (closes #90)' (#98) from chore/issue-90-04-projekt-profil-project-json-fea into main (`7d9c0bef`)
- Merge pull request '[03] Refactor: evaluation.py von jetson-llm-chat entkoppeln (closes #89)' (#97) from chore/issue-89-03-refactor-evaluation-py-von-jets into main (`0e01452e`)
- Merge pull request '[02] Projektstruktur: saubere Trennung Code / Laufzeit / Config / Scripts (closes #92)' (#96) from chore/issue-92-02-projektstruktur-saubere-trennun into main (`132bb780`)
- Merge pull request 'fix: DASHBOARD_PATH korrekt in agent/data/ (closes #86)' (#93) from fix/issue-86-dashboard-path into main (`fef9fe92`)
- .env.agent aus Tracking entfernen + .gitignore ergänzt (`a2c7765c`)
- Merge pull request '--setup: Interaktiver Einrichtungs-Wizard für neue Projekte (closes #77)' (#84) from feat/issue-77-setup-wizard into main (`e884301b`)
- Merge pull request 'Inkrementelles Refactoring: patch.py + changelog.py auslagern (closes #79)' (#83) from refactor/issue-79-plugins-patch-changelog into main (`7f7edfb5`)
- Merge pull request 'Auto-Changelog-Generator (closes #39)' (#75) from feat/issue-39-auto-changelog-generator into main (`73799515`)
- Merge pull request '5 Gitea Versionsvergleich (closes #59)' (#74) from chore/issue-59-5-gitea-versionsvergleich into main (`a53138ad`)
- Merge pull request 'Flächendeckende Codesegment-Strategie — LLM lädt nur noch relevante Blöcke (closes #72)' (#73) from chore/issue-72-flaechendeckende-codesegment-strate into main (`9c94cf3e`)
- Merge pull request '4 SEARCH/REPLACE (closes #58)' (#71) from chore/issue-58-4-search-replace into main (`ce6c46a9`)
- Merge pull request '3 Diff-Validierung (closes #57)' (#70) from chore/issue-57-3-diff-validierung into main (`56126644`)
- Merge pull request 'Titel: AST-Repository-Skelett — repo_skeleton.json bei --issue generieren (closes #68)' (#69) from chore/issue-68-titel-ast-repository-skelett-repo-s into main (`f2ce6e36`)
- Merge pull request '2 AST-Skelett (closes #56)' (#67) from chore/issue-56-2-ast-skelett into main (`978b9c3d`)
- Merge pull request 'context_export.sh — LLM-agnostischer Kontext-Export + Dual-Repo-Unterstützung (closes #65)' (#66) from feat/issue-65-context-export-dual-repo into main (`6130c6fb`)
- Merge pull request 'gemini_start.sh — Gemini CLI in Agent-Workflow integrieren (closes #61)' (#63) from fix/gemini-start-correct into main (`12050f2c`)
- Merge pull request 'gemini_start.sh — Gemini CLI in Agent-Workflow integrieren (closes #61)' (#62) from chore/issue-61-gemini-start-sh-gemini-cli-in-agent into main (`d8d48224`)
- README.md aktualisiert (`7ea06675`)
- Merge pull request 'feat: Betriebsmodi — Night / Patch / Idle (closes #52)' (#53) from feat/issue-52-betriebsmodi into main (`73c2bde8`)
- Merge pull request 'feat: Auto-Issues erst nach N aufeinanderfolgenden Passes schließen (closes #50)' (#51) from feat/issue-50-consecutive-passes into main (`7691ef48`)
- README.md aktualisiert (`c6410430`)
- Merge pull request 'feat: zentrale Agent-Instanz — Pfade aus PROJECT_ROOT/agent/ lesen (closes #35)' (#36) from feat/issue-35-zentrale-agent-instanz into main (`23a3b943`)
- Merge pull request 'feat: Kontext-Loader — Import-Analyse + Keyword-Suche für files.md (closes #33)' (#34) from feat/issue-33-kontext-loader-import-analyse into main (`0cf01850`)
- Merge pull request 'feat: Auto-Issues mit strukturiertem Kontext (closes #29)' (#32) from feat/issue-29-auto-issue-strukturierter-kontext into main (`a73ecad5`)
- Merge pull request 'pr soll vor Eval prüfen ob Server-Code aktuell ist (closes #30)' (#31) from chore/issue-30-pr-soll-vor-eval-pr-fen-ob-server-c into main (`97f34766`)
- Merge pull request 'Docs: README + COOKBOOK aktualisieren — #6/#8 Details + log_analyzer Rezept (closes #11)' (#28) from docs/issue-11-docs-readme-+-cookbook-aktualisiere into main (`f8a4c2d7`)
- Merge pull request 'Audit-Fixes — 2 FAIL + 3 WARN aus Re-Audit #23 (closes #26)' (#27) from fix/issue-26-audit-fixes into main (`9d5b9c1c`)
- Merge branch 'main' into fix/issue-26-audit-fixes (`e5499fa1`)
- Merge pull request '[Audit FAIL] Szenario 2 — Watch-Neustart-Trigger nicht implementiert (closes #19)' (#21) from fix/issue-19-szenario2-watch-neustart into main (`bb446ff3`)
- Merge branch 'main' into fix/issue-19-szenario2-watch-neustart (`d4151a5f`)
- Merge pull request '[Audit FAIL] Risiko 1 — Eval wird nicht übersprungen (kein Risk-Gate) (closes #18)' (#20) from fix/issue-18-risk-gate-eval-skip into main (`f88a163f`)
- Merge branch 'main' into fix/issue-18-risk-gate-eval-skip (`5810f47e`)
- Merge pull request '_build_metadata() — Token-Schätzung + aufklappbarer Block (closes #16)' (#17) from chore/issue-16-_build_metadata-—-token-sch-tzung-+ into main (`f577cf45`)
- Merge branch 'main' into chore/issue-16-_build_metadata-—-token-sch-tzung-+ (`3b084f70`)
- Merge pull request 'Abschluss-Kommentar — Eval-Ergebnis + Metadata + Session-Status (closes #13)' (#15) from chore/issue-13-abschluss-kommentar-—-eval-ergebnis into main (`13c023f7`)
- Merge branch 'main' into chore/issue-13-abschluss-kommentar-—-eval-ergebnis (`a914faf6`)
- Merge pull request 'Fix: --eval-after-restart Diskrepanz — COOKBOOK vs agent_start.py (closes #10)' (#12) from chore/issue-10-fix-eval-after-restart-diskrepanz-— into main (`3c6a98a5`)
- Merge pull request 'fix: Output-Validierung — _validate_comment() + COMMENT_REQUIRED_FIELDS (closes #8)' (#9) from fix/issue-8-output-validierung-kommentare into main (`3813836e`)
- Merge pull request 'Prozess-Enforcement — Kontext-Drift verhindern (closes #6)' (#7) from chore/issue-6-prozess-enforcement-—-kontext-drift into main (`3adae3a6`)
- Merge pull request 'feat: evaluation.py — generisches Eval-System vor --pr (closes #1)' (#2) from feat/issue-1-evaluation-system into main (`312913a8`)

## [Unreleased] — 2026-03-26

### Features
- Token-Budget-Tracker — Slice-Token zählen + warnen (`29690495`)
- Projekt-Profil + Feature-Flags (project.json) (closes #90) (`9caa5730`)
- --setup Wizard + --doctor Health-Check (closes #77) (`2f9eaa4d`)
- Auto-Changelog-Generator — --changelog [VERSION] (closes #39) (`84b96aba`)
- Gitea-Versionsvergleich — get_file_contents, _ast_diff, _gitea_version_compare (closes #59) (`2b9afc12`)
- context_export.sh — Slice-Schleife (plain) + Slice-Anweisung (file/gemini) (`db7365a7`)
- --get-slice loggt session.json + _warn_slices_not_requested in preconditions (`d005200f`)
- files.md Skelett-Modus — kein Volltext mehr (`a831c4ed`)
- --build-skeleton + _update_skeleton_incremental + _load_skeleton_map (`004afb86`)
- SEARCH/REPLACE Protokoll — Parser, _apply_patch, cmd_apply_patch, --apply-patch/--dry-run (closes #58) (`276417f7`)
- Diff-Validierung — Scope-Warnung bei Änderungen außerhalb AST-Skelett (closes #57) (`5747d3db`)
- AST-Skelett — ClassDef/FunctionDef-Extraktion, repo_skeleton.md, --get-slice (closes #68) (`ef9c6bf8`)
- use repo_skeleton.json for files.md truncation (`b4917d1e`)
- generate repo_skeleton.json in cmd_plan (`b663b3d3`)
- introduce find_relevant_files_advanced (`687dc46f`)
- context_export.sh + Dual-Repo --self Flag + COOKBOOK.md LLM-Workflow (closes #65) (`01bc9b5a`)
- Betriebsmodi — start_patch.sh, start_night.sh, stop_agent.sh, agent_status.sh (closes #52) (`b2c0500c`)
- Auto-Issues erst nach N aufeinanderfolgenden Passes schließen (closes #50) (`69969c26`)
- add agent_eval.template.json and tag check (`87e643a4`)
- implement patch mode and live dashboard (Issue #46) (`69eafcd3`)
- implement systematic tag failure detection in watch mode (Issue #47) (`fadeb902`)
- implement test generation via LLM prompt (Issue #44) (`74cc47e6`)
- implement performance benchmarking for eval (Issue #37) (`09a8bbed`)
- Add deterministic agent self-consistency check - Add agent_self_check.py for self checks - Run check in _check_pr_preconditions - Update COOKBOOK.md (`358a041d`)
- Kontext-Zusammenfassung als Gitea-Kommentar posten (`0e4f54b6`)
- Kontext-Loader — Kommentare in Keyword-Suche einbeziehen (`3239ae8c`)
- Issue-Kommentare in starter.md aufnehmen (## Diskussion) (`4390a8e6`)
- konfigurierbare Excludes + contexts/open/ Struktur (`018ee6f8`)
- zentrale Agent-Instanz — Pfade aus PROJECT_ROOT/agent/ lesen (closes #35) (`78ff23e1`)
- Kontext-Loader — Import-Analyse + Keyword-Suche für files.md (closes #33) (`c31e26d8`)
- Auto-Issues mit strukturiertem Kontext (closes #29) (`0a5aefec`)
- --pr prüft Server-Aktualität vor Eval + --force + --restart-before-eval (closes #30) (`5f152978`)
- _build_metadata() portiert (closes #74) (`3c180cde`)
- _build_metadata() portiert — Abschluss + Fixup Kommentare (closes #74) (`125a8aa7`)
- Szenario 2 Watch-Neustart-Trigger implementieren (closes #19) (`b5515e38`)
- _build_metadata() — aufklappbarer Block + Token-Schätzung (closes #16) (`ac811b24`)
- Abschluss-Kommentar — Eval-Ergebnis + Metadata + Session-Status (closes #13) (`4f17c02f`)
- Prioritätshinweis — technische Schranken vor Prompt-Regeln (refs #6) (`ee95c312`)
- Prozess-Enforcement — 5 Maßnahmen gegen Kontext-Drift (closes #6) (`9d8d0220`)
- STARTER_MD_PFLICHTREGELN in settings.py (Maßnahme 2+3, refs #6) (`43df45d7`)
- watch_interval_minutes aus agent_eval.json + README (closes #4) (`2d3944cf`)
- cmd_watch — generischer Hook für tools/log_analyzer.py im Watch-Loop (`7c8833cb`)
- score_history letzte 5 Einträge in PR- und Watch-Kommentare (`b3687240`)
- --score-history Anzeige-Befehl — Eval-Verlauf als Tabelle im Terminal (`a5237c4b`)
- --watch Modus + score_history.json + Auto-Issues bei Score-Verlust (`c1e0f782`)
- Baseline automatisch hochsetzen wenn Score besser wird (`47e8ace1`)
- steps-Mechanismus für mehrstufige Tests (refs #1) (`1ba15002`)
- evaluation.py — generisches Eval-System vor --pr (closes #1) (`4db5b01d`)
- --fixup, Docs-Check, Auto-Cleanup + Revert-Hinweis (`a2b56ddc`)
- Kontext-Dateien pro Issue (contexts/) (`6b1b3c10`)
- Textbausteine zentralisiert in settings.py (`b341e52a`)
- Auto-Scan + working-bot Auth + generische Konfiguration via .env (`b1ce8d0e`)
- auto-modus — Gitea-States selbständig erkennen und verarbeiten (`0a427f5d`)
- initial gitea-agent MVP — LLM-agnostischer Issue-Workflow (`09a897ce`)

### Bugfixes
- DASHBOARD_PATH in settings.py ergänzt + dashboard.py bereinigt (closes #86) (`54378fd6`)
- cmd_doctor — _api_get → gitea._request, agent_eval.json auch in agent/config/ suchen (`5b51fd21`)
- CONTEXT_DIR in .env.agent — contexts statt contexts/open (verschachtelte done/ korrigiert) (`e98cb258`)
- --build-skeleton ohne Größen-Limit (max_size_kb=10000) — agent_start.py vollständig analysiert (`2a8a5eba`)
- stufe in cmd_plan nicht definiert (NameError) (`3d04cb10`)
- evaluation.py — Commit-Hash in score_history.json (closes #55) (`e9a3a89c`)
- CONTEXT_DIR aus .env.agent lesen — --self Modus funktioniert (`4e90b93d`)
- gemini_start.sh korrekt implementiert — Kontext-Loader statt Wrapper (closes #61) (`fc577d8d`)
- _sync_closed_contexts() beim Watch-Start — verwaiste open/-Ordner nach done/ verschieben (`bc15ca67`)
- Labels bei Auto-Issue-Schließung entfernen (`a8662a53`)
- _close_resolved_auto_issues verschiebt Context nach done/ (`f8d13631`)
- Bot-Kommentar-Filter nach Username statt Keywords (`61d43b1c`)
- Kontext-Loader — Datei-Blacklist + Größen-Cap (50KB) (`b9d91bf7`)
- branch_name() — Sonderzeichen robust filtern (regex statt char-Liste) (`c68ce0f7`)
- Keyword-Suche nur in Code-Dateien (nicht .md) (`797023e1`)
- Kontext-Loader — Documentation/ excluden, AST depth=1 (`17c55edb`)
- _EXCLUDE_DIRS erweitert (Backup, vendor, contexts, .claude) (`d3db3881`)
- Path() fehlt in _resolve_path/_resolve_config — TypeError bei str project_root (`c90c0005`)
- import time fehlte — Watch-Modus crashed (NameError) (`03e05791`)
- eval_line enthält 'Score:' in allen Zweigen — COMMENT_REQUIRED_FIELDS Validierung (`e61127d1`)
- eval_line enthält Score auch bei Risikostufe-1-Skip (closes #11 Nebenfix) (`61cc1eba`)
- changed=[] Fallback vor try-Block — verhindert NameError in _neustart_required() (`73f12a5c`)
- WARN 3 — doppelte _build_metadata entfernt + Agent-Metadaten (`4bcfeaad`)
- doppelte _build_metadata() entfernt + System-Infos → Agent-Metadaten (WARN 3) (`bb0c2573`)
- baseline_score statt max_score + dynamischer Neustart-Hinweis (WARN 1+2) (`9346f4f0`)
- _save_score_history() bei baseline_created — erster Lauf in score_history (closes #25) (`6be6ca09`)
- COMMENT_REQUIRED_FIELDS[completion] — Eval + Score als Pflichtfeld (closes #24) (`211f7d50`)
- Risk-Gate in cmd_pr() — Risiko 1 überspringt Eval (closes #18) (`93414369`)
- Eval-Status und Session-Status auf separate Zeilen trennen (`2ba1e80a`)
- create_pr() gibt bestehenden PR zurück bei 409 (kein Crash bei Retry) (`61447884`)
- _format_history_block gibt Verlauf-Header zurück wenn keine History (closes #10 partial) (`dc84a582`)
- --eval-after-restart implementieren — COOKBOOK/Code Diskrepanz beheben (closes #10) (`57dee635`)
- Output-Validierung — _validate_comment() + COMMENT_REQUIRED_FIELDS (closes #8) (`a7900ba5`)
- Eval-Prüfung nur wenn agent_eval.json existiert (refs #6) (`501ac6bc`)
- sleep(2) zwischen Steps — Jetson LLM-Cooldown bei mehrstufigen Tests (`132f1a2d`)
- Eval-Fehler mit Gitea-Warnung + --summary Hinweis wenn leer (`17512cf5`)
- _ping HEAD→GET, eindeutige eval_user-ID, prompt statt message (refs #1) (`f674d0ee`)
- Label-Kette + Metadaten-Block + help-wanted Korrektur (`d39112da`)

### Refactoring
- evaluation.py von jetson-llm-chat entkoppeln (closes #89) (`75cbe428`)
- patch.py + changelog.py in plugins/ auslagern (closes #79) (`6d0c13f9`)
- Konsolenausgabe auf Statusmeldungen reduziert (`b2f49239`)
- settings.py + log.py + hardcoded Werte zentralisiert (`08748bd4`)

### Tests
- Unit-Tests für kritische reine Funktionen (closes #78) (`f5903d93`)

### Dokumentation
- Dashboard-Preview SVG + README Einbindung (`d10e6ab7`)
- Architektur-Erklärung — ein Agent, beliebig viele Projekte (`e38bb993`)
- Kapitel 1 — --setup als primärer Einstieg, manuell als Fallback (`4e4a78f6`)
- private Projektnamen anonymisiert, Legacy-Sektion entfernt (`b1894c08`)
- README + COOKBOOK — neue Features, private Daten anonymisiert (`c5b04954`)
- README aktualisiert — #77/#78/#79 in Roadmap, --generate-tests in Verwendung (`c693e4e0`)
- Gitea-Versionsvergleich (#59) in README + COOKBOOK Kapitel 18 (`5dfc9e21`)
- Codesegment-Strategie (#72) in README + COOKBOOK Kapitel 17 (`3a101c03`)
- SEARCH/REPLACE (#58) in README + COOKBOOK Kapitel 16 (`630c11da`)
- Diff-Validierung (#57) in README + COOKBOOK Kapitel 15 (`65a4e5fb`)
- AST-Skelett (#68) in README Roadmap + COOKBOOK Kapitel 14 (`74b63eed`)
- README Roadmap #65 + Gemini-Warnung; COOKBOOK Kapitel 12 Querverweise (`b0fb7b19`)
- Sitzungs-Protokoll 2026-03-23 — Änderungen + Gemini CLI Warnung (`7741c6bb`)
- README aktualisiert — Consecutive-Pass Gate, Betriebsmodi, Pflicht-Kette (`9a0c0019`)
- Feature-Übersicht in README erweitern (`d5e1e484`)
- Roadmap und Cookbook aktualisieren (`9a17348b`)
- README vollständig LLM-agnostisch formuliert (`e39474ab`)
- LLM-Anbindung (manuell vs. API) und Roadmap ausgebaut (`e821051c`)
- Feature-Übersicht mit Beschreibungen und Mini-Beispielen ausgebaut (`b518bfe8`)
- README — Intro und Feature-Übersicht ergänzt (`42809fb0`)
- konfigurierbare Excludes + contexts/open/ Struktur nachgezogen (`e37657f0`)
- agent_start.py.md + evaluation.py.md nachgezogen (Kontext-Loader Fixes, Pfade) (`1b43c7cc`)
- README + COOKBOOK — zentrale Agent-Instanz, Migrationsrezept (#35) (`5a62e026`)
- README + COOKBOOK — Kontext-Loader (Import-Analyse + grep) dokumentiert (#33) (`63635d87`)
- agent_start.py.md — Kontext-Loader dokumentiert (#33) (`fdbe29dc`)
- README + COOKBOOK — Auto-Issue strukturierter Body dokumentiert (#29) (`42997de8`)
- evaluation.py.md + agent_start.py.md — Issue #29 dokumentiert (closes #29) (`8454793e`)
- README + COOKBOOK — Server-Staleness-Check, --force, --restart-before-eval dokumentiert (`0d530d57`)
- README + COOKBOOK aktualisiert — Prozess-Enforcement, Watch-Szenario 2, log_analyzer (closes #11) (`dc7f98d9`)
- COOKBOOK.md — 10 Rezepte mit Skynet-Referenz (closes #5) (`3d2b8ebd`)
- README — Watch-Modus, score_history, Auto-Baseline, History-in-Comments (`087b96d8`)
- README — Risiko ≥ 2 Ausnahme + steps sleep(2) + Eval-Fehler-Verhalten (`496b6caa`)
- README — Eval-System Abschnitt ergänzt (evaluation.py, agent_eval.json, steps, baseline) (`1f2ccf64`)
- evaluation.py.md + README Eval-Abschnitt (refs #1) (`5f0e6dbc`)
- README komplett überarbeitet (`702e911b`)
- Problemstellung + Lösungsansätze für LLM-Trigger vollständig dokumentiert (`a4666356`)
- README Auto-Modus Diagramm + --root Fix, Docstrings vervollständigt (`e24fbb77`)

### Wartung
- TOKEN_BUDGET_WARN + TOKEN_LINES_FACTOR in .env.example (`fb738fad`)
- Projektstruktur bereinigt — scripts/, data/, .gitignore (closes #92) (`d5c0c6de`)
- push_github.sh — Mirror-Script für GitHub (`115f83a1`)
- add gemini_start.sh for Gemini CLI workflow (closes #61) (`403f86af`)
- contexts/ und gitea-agent.log zu .gitignore hinzufügen (`5f115235`)

### Sonstiges
- Merge pull request '[04] Projekt-Profil: project.json + Feature-Flags für modularen Betrieb (closes #90)' (#98) from chore/issue-90-04-projekt-profil-project-json-fea into main (`7d9c0bef`)
- Merge pull request '[03] Refactor: evaluation.py von jetson-llm-chat entkoppeln (closes #89)' (#97) from chore/issue-89-03-refactor-evaluation-py-von-jets into main (`0e01452e`)
- Merge pull request '[02] Projektstruktur: saubere Trennung Code / Laufzeit / Config / Scripts (closes #92)' (#96) from chore/issue-92-02-projektstruktur-saubere-trennun into main (`132bb780`)
- Merge pull request 'fix: DASHBOARD_PATH korrekt in agent/data/ (closes #86)' (#93) from fix/issue-86-dashboard-path into main (`fef9fe92`)
- .env.agent aus Tracking entfernen + .gitignore ergänzt (`a2c7765c`)
- Merge pull request '--setup: Interaktiver Einrichtungs-Wizard für neue Projekte (closes #77)' (#84) from feat/issue-77-setup-wizard into main (`e884301b`)
- Merge pull request 'Inkrementelles Refactoring: patch.py + changelog.py auslagern (closes #79)' (#83) from refactor/issue-79-plugins-patch-changelog into main (`7f7edfb5`)
- Merge pull request 'Auto-Changelog-Generator (closes #39)' (#75) from feat/issue-39-auto-changelog-generator into main (`73799515`)
- Merge pull request '5 Gitea Versionsvergleich (closes #59)' (#74) from chore/issue-59-5-gitea-versionsvergleich into main (`a53138ad`)
- Merge pull request 'Flächendeckende Codesegment-Strategie — LLM lädt nur noch relevante Blöcke (closes #72)' (#73) from chore/issue-72-flaechendeckende-codesegment-strate into main (`9c94cf3e`)
- Merge pull request '4 SEARCH/REPLACE (closes #58)' (#71) from chore/issue-58-4-search-replace into main (`ce6c46a9`)
- Merge pull request '3 Diff-Validierung (closes #57)' (#70) from chore/issue-57-3-diff-validierung into main (`56126644`)
- Merge pull request 'Titel: AST-Repository-Skelett — repo_skeleton.json bei --issue generieren (closes #68)' (#69) from chore/issue-68-titel-ast-repository-skelett-repo-s into main (`f2ce6e36`)
- Merge pull request '2 AST-Skelett (closes #56)' (#67) from chore/issue-56-2-ast-skelett into main (`978b9c3d`)
- Merge pull request 'context_export.sh — LLM-agnostischer Kontext-Export + Dual-Repo-Unterstützung (closes #65)' (#66) from feat/issue-65-context-export-dual-repo into main (`6130c6fb`)
- Merge pull request 'gemini_start.sh — Gemini CLI in Agent-Workflow integrieren (closes #61)' (#63) from fix/gemini-start-correct into main (`12050f2c`)
- Merge pull request 'gemini_start.sh — Gemini CLI in Agent-Workflow integrieren (closes #61)' (#62) from chore/issue-61-gemini-start-sh-gemini-cli-in-agent into main (`d8d48224`)
- README.md aktualisiert (`7ea06675`)
- Merge pull request 'feat: Betriebsmodi — Night / Patch / Idle (closes #52)' (#53) from feat/issue-52-betriebsmodi into main (`73c2bde8`)
- Merge pull request 'feat: Auto-Issues erst nach N aufeinanderfolgenden Passes schließen (closes #50)' (#51) from feat/issue-50-consecutive-passes into main (`7691ef48`)
- README.md aktualisiert (`c6410430`)
- Merge pull request 'feat: zentrale Agent-Instanz — Pfade aus PROJECT_ROOT/agent/ lesen (closes #35)' (#36) from feat/issue-35-zentrale-agent-instanz into main (`23a3b943`)
- Merge pull request 'feat: Kontext-Loader — Import-Analyse + Keyword-Suche für files.md (closes #33)' (#34) from feat/issue-33-kontext-loader-import-analyse into main (`0cf01850`)
- Merge pull request 'feat: Auto-Issues mit strukturiertem Kontext (closes #29)' (#32) from feat/issue-29-auto-issue-strukturierter-kontext into main (`a73ecad5`)
- Merge pull request 'pr soll vor Eval prüfen ob Server-Code aktuell ist (closes #30)' (#31) from chore/issue-30-pr-soll-vor-eval-pr-fen-ob-server-c into main (`97f34766`)
- Merge pull request 'Docs: README + COOKBOOK aktualisieren — #6/#8 Details + log_analyzer Rezept (closes #11)' (#28) from docs/issue-11-docs-readme-+-cookbook-aktualisiere into main (`f8a4c2d7`)
- Merge pull request 'Audit-Fixes — 2 FAIL + 3 WARN aus Re-Audit #23 (closes #26)' (#27) from fix/issue-26-audit-fixes into main (`9d5b9c1c`)
- Merge branch 'main' into fix/issue-26-audit-fixes (`e5499fa1`)
- Merge pull request '[Audit FAIL] Szenario 2 — Watch-Neustart-Trigger nicht implementiert (closes #19)' (#21) from fix/issue-19-szenario2-watch-neustart into main (`bb446ff3`)
- Merge branch 'main' into fix/issue-19-szenario2-watch-neustart (`d4151a5f`)
- Merge pull request '[Audit FAIL] Risiko 1 — Eval wird nicht übersprungen (kein Risk-Gate) (closes #18)' (#20) from fix/issue-18-risk-gate-eval-skip into main (`f88a163f`)
- Merge branch 'main' into fix/issue-18-risk-gate-eval-skip (`5810f47e`)
- Merge pull request '_build_metadata() — Token-Schätzung + aufklappbarer Block (closes #16)' (#17) from chore/issue-16-_build_metadata-—-token-sch-tzung-+ into main (`f577cf45`)
- Merge branch 'main' into chore/issue-16-_build_metadata-—-token-sch-tzung-+ (`3b084f70`)
- Merge pull request 'Abschluss-Kommentar — Eval-Ergebnis + Metadata + Session-Status (closes #13)' (#15) from chore/issue-13-abschluss-kommentar-—-eval-ergebnis into main (`13c023f7`)
- Merge branch 'main' into chore/issue-13-abschluss-kommentar-—-eval-ergebnis (`a914faf6`)
- Merge pull request 'Fix: --eval-after-restart Diskrepanz — COOKBOOK vs agent_start.py (closes #10)' (#12) from chore/issue-10-fix-eval-after-restart-diskrepanz-— into main (`3c6a98a5`)
- Merge pull request 'fix: Output-Validierung — _validate_comment() + COMMENT_REQUIRED_FIELDS (closes #8)' (#9) from fix/issue-8-output-validierung-kommentare into main (`3813836e`)
- Merge pull request 'Prozess-Enforcement — Kontext-Drift verhindern (closes #6)' (#7) from chore/issue-6-prozess-enforcement-—-kontext-drift into main (`3adae3a6`)
- Merge pull request 'feat: evaluation.py — generisches Eval-System vor --pr (closes #1)' (#2) from feat/issue-1-evaluation-system into main (`312913a8`)

## [Unreleased] — 2026-03-26

### Features
- LLM-Log-Analyse — log_analyzer.template.py (`b6157410`)
- Health-Check Engine — plugins/health.py (`e2496ab1`)
- Projekt-Profil + Feature-Flags (project.json) (closes #90) (`9caa5730`)
- --setup Wizard + --doctor Health-Check (closes #77) (`2f9eaa4d`)
- Auto-Changelog-Generator — --changelog [VERSION] (closes #39) (`84b96aba`)
- Gitea-Versionsvergleich — get_file_contents, _ast_diff, _gitea_version_compare (closes #59) (`2b9afc12`)
- context_export.sh — Slice-Schleife (plain) + Slice-Anweisung (file/gemini) (`db7365a7`)
- --get-slice loggt session.json + _warn_slices_not_requested in preconditions (`d005200f`)
- files.md Skelett-Modus — kein Volltext mehr (`a831c4ed`)
- --build-skeleton + _update_skeleton_incremental + _load_skeleton_map (`004afb86`)
- SEARCH/REPLACE Protokoll — Parser, _apply_patch, cmd_apply_patch, --apply-patch/--dry-run (closes #58) (`276417f7`)
- Diff-Validierung — Scope-Warnung bei Änderungen außerhalb AST-Skelett (closes #57) (`5747d3db`)
- AST-Skelett — ClassDef/FunctionDef-Extraktion, repo_skeleton.md, --get-slice (closes #68) (`ef9c6bf8`)
- use repo_skeleton.json for files.md truncation (`b4917d1e`)
- generate repo_skeleton.json in cmd_plan (`b663b3d3`)
- introduce find_relevant_files_advanced (`687dc46f`)
- context_export.sh + Dual-Repo --self Flag + COOKBOOK.md LLM-Workflow (closes #65) (`01bc9b5a`)
- Betriebsmodi — start_patch.sh, start_night.sh, stop_agent.sh, agent_status.sh (closes #52) (`b2c0500c`)
- Auto-Issues erst nach N aufeinanderfolgenden Passes schließen (closes #50) (`69969c26`)
- add agent_eval.template.json and tag check (`87e643a4`)
- implement patch mode and live dashboard (Issue #46) (`69eafcd3`)
- implement systematic tag failure detection in watch mode (Issue #47) (`fadeb902`)
- implement test generation via LLM prompt (Issue #44) (`74cc47e6`)
- implement performance benchmarking for eval (Issue #37) (`09a8bbed`)
- Add deterministic agent self-consistency check - Add agent_self_check.py for self checks - Run check in _check_pr_preconditions - Update COOKBOOK.md (`358a041d`)
- Kontext-Zusammenfassung als Gitea-Kommentar posten (`0e4f54b6`)
- Kontext-Loader — Kommentare in Keyword-Suche einbeziehen (`3239ae8c`)
- Issue-Kommentare in starter.md aufnehmen (## Diskussion) (`4390a8e6`)
- konfigurierbare Excludes + contexts/open/ Struktur (`018ee6f8`)
- zentrale Agent-Instanz — Pfade aus PROJECT_ROOT/agent/ lesen (closes #35) (`78ff23e1`)
- Kontext-Loader — Import-Analyse + Keyword-Suche für files.md (closes #33) (`c31e26d8`)
- Auto-Issues mit strukturiertem Kontext (closes #29) (`0a5aefec`)
- --pr prüft Server-Aktualität vor Eval + --force + --restart-before-eval (closes #30) (`5f152978`)
- _build_metadata() portiert (closes #74) (`3c180cde`)
- _build_metadata() portiert — Abschluss + Fixup Kommentare (closes #74) (`125a8aa7`)
- Szenario 2 Watch-Neustart-Trigger implementieren (closes #19) (`b5515e38`)
- _build_metadata() — aufklappbarer Block + Token-Schätzung (closes #16) (`ac811b24`)
- Abschluss-Kommentar — Eval-Ergebnis + Metadata + Session-Status (closes #13) (`4f17c02f`)
- Prioritätshinweis — technische Schranken vor Prompt-Regeln (refs #6) (`ee95c312`)
- Prozess-Enforcement — 5 Maßnahmen gegen Kontext-Drift (closes #6) (`9d8d0220`)
- STARTER_MD_PFLICHTREGELN in settings.py (Maßnahme 2+3, refs #6) (`43df45d7`)
- watch_interval_minutes aus agent_eval.json + README (closes #4) (`2d3944cf`)
- cmd_watch — generischer Hook für tools/log_analyzer.py im Watch-Loop (`7c8833cb`)
- score_history letzte 5 Einträge in PR- und Watch-Kommentare (`b3687240`)
- --score-history Anzeige-Befehl — Eval-Verlauf als Tabelle im Terminal (`a5237c4b`)
- --watch Modus + score_history.json + Auto-Issues bei Score-Verlust (`c1e0f782`)
- Baseline automatisch hochsetzen wenn Score besser wird (`47e8ace1`)
- steps-Mechanismus für mehrstufige Tests (refs #1) (`1ba15002`)
- evaluation.py — generisches Eval-System vor --pr (closes #1) (`4db5b01d`)
- --fixup, Docs-Check, Auto-Cleanup + Revert-Hinweis (`a2b56ddc`)
- Kontext-Dateien pro Issue (contexts/) (`6b1b3c10`)
- Textbausteine zentralisiert in settings.py (`b341e52a`)
- Auto-Scan + working-bot Auth + generische Konfiguration via .env (`b1ce8d0e`)
- auto-modus — Gitea-States selbständig erkennen und verarbeiten (`0a427f5d`)
- initial gitea-agent MVP — LLM-agnostischer Issue-Workflow (`09a897ce`)

### Bugfixes
- DASHBOARD_PATH in settings.py ergänzt + dashboard.py bereinigt (closes #86) (`54378fd6`)
- cmd_doctor — _api_get → gitea._request, agent_eval.json auch in agent/config/ suchen (`5b51fd21`)
- CONTEXT_DIR in .env.agent — contexts statt contexts/open (verschachtelte done/ korrigiert) (`e98cb258`)
- --build-skeleton ohne Größen-Limit (max_size_kb=10000) — agent_start.py vollständig analysiert (`2a8a5eba`)
- stufe in cmd_plan nicht definiert (NameError) (`3d04cb10`)
- evaluation.py — Commit-Hash in score_history.json (closes #55) (`e9a3a89c`)
- CONTEXT_DIR aus .env.agent lesen — --self Modus funktioniert (`4e90b93d`)
- gemini_start.sh korrekt implementiert — Kontext-Loader statt Wrapper (closes #61) (`fc577d8d`)
- _sync_closed_contexts() beim Watch-Start — verwaiste open/-Ordner nach done/ verschieben (`bc15ca67`)
- Labels bei Auto-Issue-Schließung entfernen (`a8662a53`)
- _close_resolved_auto_issues verschiebt Context nach done/ (`f8d13631`)
- Bot-Kommentar-Filter nach Username statt Keywords (`61d43b1c`)
- Kontext-Loader — Datei-Blacklist + Größen-Cap (50KB) (`b9d91bf7`)
- branch_name() — Sonderzeichen robust filtern (regex statt char-Liste) (`c68ce0f7`)
- Keyword-Suche nur in Code-Dateien (nicht .md) (`797023e1`)
- Kontext-Loader — Documentation/ excluden, AST depth=1 (`17c55edb`)
- _EXCLUDE_DIRS erweitert (Backup, vendor, contexts, .claude) (`d3db3881`)
- Path() fehlt in _resolve_path/_resolve_config — TypeError bei str project_root (`c90c0005`)
- import time fehlte — Watch-Modus crashed (NameError) (`03e05791`)
- eval_line enthält 'Score:' in allen Zweigen — COMMENT_REQUIRED_FIELDS Validierung (`e61127d1`)
- eval_line enthält Score auch bei Risikostufe-1-Skip (closes #11 Nebenfix) (`61cc1eba`)
- changed=[] Fallback vor try-Block — verhindert NameError in _neustart_required() (`73f12a5c`)
- WARN 3 — doppelte _build_metadata entfernt + Agent-Metadaten (`4bcfeaad`)
- doppelte _build_metadata() entfernt + System-Infos → Agent-Metadaten (WARN 3) (`bb0c2573`)
- baseline_score statt max_score + dynamischer Neustart-Hinweis (WARN 1+2) (`9346f4f0`)
- _save_score_history() bei baseline_created — erster Lauf in score_history (closes #25) (`6be6ca09`)
- COMMENT_REQUIRED_FIELDS[completion] — Eval + Score als Pflichtfeld (closes #24) (`211f7d50`)
- Risk-Gate in cmd_pr() — Risiko 1 überspringt Eval (closes #18) (`93414369`)
- Eval-Status und Session-Status auf separate Zeilen trennen (`2ba1e80a`)
- create_pr() gibt bestehenden PR zurück bei 409 (kein Crash bei Retry) (`61447884`)
- _format_history_block gibt Verlauf-Header zurück wenn keine History (closes #10 partial) (`dc84a582`)
- --eval-after-restart implementieren — COOKBOOK/Code Diskrepanz beheben (closes #10) (`57dee635`)
- Output-Validierung — _validate_comment() + COMMENT_REQUIRED_FIELDS (closes #8) (`a7900ba5`)
- Eval-Prüfung nur wenn agent_eval.json existiert (refs #6) (`501ac6bc`)
- sleep(2) zwischen Steps — Jetson LLM-Cooldown bei mehrstufigen Tests (`132f1a2d`)
- Eval-Fehler mit Gitea-Warnung + --summary Hinweis wenn leer (`17512cf5`)
- _ping HEAD→GET, eindeutige eval_user-ID, prompt statt message (refs #1) (`f674d0ee`)
- Label-Kette + Metadaten-Block + help-wanted Korrektur (`d39112da`)

### Refactoring
- evaluation.py von jetson-llm-chat entkoppeln (closes #89) (`75cbe428`)
- patch.py + changelog.py in plugins/ auslagern (closes #79) (`6d0c13f9`)
- Konsolenausgabe auf Statusmeldungen reduziert (`b2f49239`)
- settings.py + log.py + hardcoded Werte zentralisiert (`08748bd4`)

### Tests
- Unit-Tests für kritische reine Funktionen (closes #78) (`f5903d93`)

### Dokumentation
- Dashboard-Preview SVG + README Einbindung (`d10e6ab7`)
- Architektur-Erklärung — ein Agent, beliebig viele Projekte (`e38bb993`)
- Kapitel 1 — --setup als primärer Einstieg, manuell als Fallback (`4e4a78f6`)
- private Projektnamen anonymisiert, Legacy-Sektion entfernt (`b1894c08`)
- README + COOKBOOK — neue Features, private Daten anonymisiert (`c5b04954`)
- README aktualisiert — #77/#78/#79 in Roadmap, --generate-tests in Verwendung (`c693e4e0`)
- Gitea-Versionsvergleich (#59) in README + COOKBOOK Kapitel 18 (`5dfc9e21`)
- Codesegment-Strategie (#72) in README + COOKBOOK Kapitel 17 (`3a101c03`)
- SEARCH/REPLACE (#58) in README + COOKBOOK Kapitel 16 (`630c11da`)
- Diff-Validierung (#57) in README + COOKBOOK Kapitel 15 (`65a4e5fb`)
- AST-Skelett (#68) in README Roadmap + COOKBOOK Kapitel 14 (`74b63eed`)
- README Roadmap #65 + Gemini-Warnung; COOKBOOK Kapitel 12 Querverweise (`b0fb7b19`)
- Sitzungs-Protokoll 2026-03-23 — Änderungen + Gemini CLI Warnung (`7741c6bb`)
- README aktualisiert — Consecutive-Pass Gate, Betriebsmodi, Pflicht-Kette (`9a0c0019`)
- Feature-Übersicht in README erweitern (`d5e1e484`)
- Roadmap und Cookbook aktualisieren (`9a17348b`)
- README vollständig LLM-agnostisch formuliert (`e39474ab`)
- LLM-Anbindung (manuell vs. API) und Roadmap ausgebaut (`e821051c`)
- Feature-Übersicht mit Beschreibungen und Mini-Beispielen ausgebaut (`b518bfe8`)
- README — Intro und Feature-Übersicht ergänzt (`42809fb0`)
- konfigurierbare Excludes + contexts/open/ Struktur nachgezogen (`e37657f0`)
- agent_start.py.md + evaluation.py.md nachgezogen (Kontext-Loader Fixes, Pfade) (`1b43c7cc`)
- README + COOKBOOK — zentrale Agent-Instanz, Migrationsrezept (#35) (`5a62e026`)
- README + COOKBOOK — Kontext-Loader (Import-Analyse + grep) dokumentiert (#33) (`63635d87`)
- agent_start.py.md — Kontext-Loader dokumentiert (#33) (`fdbe29dc`)
- README + COOKBOOK — Auto-Issue strukturierter Body dokumentiert (#29) (`42997de8`)
- evaluation.py.md + agent_start.py.md — Issue #29 dokumentiert (closes #29) (`8454793e`)
- README + COOKBOOK — Server-Staleness-Check, --force, --restart-before-eval dokumentiert (`0d530d57`)
- README + COOKBOOK aktualisiert — Prozess-Enforcement, Watch-Szenario 2, log_analyzer (closes #11) (`dc7f98d9`)
- COOKBOOK.md — 10 Rezepte mit Skynet-Referenz (closes #5) (`3d2b8ebd`)
- README — Watch-Modus, score_history, Auto-Baseline, History-in-Comments (`087b96d8`)
- README — Risiko ≥ 2 Ausnahme + steps sleep(2) + Eval-Fehler-Verhalten (`496b6caa`)
- README — Eval-System Abschnitt ergänzt (evaluation.py, agent_eval.json, steps, baseline) (`1f2ccf64`)
- evaluation.py.md + README Eval-Abschnitt (refs #1) (`5f0e6dbc`)
- README komplett überarbeitet (`702e911b`)
- Problemstellung + Lösungsansätze für LLM-Trigger vollständig dokumentiert (`a4666356`)
- README Auto-Modus Diagramm + --root Fix, Docstrings vervollständigt (`e24fbb77`)

### Wartung
- Projektstruktur bereinigt — scripts/, data/, .gitignore (closes #92) (`d5c0c6de`)
- push_github.sh — Mirror-Script für GitHub (`115f83a1`)
- add gemini_start.sh for Gemini CLI workflow (closes #61) (`403f86af`)
- contexts/ und gitea-agent.log zu .gitignore hinzufügen (`5f115235`)

### Sonstiges
- Merge pull request '[04] Projekt-Profil: project.json + Feature-Flags für modularen Betrieb (closes #90)' (#98) from chore/issue-90-04-projekt-profil-project-json-fea into main (`7d9c0bef`)
- Merge pull request '[03] Refactor: evaluation.py von jetson-llm-chat entkoppeln (closes #89)' (#97) from chore/issue-89-03-refactor-evaluation-py-von-jets into main (`0e01452e`)
- Merge pull request '[02] Projektstruktur: saubere Trennung Code / Laufzeit / Config / Scripts (closes #92)' (#96) from chore/issue-92-02-projektstruktur-saubere-trennun into main (`132bb780`)
- Merge pull request 'fix: DASHBOARD_PATH korrekt in agent/data/ (closes #86)' (#93) from fix/issue-86-dashboard-path into main (`fef9fe92`)
- .env.agent aus Tracking entfernen + .gitignore ergänzt (`a2c7765c`)
- Merge pull request '--setup: Interaktiver Einrichtungs-Wizard für neue Projekte (closes #77)' (#84) from feat/issue-77-setup-wizard into main (`e884301b`)
- Merge pull request 'Inkrementelles Refactoring: patch.py + changelog.py auslagern (closes #79)' (#83) from refactor/issue-79-plugins-patch-changelog into main (`7f7edfb5`)
- Merge pull request 'Auto-Changelog-Generator (closes #39)' (#75) from feat/issue-39-auto-changelog-generator into main (`73799515`)
- Merge pull request '5 Gitea Versionsvergleich (closes #59)' (#74) from chore/issue-59-5-gitea-versionsvergleich into main (`a53138ad`)
- Merge pull request 'Flächendeckende Codesegment-Strategie — LLM lädt nur noch relevante Blöcke (closes #72)' (#73) from chore/issue-72-flaechendeckende-codesegment-strate into main (`9c94cf3e`)
- Merge pull request '4 SEARCH/REPLACE (closes #58)' (#71) from chore/issue-58-4-search-replace into main (`ce6c46a9`)
- Merge pull request '3 Diff-Validierung (closes #57)' (#70) from chore/issue-57-3-diff-validierung into main (`56126644`)
- Merge pull request 'Titel: AST-Repository-Skelett — repo_skeleton.json bei --issue generieren (closes #68)' (#69) from chore/issue-68-titel-ast-repository-skelett-repo-s into main (`f2ce6e36`)
- Merge pull request '2 AST-Skelett (closes #56)' (#67) from chore/issue-56-2-ast-skelett into main (`978b9c3d`)
- Merge pull request 'context_export.sh — LLM-agnostischer Kontext-Export + Dual-Repo-Unterstützung (closes #65)' (#66) from feat/issue-65-context-export-dual-repo into main (`6130c6fb`)
- Merge pull request 'gemini_start.sh — Gemini CLI in Agent-Workflow integrieren (closes #61)' (#63) from fix/gemini-start-correct into main (`12050f2c`)
- Merge pull request 'gemini_start.sh — Gemini CLI in Agent-Workflow integrieren (closes #61)' (#62) from chore/issue-61-gemini-start-sh-gemini-cli-in-agent into main (`d8d48224`)
- README.md aktualisiert (`7ea06675`)
- Merge pull request 'feat: Betriebsmodi — Night / Patch / Idle (closes #52)' (#53) from feat/issue-52-betriebsmodi into main (`73c2bde8`)
- Merge pull request 'feat: Auto-Issues erst nach N aufeinanderfolgenden Passes schließen (closes #50)' (#51) from feat/issue-50-consecutive-passes into main (`7691ef48`)
- README.md aktualisiert (`c6410430`)
- Merge pull request 'feat: zentrale Agent-Instanz — Pfade aus PROJECT_ROOT/agent/ lesen (closes #35)' (#36) from feat/issue-35-zentrale-agent-instanz into main (`23a3b943`)
- Merge pull request 'feat: Kontext-Loader — Import-Analyse + Keyword-Suche für files.md (closes #33)' (#34) from feat/issue-33-kontext-loader-import-analyse into main (`0cf01850`)
- Merge pull request 'feat: Auto-Issues mit strukturiertem Kontext (closes #29)' (#32) from feat/issue-29-auto-issue-strukturierter-kontext into main (`a73ecad5`)
- Merge pull request 'pr soll vor Eval prüfen ob Server-Code aktuell ist (closes #30)' (#31) from chore/issue-30-pr-soll-vor-eval-pr-fen-ob-server-c into main (`97f34766`)
- Merge pull request 'Docs: README + COOKBOOK aktualisieren — #6/#8 Details + log_analyzer Rezept (closes #11)' (#28) from docs/issue-11-docs-readme-+-cookbook-aktualisiere into main (`f8a4c2d7`)
- Merge pull request 'Audit-Fixes — 2 FAIL + 3 WARN aus Re-Audit #23 (closes #26)' (#27) from fix/issue-26-audit-fixes into main (`9d5b9c1c`)
- Merge branch 'main' into fix/issue-26-audit-fixes (`e5499fa1`)
- Merge pull request '[Audit FAIL] Szenario 2 — Watch-Neustart-Trigger nicht implementiert (closes #19)' (#21) from fix/issue-19-szenario2-watch-neustart into main (`bb446ff3`)
- Merge branch 'main' into fix/issue-19-szenario2-watch-neustart (`d4151a5f`)
- Merge pull request '[Audit FAIL] Risiko 1 — Eval wird nicht übersprungen (kein Risk-Gate) (closes #18)' (#20) from fix/issue-18-risk-gate-eval-skip into main (`f88a163f`)
- Merge branch 'main' into fix/issue-18-risk-gate-eval-skip (`5810f47e`)
- Merge pull request '_build_metadata() — Token-Schätzung + aufklappbarer Block (closes #16)' (#17) from chore/issue-16-_build_metadata-—-token-sch-tzung-+ into main (`f577cf45`)
- Merge branch 'main' into chore/issue-16-_build_metadata-—-token-sch-tzung-+ (`3b084f70`)
- Merge pull request 'Abschluss-Kommentar — Eval-Ergebnis + Metadata + Session-Status (closes #13)' (#15) from chore/issue-13-abschluss-kommentar-—-eval-ergebnis into main (`13c023f7`)
- Merge branch 'main' into chore/issue-13-abschluss-kommentar-—-eval-ergebnis (`a914faf6`)
- Merge pull request 'Fix: --eval-after-restart Diskrepanz — COOKBOOK vs agent_start.py (closes #10)' (#12) from chore/issue-10-fix-eval-after-restart-diskrepanz-— into main (`3c6a98a5`)
- Merge pull request 'fix: Output-Validierung — _validate_comment() + COMMENT_REQUIRED_FIELDS (closes #8)' (#9) from fix/issue-8-output-validierung-kommentare into main (`3813836e`)
- Merge pull request 'Prozess-Enforcement — Kontext-Drift verhindern (closes #6)' (#7) from chore/issue-6-prozess-enforcement-—-kontext-drift into main (`3adae3a6`)
- Merge pull request 'feat: evaluation.py — generisches Eval-System vor --pr (closes #1)' (#2) from feat/issue-1-evaluation-system into main (`312913a8`)

## [Unreleased] — 2026-03-26

### Features
- Health-Check Engine — plugins/health.py (`e2496ab1`)
- Projekt-Profil + Feature-Flags (project.json) (closes #90) (`9caa5730`)
- --setup Wizard + --doctor Health-Check (closes #77) (`2f9eaa4d`)
- Auto-Changelog-Generator — --changelog [VERSION] (closes #39) (`84b96aba`)
- Gitea-Versionsvergleich — get_file_contents, _ast_diff, _gitea_version_compare (closes #59) (`2b9afc12`)
- context_export.sh — Slice-Schleife (plain) + Slice-Anweisung (file/gemini) (`db7365a7`)
- --get-slice loggt session.json + _warn_slices_not_requested in preconditions (`d005200f`)
- files.md Skelett-Modus — kein Volltext mehr (`a831c4ed`)
- --build-skeleton + _update_skeleton_incremental + _load_skeleton_map (`004afb86`)
- SEARCH/REPLACE Protokoll — Parser, _apply_patch, cmd_apply_patch, --apply-patch/--dry-run (closes #58) (`276417f7`)
- Diff-Validierung — Scope-Warnung bei Änderungen außerhalb AST-Skelett (closes #57) (`5747d3db`)
- AST-Skelett — ClassDef/FunctionDef-Extraktion, repo_skeleton.md, --get-slice (closes #68) (`ef9c6bf8`)
- use repo_skeleton.json for files.md truncation (`b4917d1e`)
- generate repo_skeleton.json in cmd_plan (`b663b3d3`)
- introduce find_relevant_files_advanced (`687dc46f`)
- context_export.sh + Dual-Repo --self Flag + COOKBOOK.md LLM-Workflow (closes #65) (`01bc9b5a`)
- Betriebsmodi — start_patch.sh, start_night.sh, stop_agent.sh, agent_status.sh (closes #52) (`b2c0500c`)
- Auto-Issues erst nach N aufeinanderfolgenden Passes schließen (closes #50) (`69969c26`)
- add agent_eval.template.json and tag check (`87e643a4`)
- implement patch mode and live dashboard (Issue #46) (`69eafcd3`)
- implement systematic tag failure detection in watch mode (Issue #47) (`fadeb902`)
- implement test generation via LLM prompt (Issue #44) (`74cc47e6`)
- implement performance benchmarking for eval (Issue #37) (`09a8bbed`)
- Add deterministic agent self-consistency check - Add agent_self_check.py for self checks - Run check in _check_pr_preconditions - Update COOKBOOK.md (`358a041d`)
- Kontext-Zusammenfassung als Gitea-Kommentar posten (`0e4f54b6`)
- Kontext-Loader — Kommentare in Keyword-Suche einbeziehen (`3239ae8c`)
- Issue-Kommentare in starter.md aufnehmen (## Diskussion) (`4390a8e6`)
- konfigurierbare Excludes + contexts/open/ Struktur (`018ee6f8`)
- zentrale Agent-Instanz — Pfade aus PROJECT_ROOT/agent/ lesen (closes #35) (`78ff23e1`)
- Kontext-Loader — Import-Analyse + Keyword-Suche für files.md (closes #33) (`c31e26d8`)
- Auto-Issues mit strukturiertem Kontext (closes #29) (`0a5aefec`)
- --pr prüft Server-Aktualität vor Eval + --force + --restart-before-eval (closes #30) (`5f152978`)
- _build_metadata() portiert (closes #74) (`3c180cde`)
- _build_metadata() portiert — Abschluss + Fixup Kommentare (closes #74) (`125a8aa7`)
- Szenario 2 Watch-Neustart-Trigger implementieren (closes #19) (`b5515e38`)
- _build_metadata() — aufklappbarer Block + Token-Schätzung (closes #16) (`ac811b24`)
- Abschluss-Kommentar — Eval-Ergebnis + Metadata + Session-Status (closes #13) (`4f17c02f`)
- Prioritätshinweis — technische Schranken vor Prompt-Regeln (refs #6) (`ee95c312`)
- Prozess-Enforcement — 5 Maßnahmen gegen Kontext-Drift (closes #6) (`9d8d0220`)
- STARTER_MD_PFLICHTREGELN in settings.py (Maßnahme 2+3, refs #6) (`43df45d7`)
- watch_interval_minutes aus agent_eval.json + README (closes #4) (`2d3944cf`)
- cmd_watch — generischer Hook für tools/log_analyzer.py im Watch-Loop (`7c8833cb`)
- score_history letzte 5 Einträge in PR- und Watch-Kommentare (`b3687240`)
- --score-history Anzeige-Befehl — Eval-Verlauf als Tabelle im Terminal (`a5237c4b`)
- --watch Modus + score_history.json + Auto-Issues bei Score-Verlust (`c1e0f782`)
- Baseline automatisch hochsetzen wenn Score besser wird (`47e8ace1`)
- steps-Mechanismus für mehrstufige Tests (refs #1) (`1ba15002`)
- evaluation.py — generisches Eval-System vor --pr (closes #1) (`4db5b01d`)
- --fixup, Docs-Check, Auto-Cleanup + Revert-Hinweis (`a2b56ddc`)
- Kontext-Dateien pro Issue (contexts/) (`6b1b3c10`)
- Textbausteine zentralisiert in settings.py (`b341e52a`)
- Auto-Scan + working-bot Auth + generische Konfiguration via .env (`b1ce8d0e`)
- auto-modus — Gitea-States selbständig erkennen und verarbeiten (`0a427f5d`)
- initial gitea-agent MVP — LLM-agnostischer Issue-Workflow (`09a897ce`)

### Bugfixes
- DASHBOARD_PATH in settings.py ergänzt + dashboard.py bereinigt (closes #86) (`54378fd6`)
- cmd_doctor — _api_get → gitea._request, agent_eval.json auch in agent/config/ suchen (`5b51fd21`)
- CONTEXT_DIR in .env.agent — contexts statt contexts/open (verschachtelte done/ korrigiert) (`e98cb258`)
- --build-skeleton ohne Größen-Limit (max_size_kb=10000) — agent_start.py vollständig analysiert (`2a8a5eba`)
- stufe in cmd_plan nicht definiert (NameError) (`3d04cb10`)
- evaluation.py — Commit-Hash in score_history.json (closes #55) (`e9a3a89c`)
- CONTEXT_DIR aus .env.agent lesen — --self Modus funktioniert (`4e90b93d`)
- gemini_start.sh korrekt implementiert — Kontext-Loader statt Wrapper (closes #61) (`fc577d8d`)
- _sync_closed_contexts() beim Watch-Start — verwaiste open/-Ordner nach done/ verschieben (`bc15ca67`)
- Labels bei Auto-Issue-Schließung entfernen (`a8662a53`)
- _close_resolved_auto_issues verschiebt Context nach done/ (`f8d13631`)
- Bot-Kommentar-Filter nach Username statt Keywords (`61d43b1c`)
- Kontext-Loader — Datei-Blacklist + Größen-Cap (50KB) (`b9d91bf7`)
- branch_name() — Sonderzeichen robust filtern (regex statt char-Liste) (`c68ce0f7`)
- Keyword-Suche nur in Code-Dateien (nicht .md) (`797023e1`)
- Kontext-Loader — Documentation/ excluden, AST depth=1 (`17c55edb`)
- _EXCLUDE_DIRS erweitert (Backup, vendor, contexts, .claude) (`d3db3881`)
- Path() fehlt in _resolve_path/_resolve_config — TypeError bei str project_root (`c90c0005`)
- import time fehlte — Watch-Modus crashed (NameError) (`03e05791`)
- eval_line enthält 'Score:' in allen Zweigen — COMMENT_REQUIRED_FIELDS Validierung (`e61127d1`)
- eval_line enthält Score auch bei Risikostufe-1-Skip (closes #11 Nebenfix) (`61cc1eba`)
- changed=[] Fallback vor try-Block — verhindert NameError in _neustart_required() (`73f12a5c`)
- WARN 3 — doppelte _build_metadata entfernt + Agent-Metadaten (`4bcfeaad`)
- doppelte _build_metadata() entfernt + System-Infos → Agent-Metadaten (WARN 3) (`bb0c2573`)
- baseline_score statt max_score + dynamischer Neustart-Hinweis (WARN 1+2) (`9346f4f0`)
- _save_score_history() bei baseline_created — erster Lauf in score_history (closes #25) (`6be6ca09`)
- COMMENT_REQUIRED_FIELDS[completion] — Eval + Score als Pflichtfeld (closes #24) (`211f7d50`)
- Risk-Gate in cmd_pr() — Risiko 1 überspringt Eval (closes #18) (`93414369`)
- Eval-Status und Session-Status auf separate Zeilen trennen (`2ba1e80a`)
- create_pr() gibt bestehenden PR zurück bei 409 (kein Crash bei Retry) (`61447884`)
- _format_history_block gibt Verlauf-Header zurück wenn keine History (closes #10 partial) (`dc84a582`)
- --eval-after-restart implementieren — COOKBOOK/Code Diskrepanz beheben (closes #10) (`57dee635`)
- Output-Validierung — _validate_comment() + COMMENT_REQUIRED_FIELDS (closes #8) (`a7900ba5`)
- Eval-Prüfung nur wenn agent_eval.json existiert (refs #6) (`501ac6bc`)
- sleep(2) zwischen Steps — Jetson LLM-Cooldown bei mehrstufigen Tests (`132f1a2d`)
- Eval-Fehler mit Gitea-Warnung + --summary Hinweis wenn leer (`17512cf5`)
- _ping HEAD→GET, eindeutige eval_user-ID, prompt statt message (refs #1) (`f674d0ee`)
- Label-Kette + Metadaten-Block + help-wanted Korrektur (`d39112da`)

### Refactoring
- evaluation.py von jetson-llm-chat entkoppeln (closes #89) (`75cbe428`)
- patch.py + changelog.py in plugins/ auslagern (closes #79) (`6d0c13f9`)
- Konsolenausgabe auf Statusmeldungen reduziert (`b2f49239`)
- settings.py + log.py + hardcoded Werte zentralisiert (`08748bd4`)

### Tests
- Unit-Tests für kritische reine Funktionen (closes #78) (`f5903d93`)

### Dokumentation
- Dashboard-Preview SVG + README Einbindung (`d10e6ab7`)
- Architektur-Erklärung — ein Agent, beliebig viele Projekte (`e38bb993`)
- Kapitel 1 — --setup als primärer Einstieg, manuell als Fallback (`4e4a78f6`)
- private Projektnamen anonymisiert, Legacy-Sektion entfernt (`b1894c08`)
- README + COOKBOOK — neue Features, private Daten anonymisiert (`c5b04954`)
- README aktualisiert — #77/#78/#79 in Roadmap, --generate-tests in Verwendung (`c693e4e0`)
- Gitea-Versionsvergleich (#59) in README + COOKBOOK Kapitel 18 (`5dfc9e21`)
- Codesegment-Strategie (#72) in README + COOKBOOK Kapitel 17 (`3a101c03`)
- SEARCH/REPLACE (#58) in README + COOKBOOK Kapitel 16 (`630c11da`)
- Diff-Validierung (#57) in README + COOKBOOK Kapitel 15 (`65a4e5fb`)
- AST-Skelett (#68) in README Roadmap + COOKBOOK Kapitel 14 (`74b63eed`)
- README Roadmap #65 + Gemini-Warnung; COOKBOOK Kapitel 12 Querverweise (`b0fb7b19`)
- Sitzungs-Protokoll 2026-03-23 — Änderungen + Gemini CLI Warnung (`7741c6bb`)
- README aktualisiert — Consecutive-Pass Gate, Betriebsmodi, Pflicht-Kette (`9a0c0019`)
- Feature-Übersicht in README erweitern (`d5e1e484`)
- Roadmap und Cookbook aktualisieren (`9a17348b`)
- README vollständig LLM-agnostisch formuliert (`e39474ab`)
- LLM-Anbindung (manuell vs. API) und Roadmap ausgebaut (`e821051c`)
- Feature-Übersicht mit Beschreibungen und Mini-Beispielen ausgebaut (`b518bfe8`)
- README — Intro und Feature-Übersicht ergänzt (`42809fb0`)
- konfigurierbare Excludes + contexts/open/ Struktur nachgezogen (`e37657f0`)
- agent_start.py.md + evaluation.py.md nachgezogen (Kontext-Loader Fixes, Pfade) (`1b43c7cc`)
- README + COOKBOOK — zentrale Agent-Instanz, Migrationsrezept (#35) (`5a62e026`)
- README + COOKBOOK — Kontext-Loader (Import-Analyse + grep) dokumentiert (#33) (`63635d87`)
- agent_start.py.md — Kontext-Loader dokumentiert (#33) (`fdbe29dc`)
- README + COOKBOOK — Auto-Issue strukturierter Body dokumentiert (#29) (`42997de8`)
- evaluation.py.md + agent_start.py.md — Issue #29 dokumentiert (closes #29) (`8454793e`)
- README + COOKBOOK — Server-Staleness-Check, --force, --restart-before-eval dokumentiert (`0d530d57`)
- README + COOKBOOK aktualisiert — Prozess-Enforcement, Watch-Szenario 2, log_analyzer (closes #11) (`dc7f98d9`)
- COOKBOOK.md — 10 Rezepte mit Skynet-Referenz (closes #5) (`3d2b8ebd`)
- README — Watch-Modus, score_history, Auto-Baseline, History-in-Comments (`087b96d8`)
- README — Risiko ≥ 2 Ausnahme + steps sleep(2) + Eval-Fehler-Verhalten (`496b6caa`)
- README — Eval-System Abschnitt ergänzt (evaluation.py, agent_eval.json, steps, baseline) (`1f2ccf64`)
- evaluation.py.md + README Eval-Abschnitt (refs #1) (`5f0e6dbc`)
- README komplett überarbeitet (`702e911b`)
- Problemstellung + Lösungsansätze für LLM-Trigger vollständig dokumentiert (`a4666356`)
- README Auto-Modus Diagramm + --root Fix, Docstrings vervollständigt (`e24fbb77`)

### Wartung
- Projektstruktur bereinigt — scripts/, data/, .gitignore (closes #92) (`d5c0c6de`)
- push_github.sh — Mirror-Script für GitHub (`115f83a1`)
- add gemini_start.sh for Gemini CLI workflow (closes #61) (`403f86af`)
- contexts/ und gitea-agent.log zu .gitignore hinzufügen (`5f115235`)

### Sonstiges
- Merge pull request '[04] Projekt-Profil: project.json + Feature-Flags für modularen Betrieb (closes #90)' (#98) from chore/issue-90-04-projekt-profil-project-json-fea into main (`7d9c0bef`)
- Merge pull request '[03] Refactor: evaluation.py von jetson-llm-chat entkoppeln (closes #89)' (#97) from chore/issue-89-03-refactor-evaluation-py-von-jets into main (`0e01452e`)
- Merge pull request '[02] Projektstruktur: saubere Trennung Code / Laufzeit / Config / Scripts (closes #92)' (#96) from chore/issue-92-02-projektstruktur-saubere-trennun into main (`132bb780`)
- Merge pull request 'fix: DASHBOARD_PATH korrekt in agent/data/ (closes #86)' (#93) from fix/issue-86-dashboard-path into main (`fef9fe92`)
- .env.agent aus Tracking entfernen + .gitignore ergänzt (`a2c7765c`)
- Merge pull request '--setup: Interaktiver Einrichtungs-Wizard für neue Projekte (closes #77)' (#84) from feat/issue-77-setup-wizard into main (`e884301b`)
- Merge pull request 'Inkrementelles Refactoring: patch.py + changelog.py auslagern (closes #79)' (#83) from refactor/issue-79-plugins-patch-changelog into main (`7f7edfb5`)
- Merge pull request 'Auto-Changelog-Generator (closes #39)' (#75) from feat/issue-39-auto-changelog-generator into main (`73799515`)
- Merge pull request '5 Gitea Versionsvergleich (closes #59)' (#74) from chore/issue-59-5-gitea-versionsvergleich into main (`a53138ad`)
- Merge pull request 'Flächendeckende Codesegment-Strategie — LLM lädt nur noch relevante Blöcke (closes #72)' (#73) from chore/issue-72-flaechendeckende-codesegment-strate into main (`9c94cf3e`)
- Merge pull request '4 SEARCH/REPLACE (closes #58)' (#71) from chore/issue-58-4-search-replace into main (`ce6c46a9`)
- Merge pull request '3 Diff-Validierung (closes #57)' (#70) from chore/issue-57-3-diff-validierung into main (`56126644`)
- Merge pull request 'Titel: AST-Repository-Skelett — repo_skeleton.json bei --issue generieren (closes #68)' (#69) from chore/issue-68-titel-ast-repository-skelett-repo-s into main (`f2ce6e36`)
- Merge pull request '2 AST-Skelett (closes #56)' (#67) from chore/issue-56-2-ast-skelett into main (`978b9c3d`)
- Merge pull request 'context_export.sh — LLM-agnostischer Kontext-Export + Dual-Repo-Unterstützung (closes #65)' (#66) from feat/issue-65-context-export-dual-repo into main (`6130c6fb`)
- Merge pull request 'gemini_start.sh — Gemini CLI in Agent-Workflow integrieren (closes #61)' (#63) from fix/gemini-start-correct into main (`12050f2c`)
- Merge pull request 'gemini_start.sh — Gemini CLI in Agent-Workflow integrieren (closes #61)' (#62) from chore/issue-61-gemini-start-sh-gemini-cli-in-agent into main (`d8d48224`)
- README.md aktualisiert (`7ea06675`)
- Merge pull request 'feat: Betriebsmodi — Night / Patch / Idle (closes #52)' (#53) from feat/issue-52-betriebsmodi into main (`73c2bde8`)
- Merge pull request 'feat: Auto-Issues erst nach N aufeinanderfolgenden Passes schließen (closes #50)' (#51) from feat/issue-50-consecutive-passes into main (`7691ef48`)
- README.md aktualisiert (`c6410430`)
- Merge pull request 'feat: zentrale Agent-Instanz — Pfade aus PROJECT_ROOT/agent/ lesen (closes #35)' (#36) from feat/issue-35-zentrale-agent-instanz into main (`23a3b943`)
- Merge pull request 'feat: Kontext-Loader — Import-Analyse + Keyword-Suche für files.md (closes #33)' (#34) from feat/issue-33-kontext-loader-import-analyse into main (`0cf01850`)
- Merge pull request 'feat: Auto-Issues mit strukturiertem Kontext (closes #29)' (#32) from feat/issue-29-auto-issue-strukturierter-kontext into main (`a73ecad5`)
- Merge pull request 'pr soll vor Eval prüfen ob Server-Code aktuell ist (closes #30)' (#31) from chore/issue-30-pr-soll-vor-eval-pr-fen-ob-server-c into main (`97f34766`)
- Merge pull request 'Docs: README + COOKBOOK aktualisieren — #6/#8 Details + log_analyzer Rezept (closes #11)' (#28) from docs/issue-11-docs-readme-+-cookbook-aktualisiere into main (`f8a4c2d7`)
- Merge pull request 'Audit-Fixes — 2 FAIL + 3 WARN aus Re-Audit #23 (closes #26)' (#27) from fix/issue-26-audit-fixes into main (`9d5b9c1c`)
- Merge branch 'main' into fix/issue-26-audit-fixes (`e5499fa1`)
- Merge pull request '[Audit FAIL] Szenario 2 — Watch-Neustart-Trigger nicht implementiert (closes #19)' (#21) from fix/issue-19-szenario2-watch-neustart into main (`bb446ff3`)
- Merge branch 'main' into fix/issue-19-szenario2-watch-neustart (`d4151a5f`)
- Merge pull request '[Audit FAIL] Risiko 1 — Eval wird nicht übersprungen (kein Risk-Gate) (closes #18)' (#20) from fix/issue-18-risk-gate-eval-skip into main (`f88a163f`)
- Merge branch 'main' into fix/issue-18-risk-gate-eval-skip (`5810f47e`)
- Merge pull request '_build_metadata() — Token-Schätzung + aufklappbarer Block (closes #16)' (#17) from chore/issue-16-_build_metadata-—-token-sch-tzung-+ into main (`f577cf45`)
- Merge branch 'main' into chore/issue-16-_build_metadata-—-token-sch-tzung-+ (`3b084f70`)
- Merge pull request 'Abschluss-Kommentar — Eval-Ergebnis + Metadata + Session-Status (closes #13)' (#15) from chore/issue-13-abschluss-kommentar-—-eval-ergebnis into main (`13c023f7`)
- Merge branch 'main' into chore/issue-13-abschluss-kommentar-—-eval-ergebnis (`a914faf6`)
- Merge pull request 'Fix: --eval-after-restart Diskrepanz — COOKBOOK vs agent_start.py (closes #10)' (#12) from chore/issue-10-fix-eval-after-restart-diskrepanz-— into main (`3c6a98a5`)
- Merge pull request 'fix: Output-Validierung — _validate_comment() + COMMENT_REQUIRED_FIELDS (closes #8)' (#9) from fix/issue-8-output-validierung-kommentare into main (`3813836e`)
- Merge pull request 'Prozess-Enforcement — Kontext-Drift verhindern (closes #6)' (#7) from chore/issue-6-prozess-enforcement-—-kontext-drift into main (`3adae3a6`)
- Merge pull request 'feat: evaluation.py — generisches Eval-System vor --pr (closes #1)' (#2) from feat/issue-1-evaluation-system into main (`312913a8`)

## [Unreleased] — 2026-03-26

### Features
- Projekt-Profil + Feature-Flags (project.json) (closes #90) (`9caa5730`)
- --setup Wizard + --doctor Health-Check (closes #77) (`2f9eaa4d`)
- Auto-Changelog-Generator — --changelog [VERSION] (closes #39) (`84b96aba`)
- Gitea-Versionsvergleich — get_file_contents, _ast_diff, _gitea_version_compare (closes #59) (`2b9afc12`)
- context_export.sh — Slice-Schleife (plain) + Slice-Anweisung (file/gemini) (`db7365a7`)
- --get-slice loggt session.json + _warn_slices_not_requested in preconditions (`d005200f`)
- files.md Skelett-Modus — kein Volltext mehr (`a831c4ed`)
- --build-skeleton + _update_skeleton_incremental + _load_skeleton_map (`004afb86`)
- SEARCH/REPLACE Protokoll — Parser, _apply_patch, cmd_apply_patch, --apply-patch/--dry-run (closes #58) (`276417f7`)
- Diff-Validierung — Scope-Warnung bei Änderungen außerhalb AST-Skelett (closes #57) (`5747d3db`)
- AST-Skelett — ClassDef/FunctionDef-Extraktion, repo_skeleton.md, --get-slice (closes #68) (`ef9c6bf8`)
- use repo_skeleton.json for files.md truncation (`b4917d1e`)
- generate repo_skeleton.json in cmd_plan (`b663b3d3`)
- introduce find_relevant_files_advanced (`687dc46f`)
- context_export.sh + Dual-Repo --self Flag + COOKBOOK.md LLM-Workflow (closes #65) (`01bc9b5a`)
- Betriebsmodi — start_patch.sh, start_night.sh, stop_agent.sh, agent_status.sh (closes #52) (`b2c0500c`)
- Auto-Issues erst nach N aufeinanderfolgenden Passes schließen (closes #50) (`69969c26`)
- add agent_eval.template.json and tag check (`87e643a4`)
- implement patch mode and live dashboard (Issue #46) (`69eafcd3`)
- implement systematic tag failure detection in watch mode (Issue #47) (`fadeb902`)
- implement test generation via LLM prompt (Issue #44) (`74cc47e6`)
- implement performance benchmarking for eval (Issue #37) (`09a8bbed`)
- Add deterministic agent self-consistency check - Add agent_self_check.py for self checks - Run check in _check_pr_preconditions - Update COOKBOOK.md (`358a041d`)
- Kontext-Zusammenfassung als Gitea-Kommentar posten (`0e4f54b6`)
- Kontext-Loader — Kommentare in Keyword-Suche einbeziehen (`3239ae8c`)
- Issue-Kommentare in starter.md aufnehmen (## Diskussion) (`4390a8e6`)
- konfigurierbare Excludes + contexts/open/ Struktur (`018ee6f8`)
- zentrale Agent-Instanz — Pfade aus PROJECT_ROOT/agent/ lesen (closes #35) (`78ff23e1`)
- Kontext-Loader — Import-Analyse + Keyword-Suche für files.md (closes #33) (`c31e26d8`)
- Auto-Issues mit strukturiertem Kontext (closes #29) (`0a5aefec`)
- --pr prüft Server-Aktualität vor Eval + --force + --restart-before-eval (closes #30) (`5f152978`)
- _build_metadata() portiert (closes #74) (`3c180cde`)
- _build_metadata() portiert — Abschluss + Fixup Kommentare (closes #74) (`125a8aa7`)
- Szenario 2 Watch-Neustart-Trigger implementieren (closes #19) (`b5515e38`)
- _build_metadata() — aufklappbarer Block + Token-Schätzung (closes #16) (`ac811b24`)
- Abschluss-Kommentar — Eval-Ergebnis + Metadata + Session-Status (closes #13) (`4f17c02f`)
- Prioritätshinweis — technische Schranken vor Prompt-Regeln (refs #6) (`ee95c312`)
- Prozess-Enforcement — 5 Maßnahmen gegen Kontext-Drift (closes #6) (`9d8d0220`)
- STARTER_MD_PFLICHTREGELN in settings.py (Maßnahme 2+3, refs #6) (`43df45d7`)
- watch_interval_minutes aus agent_eval.json + README (closes #4) (`2d3944cf`)
- cmd_watch — generischer Hook für tools/log_analyzer.py im Watch-Loop (`7c8833cb`)
- score_history letzte 5 Einträge in PR- und Watch-Kommentare (`b3687240`)
- --score-history Anzeige-Befehl — Eval-Verlauf als Tabelle im Terminal (`a5237c4b`)
- --watch Modus + score_history.json + Auto-Issues bei Score-Verlust (`c1e0f782`)
- Baseline automatisch hochsetzen wenn Score besser wird (`47e8ace1`)
- steps-Mechanismus für mehrstufige Tests (refs #1) (`1ba15002`)
- evaluation.py — generisches Eval-System vor --pr (closes #1) (`4db5b01d`)
- --fixup, Docs-Check, Auto-Cleanup + Revert-Hinweis (`a2b56ddc`)
- Kontext-Dateien pro Issue (contexts/) (`6b1b3c10`)
- Textbausteine zentralisiert in settings.py (`b341e52a`)
- Auto-Scan + working-bot Auth + generische Konfiguration via .env (`b1ce8d0e`)
- auto-modus — Gitea-States selbständig erkennen und verarbeiten (`0a427f5d`)
- initial gitea-agent MVP — LLM-agnostischer Issue-Workflow (`09a897ce`)

### Bugfixes
- DASHBOARD_PATH in settings.py ergänzt + dashboard.py bereinigt (closes #86) (`54378fd6`)
- cmd_doctor — _api_get → gitea._request, agent_eval.json auch in agent/config/ suchen (`5b51fd21`)
- CONTEXT_DIR in .env.agent — contexts statt contexts/open (verschachtelte done/ korrigiert) (`e98cb258`)
- --build-skeleton ohne Größen-Limit (max_size_kb=10000) — agent_start.py vollständig analysiert (`2a8a5eba`)
- stufe in cmd_plan nicht definiert (NameError) (`3d04cb10`)
- evaluation.py — Commit-Hash in score_history.json (closes #55) (`e9a3a89c`)
- CONTEXT_DIR aus .env.agent lesen — --self Modus funktioniert (`4e90b93d`)
- gemini_start.sh korrekt implementiert — Kontext-Loader statt Wrapper (closes #61) (`fc577d8d`)
- _sync_closed_contexts() beim Watch-Start — verwaiste open/-Ordner nach done/ verschieben (`bc15ca67`)
- Labels bei Auto-Issue-Schließung entfernen (`a8662a53`)
- _close_resolved_auto_issues verschiebt Context nach done/ (`f8d13631`)
- Bot-Kommentar-Filter nach Username statt Keywords (`61d43b1c`)
- Kontext-Loader — Datei-Blacklist + Größen-Cap (50KB) (`b9d91bf7`)
- branch_name() — Sonderzeichen robust filtern (regex statt char-Liste) (`c68ce0f7`)
- Keyword-Suche nur in Code-Dateien (nicht .md) (`797023e1`)
- Kontext-Loader — Documentation/ excluden, AST depth=1 (`17c55edb`)
- _EXCLUDE_DIRS erweitert (Backup, vendor, contexts, .claude) (`d3db3881`)
- Path() fehlt in _resolve_path/_resolve_config — TypeError bei str project_root (`c90c0005`)
- import time fehlte — Watch-Modus crashed (NameError) (`03e05791`)
- eval_line enthält 'Score:' in allen Zweigen — COMMENT_REQUIRED_FIELDS Validierung (`e61127d1`)
- eval_line enthält Score auch bei Risikostufe-1-Skip (closes #11 Nebenfix) (`61cc1eba`)
- changed=[] Fallback vor try-Block — verhindert NameError in _neustart_required() (`73f12a5c`)
- WARN 3 — doppelte _build_metadata entfernt + Agent-Metadaten (`4bcfeaad`)
- doppelte _build_metadata() entfernt + System-Infos → Agent-Metadaten (WARN 3) (`bb0c2573`)
- baseline_score statt max_score + dynamischer Neustart-Hinweis (WARN 1+2) (`9346f4f0`)
- _save_score_history() bei baseline_created — erster Lauf in score_history (closes #25) (`6be6ca09`)
- COMMENT_REQUIRED_FIELDS[completion] — Eval + Score als Pflichtfeld (closes #24) (`211f7d50`)
- Risk-Gate in cmd_pr() — Risiko 1 überspringt Eval (closes #18) (`93414369`)
- Eval-Status und Session-Status auf separate Zeilen trennen (`2ba1e80a`)
- create_pr() gibt bestehenden PR zurück bei 409 (kein Crash bei Retry) (`61447884`)
- _format_history_block gibt Verlauf-Header zurück wenn keine History (closes #10 partial) (`dc84a582`)
- --eval-after-restart implementieren — COOKBOOK/Code Diskrepanz beheben (closes #10) (`57dee635`)
- Output-Validierung — _validate_comment() + COMMENT_REQUIRED_FIELDS (closes #8) (`a7900ba5`)
- Eval-Prüfung nur wenn agent_eval.json existiert (refs #6) (`501ac6bc`)
- sleep(2) zwischen Steps — Jetson LLM-Cooldown bei mehrstufigen Tests (`132f1a2d`)
- Eval-Fehler mit Gitea-Warnung + --summary Hinweis wenn leer (`17512cf5`)
- _ping HEAD→GET, eindeutige eval_user-ID, prompt statt message (refs #1) (`f674d0ee`)
- Label-Kette + Metadaten-Block + help-wanted Korrektur (`d39112da`)

### Refactoring
- evaluation.py von jetson-llm-chat entkoppeln (closes #89) (`75cbe428`)
- patch.py + changelog.py in plugins/ auslagern (closes #79) (`6d0c13f9`)
- Konsolenausgabe auf Statusmeldungen reduziert (`b2f49239`)
- settings.py + log.py + hardcoded Werte zentralisiert (`08748bd4`)

### Tests
- Unit-Tests für kritische reine Funktionen (closes #78) (`f5903d93`)

### Dokumentation
- Dashboard-Preview SVG + README Einbindung (`d10e6ab7`)
- Architektur-Erklärung — ein Agent, beliebig viele Projekte (`e38bb993`)
- Kapitel 1 — --setup als primärer Einstieg, manuell als Fallback (`4e4a78f6`)
- private Projektnamen anonymisiert, Legacy-Sektion entfernt (`b1894c08`)
- README + COOKBOOK — neue Features, private Daten anonymisiert (`c5b04954`)
- README aktualisiert — #77/#78/#79 in Roadmap, --generate-tests in Verwendung (`c693e4e0`)
- Gitea-Versionsvergleich (#59) in README + COOKBOOK Kapitel 18 (`5dfc9e21`)
- Codesegment-Strategie (#72) in README + COOKBOOK Kapitel 17 (`3a101c03`)
- SEARCH/REPLACE (#58) in README + COOKBOOK Kapitel 16 (`630c11da`)
- Diff-Validierung (#57) in README + COOKBOOK Kapitel 15 (`65a4e5fb`)
- AST-Skelett (#68) in README Roadmap + COOKBOOK Kapitel 14 (`74b63eed`)
- README Roadmap #65 + Gemini-Warnung; COOKBOOK Kapitel 12 Querverweise (`b0fb7b19`)
- Sitzungs-Protokoll 2026-03-23 — Änderungen + Gemini CLI Warnung (`7741c6bb`)
- README aktualisiert — Consecutive-Pass Gate, Betriebsmodi, Pflicht-Kette (`9a0c0019`)
- Feature-Übersicht in README erweitern (`d5e1e484`)
- Roadmap und Cookbook aktualisieren (`9a17348b`)
- README vollständig LLM-agnostisch formuliert (`e39474ab`)
- LLM-Anbindung (manuell vs. API) und Roadmap ausgebaut (`e821051c`)
- Feature-Übersicht mit Beschreibungen und Mini-Beispielen ausgebaut (`b518bfe8`)
- README — Intro und Feature-Übersicht ergänzt (`42809fb0`)
- konfigurierbare Excludes + contexts/open/ Struktur nachgezogen (`e37657f0`)
- agent_start.py.md + evaluation.py.md nachgezogen (Kontext-Loader Fixes, Pfade) (`1b43c7cc`)
- README + COOKBOOK — zentrale Agent-Instanz, Migrationsrezept (#35) (`5a62e026`)
- README + COOKBOOK — Kontext-Loader (Import-Analyse + grep) dokumentiert (#33) (`63635d87`)
- agent_start.py.md — Kontext-Loader dokumentiert (#33) (`fdbe29dc`)
- README + COOKBOOK — Auto-Issue strukturierter Body dokumentiert (#29) (`42997de8`)
- evaluation.py.md + agent_start.py.md — Issue #29 dokumentiert (closes #29) (`8454793e`)
- README + COOKBOOK — Server-Staleness-Check, --force, --restart-before-eval dokumentiert (`0d530d57`)
- README + COOKBOOK aktualisiert — Prozess-Enforcement, Watch-Szenario 2, log_analyzer (closes #11) (`dc7f98d9`)
- COOKBOOK.md — 10 Rezepte mit Skynet-Referenz (closes #5) (`3d2b8ebd`)
- README — Watch-Modus, score_history, Auto-Baseline, History-in-Comments (`087b96d8`)
- README — Risiko ≥ 2 Ausnahme + steps sleep(2) + Eval-Fehler-Verhalten (`496b6caa`)
- README — Eval-System Abschnitt ergänzt (evaluation.py, agent_eval.json, steps, baseline) (`1f2ccf64`)
- evaluation.py.md + README Eval-Abschnitt (refs #1) (`5f0e6dbc`)
- README komplett überarbeitet (`702e911b`)
- Problemstellung + Lösungsansätze für LLM-Trigger vollständig dokumentiert (`a4666356`)
- README Auto-Modus Diagramm + --root Fix, Docstrings vervollständigt (`e24fbb77`)

### Wartung
- Projektstruktur bereinigt — scripts/, data/, .gitignore (closes #92) (`d5c0c6de`)
- push_github.sh — Mirror-Script für GitHub (`115f83a1`)
- add gemini_start.sh for Gemini CLI workflow (closes #61) (`403f86af`)
- contexts/ und gitea-agent.log zu .gitignore hinzufügen (`5f115235`)

### Sonstiges
- Merge pull request '[03] Refactor: evaluation.py von jetson-llm-chat entkoppeln (closes #89)' (#97) from chore/issue-89-03-refactor-evaluation-py-von-jets into main (`0e01452e`)
- Merge pull request '[02] Projektstruktur: saubere Trennung Code / Laufzeit / Config / Scripts (closes #92)' (#96) from chore/issue-92-02-projektstruktur-saubere-trennun into main (`132bb780`)
- Merge pull request 'fix: DASHBOARD_PATH korrekt in agent/data/ (closes #86)' (#93) from fix/issue-86-dashboard-path into main (`fef9fe92`)
- .env.agent aus Tracking entfernen + .gitignore ergänzt (`a2c7765c`)
- Merge pull request '--setup: Interaktiver Einrichtungs-Wizard für neue Projekte (closes #77)' (#84) from feat/issue-77-setup-wizard into main (`e884301b`)
- Merge pull request 'Inkrementelles Refactoring: patch.py + changelog.py auslagern (closes #79)' (#83) from refactor/issue-79-plugins-patch-changelog into main (`7f7edfb5`)
- Merge pull request 'Auto-Changelog-Generator (closes #39)' (#75) from feat/issue-39-auto-changelog-generator into main (`73799515`)
- Merge pull request '5 Gitea Versionsvergleich (closes #59)' (#74) from chore/issue-59-5-gitea-versionsvergleich into main (`a53138ad`)
- Merge pull request 'Flächendeckende Codesegment-Strategie — LLM lädt nur noch relevante Blöcke (closes #72)' (#73) from chore/issue-72-flaechendeckende-codesegment-strate into main (`9c94cf3e`)
- Merge pull request '4 SEARCH/REPLACE (closes #58)' (#71) from chore/issue-58-4-search-replace into main (`ce6c46a9`)
- Merge pull request '3 Diff-Validierung (closes #57)' (#70) from chore/issue-57-3-diff-validierung into main (`56126644`)
- Merge pull request 'Titel: AST-Repository-Skelett — repo_skeleton.json bei --issue generieren (closes #68)' (#69) from chore/issue-68-titel-ast-repository-skelett-repo-s into main (`f2ce6e36`)
- Merge pull request '2 AST-Skelett (closes #56)' (#67) from chore/issue-56-2-ast-skelett into main (`978b9c3d`)
- Merge pull request 'context_export.sh — LLM-agnostischer Kontext-Export + Dual-Repo-Unterstützung (closes #65)' (#66) from feat/issue-65-context-export-dual-repo into main (`6130c6fb`)
- Merge pull request 'gemini_start.sh — Gemini CLI in Agent-Workflow integrieren (closes #61)' (#63) from fix/gemini-start-correct into main (`12050f2c`)
- Merge pull request 'gemini_start.sh — Gemini CLI in Agent-Workflow integrieren (closes #61)' (#62) from chore/issue-61-gemini-start-sh-gemini-cli-in-agent into main (`d8d48224`)
- README.md aktualisiert (`7ea06675`)
- Merge pull request 'feat: Betriebsmodi — Night / Patch / Idle (closes #52)' (#53) from feat/issue-52-betriebsmodi into main (`73c2bde8`)
- Merge pull request 'feat: Auto-Issues erst nach N aufeinanderfolgenden Passes schließen (closes #50)' (#51) from feat/issue-50-consecutive-passes into main (`7691ef48`)
- README.md aktualisiert (`c6410430`)
- Merge pull request 'feat: zentrale Agent-Instanz — Pfade aus PROJECT_ROOT/agent/ lesen (closes #35)' (#36) from feat/issue-35-zentrale-agent-instanz into main (`23a3b943`)
- Merge pull request 'feat: Kontext-Loader — Import-Analyse + Keyword-Suche für files.md (closes #33)' (#34) from feat/issue-33-kontext-loader-import-analyse into main (`0cf01850`)
- Merge pull request 'feat: Auto-Issues mit strukturiertem Kontext (closes #29)' (#32) from feat/issue-29-auto-issue-strukturierter-kontext into main (`a73ecad5`)
- Merge pull request 'pr soll vor Eval prüfen ob Server-Code aktuell ist (closes #30)' (#31) from chore/issue-30-pr-soll-vor-eval-pr-fen-ob-server-c into main (`97f34766`)
- Merge pull request 'Docs: README + COOKBOOK aktualisieren — #6/#8 Details + log_analyzer Rezept (closes #11)' (#28) from docs/issue-11-docs-readme-+-cookbook-aktualisiere into main (`f8a4c2d7`)
- Merge pull request 'Audit-Fixes — 2 FAIL + 3 WARN aus Re-Audit #23 (closes #26)' (#27) from fix/issue-26-audit-fixes into main (`9d5b9c1c`)
- Merge branch 'main' into fix/issue-26-audit-fixes (`e5499fa1`)
- Merge pull request '[Audit FAIL] Szenario 2 — Watch-Neustart-Trigger nicht implementiert (closes #19)' (#21) from fix/issue-19-szenario2-watch-neustart into main (`bb446ff3`)
- Merge branch 'main' into fix/issue-19-szenario2-watch-neustart (`d4151a5f`)
- Merge pull request '[Audit FAIL] Risiko 1 — Eval wird nicht übersprungen (kein Risk-Gate) (closes #18)' (#20) from fix/issue-18-risk-gate-eval-skip into main (`f88a163f`)
- Merge branch 'main' into fix/issue-18-risk-gate-eval-skip (`5810f47e`)
- Merge pull request '_build_metadata() — Token-Schätzung + aufklappbarer Block (closes #16)' (#17) from chore/issue-16-_build_metadata-—-token-sch-tzung-+ into main (`f577cf45`)
- Merge branch 'main' into chore/issue-16-_build_metadata-—-token-sch-tzung-+ (`3b084f70`)
- Merge pull request 'Abschluss-Kommentar — Eval-Ergebnis + Metadata + Session-Status (closes #13)' (#15) from chore/issue-13-abschluss-kommentar-—-eval-ergebnis into main (`13c023f7`)
- Merge branch 'main' into chore/issue-13-abschluss-kommentar-—-eval-ergebnis (`a914faf6`)
- Merge pull request 'Fix: --eval-after-restart Diskrepanz — COOKBOOK vs agent_start.py (closes #10)' (#12) from chore/issue-10-fix-eval-after-restart-diskrepanz-— into main (`3c6a98a5`)
- Merge pull request 'fix: Output-Validierung — _validate_comment() + COMMENT_REQUIRED_FIELDS (closes #8)' (#9) from fix/issue-8-output-validierung-kommentare into main (`3813836e`)
- Merge pull request 'Prozess-Enforcement — Kontext-Drift verhindern (closes #6)' (#7) from chore/issue-6-prozess-enforcement-—-kontext-drift into main (`3adae3a6`)
- Merge pull request 'feat: evaluation.py — generisches Eval-System vor --pr (closes #1)' (#2) from feat/issue-1-evaluation-system into main (`312913a8`)

## [Unreleased] — 2026-03-26

### Features
- --setup Wizard + --doctor Health-Check (closes #77) (`2f9eaa4d`)
- Auto-Changelog-Generator — --changelog [VERSION] (closes #39) (`84b96aba`)
- Gitea-Versionsvergleich — get_file_contents, _ast_diff, _gitea_version_compare (closes #59) (`2b9afc12`)
- context_export.sh — Slice-Schleife (plain) + Slice-Anweisung (file/gemini) (`db7365a7`)
- --get-slice loggt session.json + _warn_slices_not_requested in preconditions (`d005200f`)
- files.md Skelett-Modus — kein Volltext mehr (`a831c4ed`)
- --build-skeleton + _update_skeleton_incremental + _load_skeleton_map (`004afb86`)
- SEARCH/REPLACE Protokoll — Parser, _apply_patch, cmd_apply_patch, --apply-patch/--dry-run (closes #58) (`276417f7`)
- Diff-Validierung — Scope-Warnung bei Änderungen außerhalb AST-Skelett (closes #57) (`5747d3db`)
- AST-Skelett — ClassDef/FunctionDef-Extraktion, repo_skeleton.md, --get-slice (closes #68) (`ef9c6bf8`)
- use repo_skeleton.json for files.md truncation (`b4917d1e`)
- generate repo_skeleton.json in cmd_plan (`b663b3d3`)
- introduce find_relevant_files_advanced (`687dc46f`)
- context_export.sh + Dual-Repo --self Flag + COOKBOOK.md LLM-Workflow (closes #65) (`01bc9b5a`)
- Betriebsmodi — start_patch.sh, start_night.sh, stop_agent.sh, agent_status.sh (closes #52) (`b2c0500c`)
- Auto-Issues erst nach N aufeinanderfolgenden Passes schließen (closes #50) (`69969c26`)
- add agent_eval.template.json and tag check (`87e643a4`)
- implement patch mode and live dashboard (Issue #46) (`69eafcd3`)
- implement systematic tag failure detection in watch mode (Issue #47) (`fadeb902`)
- implement test generation via LLM prompt (Issue #44) (`74cc47e6`)
- implement performance benchmarking for eval (Issue #37) (`09a8bbed`)
- Add deterministic agent self-consistency check - Add agent_self_check.py for self checks - Run check in _check_pr_preconditions - Update COOKBOOK.md (`358a041d`)
- Kontext-Zusammenfassung als Gitea-Kommentar posten (`0e4f54b6`)
- Kontext-Loader — Kommentare in Keyword-Suche einbeziehen (`3239ae8c`)
- Issue-Kommentare in starter.md aufnehmen (## Diskussion) (`4390a8e6`)
- konfigurierbare Excludes + contexts/open/ Struktur (`018ee6f8`)
- zentrale Agent-Instanz — Pfade aus PROJECT_ROOT/agent/ lesen (closes #35) (`78ff23e1`)
- Kontext-Loader — Import-Analyse + Keyword-Suche für files.md (closes #33) (`c31e26d8`)
- Auto-Issues mit strukturiertem Kontext (closes #29) (`0a5aefec`)
- --pr prüft Server-Aktualität vor Eval + --force + --restart-before-eval (closes #30) (`5f152978`)
- _build_metadata() portiert (closes #74) (`3c180cde`)
- _build_metadata() portiert — Abschluss + Fixup Kommentare (closes #74) (`125a8aa7`)
- Szenario 2 Watch-Neustart-Trigger implementieren (closes #19) (`b5515e38`)
- _build_metadata() — aufklappbarer Block + Token-Schätzung (closes #16) (`ac811b24`)
- Abschluss-Kommentar — Eval-Ergebnis + Metadata + Session-Status (closes #13) (`4f17c02f`)
- Prioritätshinweis — technische Schranken vor Prompt-Regeln (refs #6) (`ee95c312`)
- Prozess-Enforcement — 5 Maßnahmen gegen Kontext-Drift (closes #6) (`9d8d0220`)
- STARTER_MD_PFLICHTREGELN in settings.py (Maßnahme 2+3, refs #6) (`43df45d7`)
- watch_interval_minutes aus agent_eval.json + README (closes #4) (`2d3944cf`)
- cmd_watch — generischer Hook für tools/log_analyzer.py im Watch-Loop (`7c8833cb`)
- score_history letzte 5 Einträge in PR- und Watch-Kommentare (`b3687240`)
- --score-history Anzeige-Befehl — Eval-Verlauf als Tabelle im Terminal (`a5237c4b`)
- --watch Modus + score_history.json + Auto-Issues bei Score-Verlust (`c1e0f782`)
- Baseline automatisch hochsetzen wenn Score besser wird (`47e8ace1`)
- steps-Mechanismus für mehrstufige Tests (refs #1) (`1ba15002`)
- evaluation.py — generisches Eval-System vor --pr (closes #1) (`4db5b01d`)
- --fixup, Docs-Check, Auto-Cleanup + Revert-Hinweis (`a2b56ddc`)
- Kontext-Dateien pro Issue (contexts/) (`6b1b3c10`)
- Textbausteine zentralisiert in settings.py (`b341e52a`)
- Auto-Scan + working-bot Auth + generische Konfiguration via .env (`b1ce8d0e`)
- auto-modus — Gitea-States selbständig erkennen und verarbeiten (`0a427f5d`)
- initial gitea-agent MVP — LLM-agnostischer Issue-Workflow (`09a897ce`)

### Bugfixes
- DASHBOARD_PATH in settings.py ergänzt + dashboard.py bereinigt (closes #86) (`54378fd6`)
- cmd_doctor — _api_get → gitea._request, agent_eval.json auch in agent/config/ suchen (`5b51fd21`)
- CONTEXT_DIR in .env.agent — contexts statt contexts/open (verschachtelte done/ korrigiert) (`e98cb258`)
- --build-skeleton ohne Größen-Limit (max_size_kb=10000) — agent_start.py vollständig analysiert (`2a8a5eba`)
- stufe in cmd_plan nicht definiert (NameError) (`3d04cb10`)
- evaluation.py — Commit-Hash in score_history.json (closes #55) (`e9a3a89c`)
- CONTEXT_DIR aus .env.agent lesen — --self Modus funktioniert (`4e90b93d`)
- gemini_start.sh korrekt implementiert — Kontext-Loader statt Wrapper (closes #61) (`fc577d8d`)
- _sync_closed_contexts() beim Watch-Start — verwaiste open/-Ordner nach done/ verschieben (`bc15ca67`)
- Labels bei Auto-Issue-Schließung entfernen (`a8662a53`)
- _close_resolved_auto_issues verschiebt Context nach done/ (`f8d13631`)
- Bot-Kommentar-Filter nach Username statt Keywords (`61d43b1c`)
- Kontext-Loader — Datei-Blacklist + Größen-Cap (50KB) (`b9d91bf7`)
- branch_name() — Sonderzeichen robust filtern (regex statt char-Liste) (`c68ce0f7`)
- Keyword-Suche nur in Code-Dateien (nicht .md) (`797023e1`)
- Kontext-Loader — Documentation/ excluden, AST depth=1 (`17c55edb`)
- _EXCLUDE_DIRS erweitert (Backup, vendor, contexts, .claude) (`d3db3881`)
- Path() fehlt in _resolve_path/_resolve_config — TypeError bei str project_root (`c90c0005`)
- import time fehlte — Watch-Modus crashed (NameError) (`03e05791`)
- eval_line enthält 'Score:' in allen Zweigen — COMMENT_REQUIRED_FIELDS Validierung (`e61127d1`)
- eval_line enthält Score auch bei Risikostufe-1-Skip (closes #11 Nebenfix) (`61cc1eba`)
- changed=[] Fallback vor try-Block — verhindert NameError in _neustart_required() (`73f12a5c`)
- WARN 3 — doppelte _build_metadata entfernt + Agent-Metadaten (`4bcfeaad`)
- doppelte _build_metadata() entfernt + System-Infos → Agent-Metadaten (WARN 3) (`bb0c2573`)
- baseline_score statt max_score + dynamischer Neustart-Hinweis (WARN 1+2) (`9346f4f0`)
- _save_score_history() bei baseline_created — erster Lauf in score_history (closes #25) (`6be6ca09`)
- COMMENT_REQUIRED_FIELDS[completion] — Eval + Score als Pflichtfeld (closes #24) (`211f7d50`)
- Risk-Gate in cmd_pr() — Risiko 1 überspringt Eval (closes #18) (`93414369`)
- Eval-Status und Session-Status auf separate Zeilen trennen (`2ba1e80a`)
- create_pr() gibt bestehenden PR zurück bei 409 (kein Crash bei Retry) (`61447884`)
- _format_history_block gibt Verlauf-Header zurück wenn keine History (closes #10 partial) (`dc84a582`)
- --eval-after-restart implementieren — COOKBOOK/Code Diskrepanz beheben (closes #10) (`57dee635`)
- Output-Validierung — _validate_comment() + COMMENT_REQUIRED_FIELDS (closes #8) (`a7900ba5`)
- Eval-Prüfung nur wenn agent_eval.json existiert (refs #6) (`501ac6bc`)
- sleep(2) zwischen Steps — Jetson LLM-Cooldown bei mehrstufigen Tests (`132f1a2d`)
- Eval-Fehler mit Gitea-Warnung + --summary Hinweis wenn leer (`17512cf5`)
- _ping HEAD→GET, eindeutige eval_user-ID, prompt statt message (refs #1) (`f674d0ee`)
- Label-Kette + Metadaten-Block + help-wanted Korrektur (`d39112da`)

### Refactoring
- evaluation.py von jetson-llm-chat entkoppeln (closes #89) (`75cbe428`)
- patch.py + changelog.py in plugins/ auslagern (closes #79) (`6d0c13f9`)
- Konsolenausgabe auf Statusmeldungen reduziert (`b2f49239`)
- settings.py + log.py + hardcoded Werte zentralisiert (`08748bd4`)

### Tests
- Unit-Tests für kritische reine Funktionen (closes #78) (`f5903d93`)

### Dokumentation
- Dashboard-Preview SVG + README Einbindung (`d10e6ab7`)
- Architektur-Erklärung — ein Agent, beliebig viele Projekte (`e38bb993`)
- Kapitel 1 — --setup als primärer Einstieg, manuell als Fallback (`4e4a78f6`)
- private Projektnamen anonymisiert, Legacy-Sektion entfernt (`b1894c08`)
- README + COOKBOOK — neue Features, private Daten anonymisiert (`c5b04954`)
- README aktualisiert — #77/#78/#79 in Roadmap, --generate-tests in Verwendung (`c693e4e0`)
- Gitea-Versionsvergleich (#59) in README + COOKBOOK Kapitel 18 (`5dfc9e21`)
- Codesegment-Strategie (#72) in README + COOKBOOK Kapitel 17 (`3a101c03`)
- SEARCH/REPLACE (#58) in README + COOKBOOK Kapitel 16 (`630c11da`)
- Diff-Validierung (#57) in README + COOKBOOK Kapitel 15 (`65a4e5fb`)
- AST-Skelett (#68) in README Roadmap + COOKBOOK Kapitel 14 (`74b63eed`)
- README Roadmap #65 + Gemini-Warnung; COOKBOOK Kapitel 12 Querverweise (`b0fb7b19`)
- Sitzungs-Protokoll 2026-03-23 — Änderungen + Gemini CLI Warnung (`7741c6bb`)
- README aktualisiert — Consecutive-Pass Gate, Betriebsmodi, Pflicht-Kette (`9a0c0019`)
- Feature-Übersicht in README erweitern (`d5e1e484`)
- Roadmap und Cookbook aktualisieren (`9a17348b`)
- README vollständig LLM-agnostisch formuliert (`e39474ab`)
- LLM-Anbindung (manuell vs. API) und Roadmap ausgebaut (`e821051c`)
- Feature-Übersicht mit Beschreibungen und Mini-Beispielen ausgebaut (`b518bfe8`)
- README — Intro und Feature-Übersicht ergänzt (`42809fb0`)
- konfigurierbare Excludes + contexts/open/ Struktur nachgezogen (`e37657f0`)
- agent_start.py.md + evaluation.py.md nachgezogen (Kontext-Loader Fixes, Pfade) (`1b43c7cc`)
- README + COOKBOOK — zentrale Agent-Instanz, Migrationsrezept (#35) (`5a62e026`)
- README + COOKBOOK — Kontext-Loader (Import-Analyse + grep) dokumentiert (#33) (`63635d87`)
- agent_start.py.md — Kontext-Loader dokumentiert (#33) (`fdbe29dc`)
- README + COOKBOOK — Auto-Issue strukturierter Body dokumentiert (#29) (`42997de8`)
- evaluation.py.md + agent_start.py.md — Issue #29 dokumentiert (closes #29) (`8454793e`)
- README + COOKBOOK — Server-Staleness-Check, --force, --restart-before-eval dokumentiert (`0d530d57`)
- README + COOKBOOK aktualisiert — Prozess-Enforcement, Watch-Szenario 2, log_analyzer (closes #11) (`dc7f98d9`)
- COOKBOOK.md — 10 Rezepte mit Skynet-Referenz (closes #5) (`3d2b8ebd`)
- README — Watch-Modus, score_history, Auto-Baseline, History-in-Comments (`087b96d8`)
- README — Risiko ≥ 2 Ausnahme + steps sleep(2) + Eval-Fehler-Verhalten (`496b6caa`)
- README — Eval-System Abschnitt ergänzt (evaluation.py, agent_eval.json, steps, baseline) (`1f2ccf64`)
- evaluation.py.md + README Eval-Abschnitt (refs #1) (`5f0e6dbc`)
- README komplett überarbeitet (`702e911b`)
- Problemstellung + Lösungsansätze für LLM-Trigger vollständig dokumentiert (`a4666356`)
- README Auto-Modus Diagramm + --root Fix, Docstrings vervollständigt (`e24fbb77`)

### Wartung
- Projektstruktur bereinigt — scripts/, data/, .gitignore (closes #92) (`d5c0c6de`)
- push_github.sh — Mirror-Script für GitHub (`115f83a1`)
- add gemini_start.sh for Gemini CLI workflow (closes #61) (`403f86af`)
- contexts/ und gitea-agent.log zu .gitignore hinzufügen (`5f115235`)

### Sonstiges
- Merge pull request '[02] Projektstruktur: saubere Trennung Code / Laufzeit / Config / Scripts (closes #92)' (#96) from chore/issue-92-02-projektstruktur-saubere-trennun into main (`132bb780`)
- Merge pull request 'fix: DASHBOARD_PATH korrekt in agent/data/ (closes #86)' (#93) from fix/issue-86-dashboard-path into main (`fef9fe92`)
- .env.agent aus Tracking entfernen + .gitignore ergänzt (`a2c7765c`)
- Merge pull request '--setup: Interaktiver Einrichtungs-Wizard für neue Projekte (closes #77)' (#84) from feat/issue-77-setup-wizard into main (`e884301b`)
- Merge pull request 'Inkrementelles Refactoring: patch.py + changelog.py auslagern (closes #79)' (#83) from refactor/issue-79-plugins-patch-changelog into main (`7f7edfb5`)
- Merge pull request 'Auto-Changelog-Generator (closes #39)' (#75) from feat/issue-39-auto-changelog-generator into main (`73799515`)
- Merge pull request '5 Gitea Versionsvergleich (closes #59)' (#74) from chore/issue-59-5-gitea-versionsvergleich into main (`a53138ad`)
- Merge pull request 'Flächendeckende Codesegment-Strategie — LLM lädt nur noch relevante Blöcke (closes #72)' (#73) from chore/issue-72-flaechendeckende-codesegment-strate into main (`9c94cf3e`)
- Merge pull request '4 SEARCH/REPLACE (closes #58)' (#71) from chore/issue-58-4-search-replace into main (`ce6c46a9`)
- Merge pull request '3 Diff-Validierung (closes #57)' (#70) from chore/issue-57-3-diff-validierung into main (`56126644`)
- Merge pull request 'Titel: AST-Repository-Skelett — repo_skeleton.json bei --issue generieren (closes #68)' (#69) from chore/issue-68-titel-ast-repository-skelett-repo-s into main (`f2ce6e36`)
- Merge pull request '2 AST-Skelett (closes #56)' (#67) from chore/issue-56-2-ast-skelett into main (`978b9c3d`)
- Merge pull request 'context_export.sh — LLM-agnostischer Kontext-Export + Dual-Repo-Unterstützung (closes #65)' (#66) from feat/issue-65-context-export-dual-repo into main (`6130c6fb`)
- Merge pull request 'gemini_start.sh — Gemini CLI in Agent-Workflow integrieren (closes #61)' (#63) from fix/gemini-start-correct into main (`12050f2c`)
- Merge pull request 'gemini_start.sh — Gemini CLI in Agent-Workflow integrieren (closes #61)' (#62) from chore/issue-61-gemini-start-sh-gemini-cli-in-agent into main (`d8d48224`)
- README.md aktualisiert (`7ea06675`)
- Merge pull request 'feat: Betriebsmodi — Night / Patch / Idle (closes #52)' (#53) from feat/issue-52-betriebsmodi into main (`73c2bde8`)
- Merge pull request 'feat: Auto-Issues erst nach N aufeinanderfolgenden Passes schließen (closes #50)' (#51) from feat/issue-50-consecutive-passes into main (`7691ef48`)
- README.md aktualisiert (`c6410430`)
- Merge pull request 'feat: zentrale Agent-Instanz — Pfade aus PROJECT_ROOT/agent/ lesen (closes #35)' (#36) from feat/issue-35-zentrale-agent-instanz into main (`23a3b943`)
- Merge pull request 'feat: Kontext-Loader — Import-Analyse + Keyword-Suche für files.md (closes #33)' (#34) from feat/issue-33-kontext-loader-import-analyse into main (`0cf01850`)
- Merge pull request 'feat: Auto-Issues mit strukturiertem Kontext (closes #29)' (#32) from feat/issue-29-auto-issue-strukturierter-kontext into main (`a73ecad5`)
- Merge pull request 'pr soll vor Eval prüfen ob Server-Code aktuell ist (closes #30)' (#31) from chore/issue-30-pr-soll-vor-eval-pr-fen-ob-server-c into main (`97f34766`)
- Merge pull request 'Docs: README + COOKBOOK aktualisieren — #6/#8 Details + log_analyzer Rezept (closes #11)' (#28) from docs/issue-11-docs-readme-+-cookbook-aktualisiere into main (`f8a4c2d7`)
- Merge pull request 'Audit-Fixes — 2 FAIL + 3 WARN aus Re-Audit #23 (closes #26)' (#27) from fix/issue-26-audit-fixes into main (`9d5b9c1c`)
- Merge branch 'main' into fix/issue-26-audit-fixes (`e5499fa1`)
- Merge pull request '[Audit FAIL] Szenario 2 — Watch-Neustart-Trigger nicht implementiert (closes #19)' (#21) from fix/issue-19-szenario2-watch-neustart into main (`bb446ff3`)
- Merge branch 'main' into fix/issue-19-szenario2-watch-neustart (`d4151a5f`)
- Merge pull request '[Audit FAIL] Risiko 1 — Eval wird nicht übersprungen (kein Risk-Gate) (closes #18)' (#20) from fix/issue-18-risk-gate-eval-skip into main (`f88a163f`)
- Merge branch 'main' into fix/issue-18-risk-gate-eval-skip (`5810f47e`)
- Merge pull request '_build_metadata() — Token-Schätzung + aufklappbarer Block (closes #16)' (#17) from chore/issue-16-_build_metadata-—-token-sch-tzung-+ into main (`f577cf45`)
- Merge branch 'main' into chore/issue-16-_build_metadata-—-token-sch-tzung-+ (`3b084f70`)
- Merge pull request 'Abschluss-Kommentar — Eval-Ergebnis + Metadata + Session-Status (closes #13)' (#15) from chore/issue-13-abschluss-kommentar-—-eval-ergebnis into main (`13c023f7`)
- Merge branch 'main' into chore/issue-13-abschluss-kommentar-—-eval-ergebnis (`a914faf6`)
- Merge pull request 'Fix: --eval-after-restart Diskrepanz — COOKBOOK vs agent_start.py (closes #10)' (#12) from chore/issue-10-fix-eval-after-restart-diskrepanz-— into main (`3c6a98a5`)
- Merge pull request 'fix: Output-Validierung — _validate_comment() + COMMENT_REQUIRED_FIELDS (closes #8)' (#9) from fix/issue-8-output-validierung-kommentare into main (`3813836e`)
- Merge pull request 'Prozess-Enforcement — Kontext-Drift verhindern (closes #6)' (#7) from chore/issue-6-prozess-enforcement-—-kontext-drift into main (`3adae3a6`)
- Merge pull request 'feat: evaluation.py — generisches Eval-System vor --pr (closes #1)' (#2) from feat/issue-1-evaluation-system into main (`312913a8`)

## [Unreleased] — 2026-03-26

### Features
- --setup Wizard + --doctor Health-Check (closes #77) (`2f9eaa4d`)
- Auto-Changelog-Generator — --changelog [VERSION] (closes #39) (`84b96aba`)
- Gitea-Versionsvergleich — get_file_contents, _ast_diff, _gitea_version_compare (closes #59) (`2b9afc12`)
- context_export.sh — Slice-Schleife (plain) + Slice-Anweisung (file/gemini) (`db7365a7`)
- --get-slice loggt session.json + _warn_slices_not_requested in preconditions (`d005200f`)
- files.md Skelett-Modus — kein Volltext mehr (`a831c4ed`)
- --build-skeleton + _update_skeleton_incremental + _load_skeleton_map (`004afb86`)
- SEARCH/REPLACE Protokoll — Parser, _apply_patch, cmd_apply_patch, --apply-patch/--dry-run (closes #58) (`276417f7`)
- Diff-Validierung — Scope-Warnung bei Änderungen außerhalb AST-Skelett (closes #57) (`5747d3db`)
- AST-Skelett — ClassDef/FunctionDef-Extraktion, repo_skeleton.md, --get-slice (closes #68) (`ef9c6bf8`)
- use repo_skeleton.json for files.md truncation (`b4917d1e`)
- generate repo_skeleton.json in cmd_plan (`b663b3d3`)
- introduce find_relevant_files_advanced (`687dc46f`)
- context_export.sh + Dual-Repo --self Flag + COOKBOOK.md LLM-Workflow (closes #65) (`01bc9b5a`)
- Betriebsmodi — start_patch.sh, start_night.sh, stop_agent.sh, agent_status.sh (closes #52) (`b2c0500c`)
- Auto-Issues erst nach N aufeinanderfolgenden Passes schließen (closes #50) (`69969c26`)
- add agent_eval.template.json and tag check (`87e643a4`)
- implement patch mode and live dashboard (Issue #46) (`69eafcd3`)
- implement systematic tag failure detection in watch mode (Issue #47) (`fadeb902`)
- implement test generation via LLM prompt (Issue #44) (`74cc47e6`)
- implement performance benchmarking for eval (Issue #37) (`09a8bbed`)
- Add deterministic agent self-consistency check - Add agent_self_check.py for self checks - Run check in _check_pr_preconditions - Update COOKBOOK.md (`358a041d`)
- Kontext-Zusammenfassung als Gitea-Kommentar posten (`0e4f54b6`)
- Kontext-Loader — Kommentare in Keyword-Suche einbeziehen (`3239ae8c`)
- Issue-Kommentare in starter.md aufnehmen (## Diskussion) (`4390a8e6`)
- konfigurierbare Excludes + contexts/open/ Struktur (`018ee6f8`)
- zentrale Agent-Instanz — Pfade aus PROJECT_ROOT/agent/ lesen (closes #35) (`78ff23e1`)
- Kontext-Loader — Import-Analyse + Keyword-Suche für files.md (closes #33) (`c31e26d8`)
- Auto-Issues mit strukturiertem Kontext (closes #29) (`0a5aefec`)
- --pr prüft Server-Aktualität vor Eval + --force + --restart-before-eval (closes #30) (`5f152978`)
- _build_metadata() portiert (closes #74) (`3c180cde`)
- _build_metadata() portiert — Abschluss + Fixup Kommentare (closes #74) (`125a8aa7`)
- Szenario 2 Watch-Neustart-Trigger implementieren (closes #19) (`b5515e38`)
- _build_metadata() — aufklappbarer Block + Token-Schätzung (closes #16) (`ac811b24`)
- Abschluss-Kommentar — Eval-Ergebnis + Metadata + Session-Status (closes #13) (`4f17c02f`)
- Prioritätshinweis — technische Schranken vor Prompt-Regeln (refs #6) (`ee95c312`)
- Prozess-Enforcement — 5 Maßnahmen gegen Kontext-Drift (closes #6) (`9d8d0220`)
- STARTER_MD_PFLICHTREGELN in settings.py (Maßnahme 2+3, refs #6) (`43df45d7`)
- watch_interval_minutes aus agent_eval.json + README (closes #4) (`2d3944cf`)
- cmd_watch — generischer Hook für tools/log_analyzer.py im Watch-Loop (`7c8833cb`)
- score_history letzte 5 Einträge in PR- und Watch-Kommentare (`b3687240`)
- --score-history Anzeige-Befehl — Eval-Verlauf als Tabelle im Terminal (`a5237c4b`)
- --watch Modus + score_history.json + Auto-Issues bei Score-Verlust (`c1e0f782`)
- Baseline automatisch hochsetzen wenn Score besser wird (`47e8ace1`)
- steps-Mechanismus für mehrstufige Tests (refs #1) (`1ba15002`)
- evaluation.py — generisches Eval-System vor --pr (closes #1) (`4db5b01d`)
- --fixup, Docs-Check, Auto-Cleanup + Revert-Hinweis (`a2b56ddc`)
- Kontext-Dateien pro Issue (contexts/) (`6b1b3c10`)
- Textbausteine zentralisiert in settings.py (`b341e52a`)
- Auto-Scan + working-bot Auth + generische Konfiguration via .env (`b1ce8d0e`)
- auto-modus — Gitea-States selbständig erkennen und verarbeiten (`0a427f5d`)
- initial gitea-agent MVP — LLM-agnostischer Issue-Workflow (`09a897ce`)

### Bugfixes
- DASHBOARD_PATH in settings.py ergänzt + dashboard.py bereinigt (closes #86) (`54378fd6`)
- cmd_doctor — _api_get → gitea._request, agent_eval.json auch in agent/config/ suchen (`5b51fd21`)
- CONTEXT_DIR in .env.agent — contexts statt contexts/open (verschachtelte done/ korrigiert) (`e98cb258`)
- --build-skeleton ohne Größen-Limit (max_size_kb=10000) — agent_start.py vollständig analysiert (`2a8a5eba`)
- stufe in cmd_plan nicht definiert (NameError) (`3d04cb10`)
- evaluation.py — Commit-Hash in score_history.json (closes #55) (`e9a3a89c`)
- CONTEXT_DIR aus .env.agent lesen — --self Modus funktioniert (`4e90b93d`)
- gemini_start.sh korrekt implementiert — Kontext-Loader statt Wrapper (closes #61) (`fc577d8d`)
- _sync_closed_contexts() beim Watch-Start — verwaiste open/-Ordner nach done/ verschieben (`bc15ca67`)
- Labels bei Auto-Issue-Schließung entfernen (`a8662a53`)
- _close_resolved_auto_issues verschiebt Context nach done/ (`f8d13631`)
- Bot-Kommentar-Filter nach Username statt Keywords (`61d43b1c`)
- Kontext-Loader — Datei-Blacklist + Größen-Cap (50KB) (`b9d91bf7`)
- branch_name() — Sonderzeichen robust filtern (regex statt char-Liste) (`c68ce0f7`)
- Keyword-Suche nur in Code-Dateien (nicht .md) (`797023e1`)
- Kontext-Loader — Documentation/ excluden, AST depth=1 (`17c55edb`)
- _EXCLUDE_DIRS erweitert (Backup, vendor, contexts, .claude) (`d3db3881`)
- Path() fehlt in _resolve_path/_resolve_config — TypeError bei str project_root (`c90c0005`)
- import time fehlte — Watch-Modus crashed (NameError) (`03e05791`)
- eval_line enthält 'Score:' in allen Zweigen — COMMENT_REQUIRED_FIELDS Validierung (`e61127d1`)
- eval_line enthält Score auch bei Risikostufe-1-Skip (closes #11 Nebenfix) (`61cc1eba`)
- changed=[] Fallback vor try-Block — verhindert NameError in _neustart_required() (`73f12a5c`)
- WARN 3 — doppelte _build_metadata entfernt + Agent-Metadaten (`4bcfeaad`)
- doppelte _build_metadata() entfernt + System-Infos → Agent-Metadaten (WARN 3) (`bb0c2573`)
- baseline_score statt max_score + dynamischer Neustart-Hinweis (WARN 1+2) (`9346f4f0`)
- _save_score_history() bei baseline_created — erster Lauf in score_history (closes #25) (`6be6ca09`)
- COMMENT_REQUIRED_FIELDS[completion] — Eval + Score als Pflichtfeld (closes #24) (`211f7d50`)
- Risk-Gate in cmd_pr() — Risiko 1 überspringt Eval (closes #18) (`93414369`)
- Eval-Status und Session-Status auf separate Zeilen trennen (`2ba1e80a`)
- create_pr() gibt bestehenden PR zurück bei 409 (kein Crash bei Retry) (`61447884`)
- _format_history_block gibt Verlauf-Header zurück wenn keine History (closes #10 partial) (`dc84a582`)
- --eval-after-restart implementieren — COOKBOOK/Code Diskrepanz beheben (closes #10) (`57dee635`)
- Output-Validierung — _validate_comment() + COMMENT_REQUIRED_FIELDS (closes #8) (`a7900ba5`)
- Eval-Prüfung nur wenn agent_eval.json existiert (refs #6) (`501ac6bc`)
- sleep(2) zwischen Steps — Jetson LLM-Cooldown bei mehrstufigen Tests (`132f1a2d`)
- Eval-Fehler mit Gitea-Warnung + --summary Hinweis wenn leer (`17512cf5`)
- _ping HEAD→GET, eindeutige eval_user-ID, prompt statt message (refs #1) (`f674d0ee`)
- Label-Kette + Metadaten-Block + help-wanted Korrektur (`d39112da`)

### Refactoring
- patch.py + changelog.py in plugins/ auslagern (closes #79) (`6d0c13f9`)
- Konsolenausgabe auf Statusmeldungen reduziert (`b2f49239`)
- settings.py + log.py + hardcoded Werte zentralisiert (`08748bd4`)

### Tests
- Unit-Tests für kritische reine Funktionen (closes #78) (`f5903d93`)

### Dokumentation
- Dashboard-Preview SVG + README Einbindung (`d10e6ab7`)
- Architektur-Erklärung — ein Agent, beliebig viele Projekte (`e38bb993`)
- Kapitel 1 — --setup als primärer Einstieg, manuell als Fallback (`4e4a78f6`)
- private Projektnamen anonymisiert, Legacy-Sektion entfernt (`b1894c08`)
- README + COOKBOOK — neue Features, private Daten anonymisiert (`c5b04954`)
- README aktualisiert — #77/#78/#79 in Roadmap, --generate-tests in Verwendung (`c693e4e0`)
- Gitea-Versionsvergleich (#59) in README + COOKBOOK Kapitel 18 (`5dfc9e21`)
- Codesegment-Strategie (#72) in README + COOKBOOK Kapitel 17 (`3a101c03`)
- SEARCH/REPLACE (#58) in README + COOKBOOK Kapitel 16 (`630c11da`)
- Diff-Validierung (#57) in README + COOKBOOK Kapitel 15 (`65a4e5fb`)
- AST-Skelett (#68) in README Roadmap + COOKBOOK Kapitel 14 (`74b63eed`)
- README Roadmap #65 + Gemini-Warnung; COOKBOOK Kapitel 12 Querverweise (`b0fb7b19`)
- Sitzungs-Protokoll 2026-03-23 — Änderungen + Gemini CLI Warnung (`7741c6bb`)
- README aktualisiert — Consecutive-Pass Gate, Betriebsmodi, Pflicht-Kette (`9a0c0019`)
- Feature-Übersicht in README erweitern (`d5e1e484`)
- Roadmap und Cookbook aktualisieren (`9a17348b`)
- README vollständig LLM-agnostisch formuliert (`e39474ab`)
- LLM-Anbindung (manuell vs. API) und Roadmap ausgebaut (`e821051c`)
- Feature-Übersicht mit Beschreibungen und Mini-Beispielen ausgebaut (`b518bfe8`)
- README — Intro und Feature-Übersicht ergänzt (`42809fb0`)
- konfigurierbare Excludes + contexts/open/ Struktur nachgezogen (`e37657f0`)
- agent_start.py.md + evaluation.py.md nachgezogen (Kontext-Loader Fixes, Pfade) (`1b43c7cc`)
- README + COOKBOOK — zentrale Agent-Instanz, Migrationsrezept (#35) (`5a62e026`)
- README + COOKBOOK — Kontext-Loader (Import-Analyse + grep) dokumentiert (#33) (`63635d87`)
- agent_start.py.md — Kontext-Loader dokumentiert (#33) (`fdbe29dc`)
- README + COOKBOOK — Auto-Issue strukturierter Body dokumentiert (#29) (`42997de8`)
- evaluation.py.md + agent_start.py.md — Issue #29 dokumentiert (closes #29) (`8454793e`)
- README + COOKBOOK — Server-Staleness-Check, --force, --restart-before-eval dokumentiert (`0d530d57`)
- README + COOKBOOK aktualisiert — Prozess-Enforcement, Watch-Szenario 2, log_analyzer (closes #11) (`dc7f98d9`)
- COOKBOOK.md — 10 Rezepte mit Skynet-Referenz (closes #5) (`3d2b8ebd`)
- README — Watch-Modus, score_history, Auto-Baseline, History-in-Comments (`087b96d8`)
- README — Risiko ≥ 2 Ausnahme + steps sleep(2) + Eval-Fehler-Verhalten (`496b6caa`)
- README — Eval-System Abschnitt ergänzt (evaluation.py, agent_eval.json, steps, baseline) (`1f2ccf64`)
- evaluation.py.md + README Eval-Abschnitt (refs #1) (`5f0e6dbc`)
- README komplett überarbeitet (`702e911b`)
- Problemstellung + Lösungsansätze für LLM-Trigger vollständig dokumentiert (`a4666356`)
- README Auto-Modus Diagramm + --root Fix, Docstrings vervollständigt (`e24fbb77`)

### Wartung
- Projektstruktur bereinigt — scripts/, data/, .gitignore (closes #92) (`d5c0c6de`)
- push_github.sh — Mirror-Script für GitHub (`115f83a1`)
- add gemini_start.sh for Gemini CLI workflow (closes #61) (`403f86af`)
- contexts/ und gitea-agent.log zu .gitignore hinzufügen (`5f115235`)

### Sonstiges
- .env.agent aus Tracking entfernen + .gitignore ergänzt (`a2c7765c`)
- Merge pull request '--setup: Interaktiver Einrichtungs-Wizard für neue Projekte (closes #77)' (#84) from feat/issue-77-setup-wizard into main (`e884301b`)
- Merge pull request 'Inkrementelles Refactoring: patch.py + changelog.py auslagern (closes #79)' (#83) from refactor/issue-79-plugins-patch-changelog into main (`7f7edfb5`)
- Merge pull request 'Auto-Changelog-Generator (closes #39)' (#75) from feat/issue-39-auto-changelog-generator into main (`73799515`)
- Merge pull request '5 Gitea Versionsvergleich (closes #59)' (#74) from chore/issue-59-5-gitea-versionsvergleich into main (`a53138ad`)
- Merge pull request 'Flächendeckende Codesegment-Strategie — LLM lädt nur noch relevante Blöcke (closes #72)' (#73) from chore/issue-72-flaechendeckende-codesegment-strate into main (`9c94cf3e`)
- Merge pull request '4 SEARCH/REPLACE (closes #58)' (#71) from chore/issue-58-4-search-replace into main (`ce6c46a9`)
- Merge pull request '3 Diff-Validierung (closes #57)' (#70) from chore/issue-57-3-diff-validierung into main (`56126644`)
- Merge pull request 'Titel: AST-Repository-Skelett — repo_skeleton.json bei --issue generieren (closes #68)' (#69) from chore/issue-68-titel-ast-repository-skelett-repo-s into main (`f2ce6e36`)
- Merge pull request '2 AST-Skelett (closes #56)' (#67) from chore/issue-56-2-ast-skelett into main (`978b9c3d`)
- Merge pull request 'context_export.sh — LLM-agnostischer Kontext-Export + Dual-Repo-Unterstützung (closes #65)' (#66) from feat/issue-65-context-export-dual-repo into main (`6130c6fb`)
- Merge pull request 'gemini_start.sh — Gemini CLI in Agent-Workflow integrieren (closes #61)' (#63) from fix/gemini-start-correct into main (`12050f2c`)
- Merge pull request 'gemini_start.sh — Gemini CLI in Agent-Workflow integrieren (closes #61)' (#62) from chore/issue-61-gemini-start-sh-gemini-cli-in-agent into main (`d8d48224`)
- README.md aktualisiert (`7ea06675`)
- Merge pull request 'feat: Betriebsmodi — Night / Patch / Idle (closes #52)' (#53) from feat/issue-52-betriebsmodi into main (`73c2bde8`)
- Merge pull request 'feat: Auto-Issues erst nach N aufeinanderfolgenden Passes schließen (closes #50)' (#51) from feat/issue-50-consecutive-passes into main (`7691ef48`)
- README.md aktualisiert (`c6410430`)
- Merge pull request 'feat: zentrale Agent-Instanz — Pfade aus PROJECT_ROOT/agent/ lesen (closes #35)' (#36) from feat/issue-35-zentrale-agent-instanz into main (`23a3b943`)
- Merge pull request 'feat: Kontext-Loader — Import-Analyse + Keyword-Suche für files.md (closes #33)' (#34) from feat/issue-33-kontext-loader-import-analyse into main (`0cf01850`)
- Merge pull request 'feat: Auto-Issues mit strukturiertem Kontext (closes #29)' (#32) from feat/issue-29-auto-issue-strukturierter-kontext into main (`a73ecad5`)
- Merge pull request 'pr soll vor Eval prüfen ob Server-Code aktuell ist (closes #30)' (#31) from chore/issue-30-pr-soll-vor-eval-pr-fen-ob-server-c into main (`97f34766`)
- Merge pull request 'Docs: README + COOKBOOK aktualisieren — #6/#8 Details + log_analyzer Rezept (closes #11)' (#28) from docs/issue-11-docs-readme-+-cookbook-aktualisiere into main (`f8a4c2d7`)
- Merge pull request 'Audit-Fixes — 2 FAIL + 3 WARN aus Re-Audit #23 (closes #26)' (#27) from fix/issue-26-audit-fixes into main (`9d5b9c1c`)
- Merge branch 'main' into fix/issue-26-audit-fixes (`e5499fa1`)
- Merge pull request '[Audit FAIL] Szenario 2 — Watch-Neustart-Trigger nicht implementiert (closes #19)' (#21) from fix/issue-19-szenario2-watch-neustart into main (`bb446ff3`)
- Merge branch 'main' into fix/issue-19-szenario2-watch-neustart (`d4151a5f`)
- Merge pull request '[Audit FAIL] Risiko 1 — Eval wird nicht übersprungen (kein Risk-Gate) (closes #18)' (#20) from fix/issue-18-risk-gate-eval-skip into main (`f88a163f`)
- Merge branch 'main' into fix/issue-18-risk-gate-eval-skip (`5810f47e`)
- Merge pull request '_build_metadata() — Token-Schätzung + aufklappbarer Block (closes #16)' (#17) from chore/issue-16-_build_metadata-—-token-sch-tzung-+ into main (`f577cf45`)
- Merge branch 'main' into chore/issue-16-_build_metadata-—-token-sch-tzung-+ (`3b084f70`)
- Merge pull request 'Abschluss-Kommentar — Eval-Ergebnis + Metadata + Session-Status (closes #13)' (#15) from chore/issue-13-abschluss-kommentar-—-eval-ergebnis into main (`13c023f7`)
- Merge branch 'main' into chore/issue-13-abschluss-kommentar-—-eval-ergebnis (`a914faf6`)
- Merge pull request 'Fix: --eval-after-restart Diskrepanz — COOKBOOK vs agent_start.py (closes #10)' (#12) from chore/issue-10-fix-eval-after-restart-diskrepanz-— into main (`3c6a98a5`)
- Merge pull request 'fix: Output-Validierung — _validate_comment() + COMMENT_REQUIRED_FIELDS (closes #8)' (#9) from fix/issue-8-output-validierung-kommentare into main (`3813836e`)
- Merge pull request 'Prozess-Enforcement — Kontext-Drift verhindern (closes #6)' (#7) from chore/issue-6-prozess-enforcement-—-kontext-drift into main (`3adae3a6`)
- Merge pull request 'feat: evaluation.py — generisches Eval-System vor --pr (closes #1)' (#2) from feat/issue-1-evaluation-system into main (`312913a8`)

## [Unreleased] — 2026-03-25

### Features
- --setup Wizard + --doctor Health-Check (closes #77) (`6fe891b3`)
- Auto-Changelog-Generator — --changelog [VERSION] (closes #39) (`6fe6ef29`)
- Gitea-Versionsvergleich — get_file_contents, _ast_diff, _gitea_version_compare (closes #59) (`002ba078`)
- context_export.sh — Slice-Schleife (plain) + Slice-Anweisung (file/gemini) (`09a855b1`)
- --get-slice loggt session.json + _warn_slices_not_requested in preconditions (`c9f0a470`)
- files.md Skelett-Modus — kein Volltext mehr (`02be831e`)
- --build-skeleton + _update_skeleton_incremental + _load_skeleton_map (`878ec0d3`)
- SEARCH/REPLACE Protokoll — Parser, _apply_patch, cmd_apply_patch, --apply-patch/--dry-run (closes #58) (`d3fd8f5d`)
- Diff-Validierung — Scope-Warnung bei Änderungen außerhalb AST-Skelett (closes #57) (`88e6205c`)
- AST-Skelett — ClassDef/FunctionDef-Extraktion, repo_skeleton.md, --get-slice (closes #68) (`0955d0cb`)
- use repo_skeleton.json for files.md truncation (`e6ff1c79`)
- generate repo_skeleton.json in cmd_plan (`d1910e3a`)
- introduce find_relevant_files_advanced (`1259b4a0`)
- context_export.sh + Dual-Repo --self Flag + COOKBOOK.md LLM-Workflow (closes #65) (`bc8367d5`)
- Betriebsmodi — start_patch.sh, start_night.sh, stop_agent.sh, agent_status.sh (closes #52) (`3e4a8123`)
- Auto-Issues erst nach N aufeinanderfolgenden Passes schließen (closes #50) (`601fc854`)
- add agent_eval.template.json and tag check (`5993a82a`)
- implement patch mode and live dashboard (Issue #46) (`39216083`)
- implement systematic tag failure detection in watch mode (Issue #47) (`b65bcc70`)
- implement test generation via LLM prompt (Issue #44) (`2b5b8cc8`)
- implement performance benchmarking for eval (Issue #37) (`de88cbb9`)
- Add deterministic agent self-consistency check - Add agent_self_check.py for self checks - Run check in _check_pr_preconditions - Update COOKBOOK.md (`fb5d3306`)
- Kontext-Zusammenfassung als Gitea-Kommentar posten (`710d678c`)
- Kontext-Loader — Kommentare in Keyword-Suche einbeziehen (`e9f173fd`)
- Issue-Kommentare in starter.md aufnehmen (## Diskussion) (`080dae98`)
- konfigurierbare Excludes + contexts/open/ Struktur (`e81b7718`)
- zentrale Agent-Instanz — Pfade aus PROJECT_ROOT/agent/ lesen (closes #35) (`c8cb35b6`)
- Kontext-Loader — Import-Analyse + Keyword-Suche für files.md (closes #33) (`be8b1756`)
- Auto-Issues mit strukturiertem Kontext (closes #29) (`3481c6e9`)
- --pr prüft Server-Aktualität vor Eval + --force + --restart-before-eval (closes #30) (`10372c09`)
- _build_metadata() portiert (closes #74) (`c02a3a60`)
- _build_metadata() portiert — Abschluss + Fixup Kommentare (closes #74) (`186981b3`)
- Szenario 2 Watch-Neustart-Trigger implementieren (closes #19) (`04d342f2`)
- _build_metadata() — aufklappbarer Block + Token-Schätzung (closes #16) (`5fce1593`)
- Abschluss-Kommentar — Eval-Ergebnis + Metadata + Session-Status (closes #13) (`a923edc4`)
- Prioritätshinweis — technische Schranken vor Prompt-Regeln (refs #6) (`5955f2ac`)
- Prozess-Enforcement — 5 Maßnahmen gegen Kontext-Drift (closes #6) (`856c3bfd`)
- STARTER_MD_PFLICHTREGELN in settings.py (Maßnahme 2+3, refs #6) (`87cc28a9`)
- watch_interval_minutes aus agent_eval.json + README (closes #4) (`2c559d37`)
- cmd_watch — generischer Hook für tools/log_analyzer.py im Watch-Loop (`fb7f335f`)
- score_history letzte 5 Einträge in PR- und Watch-Kommentare (`b4ec8ab3`)
- --score-history Anzeige-Befehl — Eval-Verlauf als Tabelle im Terminal (`736cfa8b`)
- --watch Modus + score_history.json + Auto-Issues bei Score-Verlust (`8ed46cc7`)
- Baseline automatisch hochsetzen wenn Score besser wird (`172ee6a6`)
- steps-Mechanismus für mehrstufige Tests (refs #1) (`264b225e`)
- evaluation.py — generisches Eval-System vor --pr (closes #1) (`b9fc5989`)
- --fixup, Docs-Check, Auto-Cleanup + Revert-Hinweis (`323247d5`)
- Kontext-Dateien pro Issue (contexts/) (`5cafee8e`)
- Textbausteine zentralisiert in settings.py (`cd368efc`)
- Auto-Scan + working-bot Auth + generische Konfiguration via .env (`abb3b05b`)
- auto-modus — Gitea-States selbständig erkennen und verarbeiten (`c6b8db83`)
- initial gitea-agent MVP — LLM-agnostischer Issue-Workflow (`0b12bfe7`)

### Bugfixes
- CONTEXT_DIR in .env.agent — contexts statt contexts/open (verschachtelte done/ korrigiert) (`91eb9658`)
- --build-skeleton ohne Größen-Limit (max_size_kb=10000) — agent_start.py vollständig analysiert (`04638aaa`)
- stufe in cmd_plan nicht definiert (NameError) (`67cda8ee`)
- evaluation.py — Commit-Hash in score_history.json (closes #55) (`8eea1978`)
- CONTEXT_DIR aus .env.agent lesen — --self Modus funktioniert (`4e703ed8`)
- gemini_start.sh korrekt implementiert — Kontext-Loader statt Wrapper (closes #61) (`a27f30b4`)
- _sync_closed_contexts() beim Watch-Start — verwaiste open/-Ordner nach done/ verschieben (`8ff336b4`)
- Labels bei Auto-Issue-Schließung entfernen (`ecabd722`)
- _close_resolved_auto_issues verschiebt Context nach done/ (`781241e3`)
- Bot-Kommentar-Filter nach Username statt Keywords (`d7c6dd3c`)
- Kontext-Loader — Datei-Blacklist + Größen-Cap (50KB) (`d4b53d59`)
- branch_name() — Sonderzeichen robust filtern (regex statt char-Liste) (`3517e66e`)
- Keyword-Suche nur in Code-Dateien (nicht .md) (`c01ee514`)
- Kontext-Loader — Documentation/ excluden, AST depth=1 (`919cadc4`)
- _EXCLUDE_DIRS erweitert (Backup, vendor, contexts, .claude) (`b9d69bbb`)
- Path() fehlt in _resolve_path/_resolve_config — TypeError bei str project_root (`9232f4c8`)
- import time fehlte — Watch-Modus crashed (NameError) (`8f3e282a`)
- eval_line enthält 'Score:' in allen Zweigen — COMMENT_REQUIRED_FIELDS Validierung (`bc95cd25`)
- eval_line enthält Score auch bei Risikostufe-1-Skip (closes #11 Nebenfix) (`ebb44101`)
- changed=[] Fallback vor try-Block — verhindert NameError in _neustart_required() (`42cf01a9`)
- WARN 3 — doppelte _build_metadata entfernt + Agent-Metadaten (`679d53b1`)
- doppelte _build_metadata() entfernt + System-Infos → Agent-Metadaten (WARN 3) (`fedc4267`)
- baseline_score statt max_score + dynamischer Neustart-Hinweis (WARN 1+2) (`59602542`)
- _save_score_history() bei baseline_created — erster Lauf in score_history (closes #25) (`c559f24b`)
- COMMENT_REQUIRED_FIELDS[completion] — Eval + Score als Pflichtfeld (closes #24) (`af70545a`)
- Risk-Gate in cmd_pr() — Risiko 1 überspringt Eval (closes #18) (`da11d4a4`)
- Eval-Status und Session-Status auf separate Zeilen trennen (`dea0efda`)
- create_pr() gibt bestehenden PR zurück bei 409 (kein Crash bei Retry) (`71047b4c`)
- _format_history_block gibt Verlauf-Header zurück wenn keine History (closes #10 partial) (`85714c24`)
- --eval-after-restart implementieren — COOKBOOK/Code Diskrepanz beheben (closes #10) (`7fe798ab`)
- Output-Validierung — _validate_comment() + COMMENT_REQUIRED_FIELDS (closes #8) (`c448c355`)
- Eval-Prüfung nur wenn agent_eval.json existiert (refs #6) (`bf616363`)
- sleep(2) zwischen Steps — Jetson LLM-Cooldown bei mehrstufigen Tests (`c63b6138`)
- Eval-Fehler mit Gitea-Warnung + --summary Hinweis wenn leer (`1930f256`)
- _ping HEAD→GET, eindeutige eval_user-ID, prompt statt message (refs #1) (`aa2aea51`)
- Label-Kette + Metadaten-Block + help-wanted Korrektur (`f01df702`)

### Refactoring
- patch.py + changelog.py in plugins/ auslagern (closes #79) (`25aa4d7d`)
- Konsolenausgabe auf Statusmeldungen reduziert (`76d92bf3`)
- settings.py + log.py + hardcoded Werte zentralisiert (`bd9a06b1`)

### Tests
- Unit-Tests für kritische reine Funktionen (closes #78) (`5a86bb92`)

### Dokumentation
- Gitea-Versionsvergleich (#59) in README + COOKBOOK Kapitel 18 (`10a090fb`)
- Codesegment-Strategie (#72) in README + COOKBOOK Kapitel 17 (`116b63d7`)
- SEARCH/REPLACE (#58) in README + COOKBOOK Kapitel 16 (`6de6d1b1`)
- Diff-Validierung (#57) in README + COOKBOOK Kapitel 15 (`866a602f`)
- AST-Skelett (#68) in README Roadmap + COOKBOOK Kapitel 14 (`1f9db1ca`)
- README Roadmap #65 + Gemini-Warnung; COOKBOOK Kapitel 12 Querverweise (`c06fcae9`)
- Sitzungs-Protokoll 2026-03-23 — Änderungen + Gemini CLI Warnung (`4bfe8931`)
- README aktualisiert — Consecutive-Pass Gate, Betriebsmodi, Pflicht-Kette (`53c23453`)
- Feature-Übersicht in README erweitern (`d0dc2085`)
- Roadmap und Cookbook aktualisieren (`cee2ddb1`)
- README vollständig LLM-agnostisch formuliert (`49aa7aaa`)
- LLM-Anbindung (manuell vs. API) und Roadmap ausgebaut (`7626eb6b`)
- Feature-Übersicht mit Beschreibungen und Mini-Beispielen ausgebaut (`d3e1bb71`)
- README — Intro und Feature-Übersicht ergänzt (`dc1aeb0c`)
- konfigurierbare Excludes + contexts/open/ Struktur nachgezogen (`c7ceb8a5`)
- agent_start.py.md + evaluation.py.md nachgezogen (Kontext-Loader Fixes, Pfade) (`b25086e1`)
- README + COOKBOOK — zentrale Agent-Instanz, Migrationsrezept (#35) (`6fd0b794`)
- README + COOKBOOK — Kontext-Loader (Import-Analyse + grep) dokumentiert (#33) (`da428dc8`)
- agent_start.py.md — Kontext-Loader dokumentiert (#33) (`ae75ebab`)
- README + COOKBOOK — Auto-Issue strukturierter Body dokumentiert (#29) (`a0c81c81`)
- evaluation.py.md + agent_start.py.md — Issue #29 dokumentiert (closes #29) (`0b8c0d9e`)
- README + COOKBOOK — Server-Staleness-Check, --force, --restart-before-eval dokumentiert (`f595f400`)
- README + COOKBOOK aktualisiert — Prozess-Enforcement, Watch-Szenario 2, log_analyzer (closes #11) (`30f58dee`)
- COOKBOOK.md — 10 Rezepte mit Skynet-Referenz (closes #5) (`8dd50f42`)
- README — Watch-Modus, score_history, Auto-Baseline, History-in-Comments (`ca88ce6a`)
- README — Risiko ≥ 2 Ausnahme + steps sleep(2) + Eval-Fehler-Verhalten (`b80a991d`)
- README — Eval-System Abschnitt ergänzt (evaluation.py, agent_eval.json, steps, baseline) (`4f31887f`)
- evaluation.py.md + README Eval-Abschnitt (refs #1) (`7ca16b68`)
- README komplett überarbeitet (`0e3dad45`)
- Problemstellung + Lösungsansätze für LLM-Trigger vollständig dokumentiert (`be1b4f61`)
- README Auto-Modus Diagramm + --root Fix, Docstrings vervollständigt (`06098992`)

### Wartung
- add gemini_start.sh for Gemini CLI workflow (closes #61) (`9e8639dd`)
- contexts/ und gitea-agent.log zu .gitignore hinzufügen (`a96ce2bb`)

### Sonstiges
- Merge pull request 'Auto-Changelog-Generator (closes #39)' (#75) from feat/issue-39-auto-changelog-generator into main (`955ba08d`)
- Merge pull request '5 Gitea Versionsvergleich (closes #59)' (#74) from chore/issue-59-5-gitea-versionsvergleich into main (`bba525fc`)
- Merge pull request 'Flächendeckende Codesegment-Strategie — LLM lädt nur noch relevante Blöcke (closes #72)' (#73) from chore/issue-72-flaechendeckende-codesegment-strate into main (`0e3a3121`)
- Merge pull request '4 SEARCH/REPLACE (closes #58)' (#71) from chore/issue-58-4-search-replace into main (`9b9b6f8d`)
- Merge pull request '3 Diff-Validierung (closes #57)' (#70) from chore/issue-57-3-diff-validierung into main (`5ebeb5ae`)
- Merge pull request 'Titel: AST-Repository-Skelett — repo_skeleton.json bei --issue generieren (closes #68)' (#69) from chore/issue-68-titel-ast-repository-skelett-repo-s into main (`0995050b`)
- Merge pull request '2 AST-Skelett (closes #56)' (#67) from chore/issue-56-2-ast-skelett into main (`757ba664`)
- Merge pull request 'context_export.sh — LLM-agnostischer Kontext-Export + Dual-Repo-Unterstützung (closes #65)' (#66) from feat/issue-65-context-export-dual-repo into main (`f9fa27f3`)
- Merge pull request 'gemini_start.sh — Gemini CLI in Agent-Workflow integrieren (closes #61)' (#63) from fix/gemini-start-correct into main (`3a75fa28`)
- Merge pull request 'gemini_start.sh — Gemini CLI in Agent-Workflow integrieren (closes #61)' (#62) from chore/issue-61-gemini-start-sh-gemini-cli-in-agent into main (`d6c0fa82`)
- README.md aktualisiert (`8be81bdc`)
- Merge pull request 'feat: Betriebsmodi — Night / Patch / Idle (closes #52)' (#53) from feat/issue-52-betriebsmodi into main (`611e83b5`)
- Merge pull request 'feat: Auto-Issues erst nach N aufeinanderfolgenden Passes schließen (closes #50)' (#51) from feat/issue-50-consecutive-passes into main (`ac05027a`)
- README.md aktualisiert (`738c7e45`)
- Merge pull request 'feat: zentrale Agent-Instanz — Pfade aus PROJECT_ROOT/agent/ lesen (closes #35)' (#36) from feat/issue-35-zentrale-agent-instanz into main (`4f9c730d`)
- Merge pull request 'feat: Kontext-Loader — Import-Analyse + Keyword-Suche für files.md (closes #33)' (#34) from feat/issue-33-kontext-loader-import-analyse into main (`67f1598b`)
- Merge pull request 'feat: Auto-Issues mit strukturiertem Kontext (closes #29)' (#32) from feat/issue-29-auto-issue-strukturierter-kontext into main (`2030c2c1`)
- Merge pull request 'pr soll vor Eval prüfen ob Server-Code aktuell ist (closes #30)' (#31) from chore/issue-30-pr-soll-vor-eval-pr-fen-ob-server-c into main (`760f6372`)
- Merge pull request 'Docs: README + COOKBOOK aktualisieren — #6/#8 Details + log_analyzer Rezept (closes #11)' (#28) from docs/issue-11-docs-readme-+-cookbook-aktualisiere into main (`c377a0fe`)
- Merge pull request 'Audit-Fixes — 2 FAIL + 3 WARN aus Re-Audit #23 (closes #26)' (#27) from fix/issue-26-audit-fixes into main (`93d4e687`)
- Merge branch 'main' into fix/issue-26-audit-fixes (`3e4fc6c0`)
- Merge pull request '[Audit FAIL] Szenario 2 — Watch-Neustart-Trigger nicht implementiert (closes #19)' (#21) from fix/issue-19-szenario2-watch-neustart into main (`bb9af28e`)
- Merge branch 'main' into fix/issue-19-szenario2-watch-neustart (`6f745179`)
- Merge pull request '[Audit FAIL] Risiko 1 — Eval wird nicht übersprungen (kein Risk-Gate) (closes #18)' (#20) from fix/issue-18-risk-gate-eval-skip into main (`ac2dcfeb`)
- Merge branch 'main' into fix/issue-18-risk-gate-eval-skip (`c0d56c83`)
- Merge pull request '_build_metadata() — Token-Schätzung + aufklappbarer Block (closes #16)' (#17) from chore/issue-16-_build_metadata-—-token-sch-tzung-+ into main (`933eea13`)
- Merge branch 'main' into chore/issue-16-_build_metadata-—-token-sch-tzung-+ (`878c125d`)
- Merge pull request 'Abschluss-Kommentar — Eval-Ergebnis + Metadata + Session-Status (closes #13)' (#15) from chore/issue-13-abschluss-kommentar-—-eval-ergebnis into main (`4910ddca`)
- Merge branch 'main' into chore/issue-13-abschluss-kommentar-—-eval-ergebnis (`0a2c3e12`)
- Merge pull request 'Fix: --eval-after-restart Diskrepanz — COOKBOOK vs agent_start.py (closes #10)' (#12) from chore/issue-10-fix-eval-after-restart-diskrepanz-— into main (`835c262c`)
- Merge pull request 'fix: Output-Validierung — _validate_comment() + COMMENT_REQUIRED_FIELDS (closes #8)' (#9) from fix/issue-8-output-validierung-kommentare into main (`17d905c6`)
- Merge pull request 'Prozess-Enforcement — Kontext-Drift verhindern (closes #6)' (#7) from chore/issue-6-prozess-enforcement-—-kontext-drift into main (`dc366abb`)
- Merge pull request 'feat: evaluation.py — generisches Eval-System vor --pr (closes #1)' (#2) from feat/issue-1-evaluation-system into main (`afb8a7a8`)

## [Unreleased] — 2026-03-25

### Features
- --setup Wizard + --doctor Health-Check (closes #77) (`6fe891b3`)
- Auto-Changelog-Generator — --changelog [VERSION] (closes #39) (`6fe6ef29`)
- Gitea-Versionsvergleich — get_file_contents, _ast_diff, _gitea_version_compare (closes #59) (`002ba078`)
- context_export.sh — Slice-Schleife (plain) + Slice-Anweisung (file/gemini) (`09a855b1`)
- --get-slice loggt session.json + _warn_slices_not_requested in preconditions (`c9f0a470`)
- files.md Skelett-Modus — kein Volltext mehr (`02be831e`)
- --build-skeleton + _update_skeleton_incremental + _load_skeleton_map (`878ec0d3`)
- SEARCH/REPLACE Protokoll — Parser, _apply_patch, cmd_apply_patch, --apply-patch/--dry-run (closes #58) (`d3fd8f5d`)
- Diff-Validierung — Scope-Warnung bei Änderungen außerhalb AST-Skelett (closes #57) (`88e6205c`)
- AST-Skelett — ClassDef/FunctionDef-Extraktion, repo_skeleton.md, --get-slice (closes #68) (`0955d0cb`)
- use repo_skeleton.json for files.md truncation (`e6ff1c79`)
- generate repo_skeleton.json in cmd_plan (`d1910e3a`)
- introduce find_relevant_files_advanced (`1259b4a0`)
- context_export.sh + Dual-Repo --self Flag + COOKBOOK.md LLM-Workflow (closes #65) (`bc8367d5`)
- Betriebsmodi — start_patch.sh, start_night.sh, stop_agent.sh, agent_status.sh (closes #52) (`3e4a8123`)
- Auto-Issues erst nach N aufeinanderfolgenden Passes schließen (closes #50) (`601fc854`)
- add agent_eval.template.json and tag check (`5993a82a`)
- implement patch mode and live dashboard (Issue #46) (`39216083`)
- implement systematic tag failure detection in watch mode (Issue #47) (`b65bcc70`)
- implement test generation via LLM prompt (Issue #44) (`2b5b8cc8`)
- implement performance benchmarking for eval (Issue #37) (`de88cbb9`)
- Add deterministic agent self-consistency check - Add agent_self_check.py for self checks - Run check in _check_pr_preconditions - Update COOKBOOK.md (`fb5d3306`)
- Kontext-Zusammenfassung als Gitea-Kommentar posten (`710d678c`)
- Kontext-Loader — Kommentare in Keyword-Suche einbeziehen (`e9f173fd`)
- Issue-Kommentare in starter.md aufnehmen (## Diskussion) (`080dae98`)
- konfigurierbare Excludes + contexts/open/ Struktur (`e81b7718`)
- zentrale Agent-Instanz — Pfade aus PROJECT_ROOT/agent/ lesen (closes #35) (`c8cb35b6`)
- Kontext-Loader — Import-Analyse + Keyword-Suche für files.md (closes #33) (`be8b1756`)
- Auto-Issues mit strukturiertem Kontext (closes #29) (`3481c6e9`)
- --pr prüft Server-Aktualität vor Eval + --force + --restart-before-eval (closes #30) (`10372c09`)
- _build_metadata() portiert (closes #74) (`c02a3a60`)
- _build_metadata() portiert — Abschluss + Fixup Kommentare (closes #74) (`186981b3`)
- Szenario 2 Watch-Neustart-Trigger implementieren (closes #19) (`04d342f2`)
- _build_metadata() — aufklappbarer Block + Token-Schätzung (closes #16) (`5fce1593`)
- Abschluss-Kommentar — Eval-Ergebnis + Metadata + Session-Status (closes #13) (`a923edc4`)
- Prioritätshinweis — technische Schranken vor Prompt-Regeln (refs #6) (`5955f2ac`)
- Prozess-Enforcement — 5 Maßnahmen gegen Kontext-Drift (closes #6) (`856c3bfd`)
- STARTER_MD_PFLICHTREGELN in settings.py (Maßnahme 2+3, refs #6) (`87cc28a9`)
- watch_interval_minutes aus agent_eval.json + README (closes #4) (`2c559d37`)
- cmd_watch — generischer Hook für tools/log_analyzer.py im Watch-Loop (`fb7f335f`)
- score_history letzte 5 Einträge in PR- und Watch-Kommentare (`b4ec8ab3`)
- --score-history Anzeige-Befehl — Eval-Verlauf als Tabelle im Terminal (`736cfa8b`)
- --watch Modus + score_history.json + Auto-Issues bei Score-Verlust (`8ed46cc7`)
- Baseline automatisch hochsetzen wenn Score besser wird (`172ee6a6`)
- steps-Mechanismus für mehrstufige Tests (refs #1) (`264b225e`)
- evaluation.py — generisches Eval-System vor --pr (closes #1) (`b9fc5989`)
- --fixup, Docs-Check, Auto-Cleanup + Revert-Hinweis (`323247d5`)
- Kontext-Dateien pro Issue (contexts/) (`5cafee8e`)
- Textbausteine zentralisiert in settings.py (`cd368efc`)
- Auto-Scan + working-bot Auth + generische Konfiguration via .env (`abb3b05b`)
- auto-modus — Gitea-States selbständig erkennen und verarbeiten (`c6b8db83`)
- initial gitea-agent MVP — LLM-agnostischer Issue-Workflow (`0b12bfe7`)

### Bugfixes
- CONTEXT_DIR in .env.agent — contexts statt contexts/open (verschachtelte done/ korrigiert) (`91eb9658`)
- --build-skeleton ohne Größen-Limit (max_size_kb=10000) — agent_start.py vollständig analysiert (`04638aaa`)
- stufe in cmd_plan nicht definiert (NameError) (`67cda8ee`)
- evaluation.py — Commit-Hash in score_history.json (closes #55) (`8eea1978`)
- CONTEXT_DIR aus .env.agent lesen — --self Modus funktioniert (`4e703ed8`)
- gemini_start.sh korrekt implementiert — Kontext-Loader statt Wrapper (closes #61) (`a27f30b4`)
- _sync_closed_contexts() beim Watch-Start — verwaiste open/-Ordner nach done/ verschieben (`8ff336b4`)
- Labels bei Auto-Issue-Schließung entfernen (`ecabd722`)
- _close_resolved_auto_issues verschiebt Context nach done/ (`781241e3`)
- Bot-Kommentar-Filter nach Username statt Keywords (`d7c6dd3c`)
- Kontext-Loader — Datei-Blacklist + Größen-Cap (50KB) (`d4b53d59`)
- branch_name() — Sonderzeichen robust filtern (regex statt char-Liste) (`3517e66e`)
- Keyword-Suche nur in Code-Dateien (nicht .md) (`c01ee514`)
- Kontext-Loader — Documentation/ excluden, AST depth=1 (`919cadc4`)
- _EXCLUDE_DIRS erweitert (Backup, vendor, contexts, .claude) (`b9d69bbb`)
- Path() fehlt in _resolve_path/_resolve_config — TypeError bei str project_root (`9232f4c8`)
- import time fehlte — Watch-Modus crashed (NameError) (`8f3e282a`)
- eval_line enthält 'Score:' in allen Zweigen — COMMENT_REQUIRED_FIELDS Validierung (`bc95cd25`)
- eval_line enthält Score auch bei Risikostufe-1-Skip (closes #11 Nebenfix) (`ebb44101`)
- changed=[] Fallback vor try-Block — verhindert NameError in _neustart_required() (`42cf01a9`)
- WARN 3 — doppelte _build_metadata entfernt + Agent-Metadaten (`679d53b1`)
- doppelte _build_metadata() entfernt + System-Infos → Agent-Metadaten (WARN 3) (`fedc4267`)
- baseline_score statt max_score + dynamischer Neustart-Hinweis (WARN 1+2) (`59602542`)
- _save_score_history() bei baseline_created — erster Lauf in score_history (closes #25) (`c559f24b`)
- COMMENT_REQUIRED_FIELDS[completion] — Eval + Score als Pflichtfeld (closes #24) (`af70545a`)
- Risk-Gate in cmd_pr() — Risiko 1 überspringt Eval (closes #18) (`da11d4a4`)
- Eval-Status und Session-Status auf separate Zeilen trennen (`dea0efda`)
- create_pr() gibt bestehenden PR zurück bei 409 (kein Crash bei Retry) (`71047b4c`)
- _format_history_block gibt Verlauf-Header zurück wenn keine History (closes #10 partial) (`85714c24`)
- --eval-after-restart implementieren — COOKBOOK/Code Diskrepanz beheben (closes #10) (`7fe798ab`)
- Output-Validierung — _validate_comment() + COMMENT_REQUIRED_FIELDS (closes #8) (`c448c355`)
- Eval-Prüfung nur wenn agent_eval.json existiert (refs #6) (`bf616363`)
- sleep(2) zwischen Steps — Jetson LLM-Cooldown bei mehrstufigen Tests (`c63b6138`)
- Eval-Fehler mit Gitea-Warnung + --summary Hinweis wenn leer (`1930f256`)
- _ping HEAD→GET, eindeutige eval_user-ID, prompt statt message (refs #1) (`aa2aea51`)
- Label-Kette + Metadaten-Block + help-wanted Korrektur (`f01df702`)

### Refactoring
- patch.py + changelog.py in plugins/ auslagern (closes #79) (`25aa4d7d`)
- Konsolenausgabe auf Statusmeldungen reduziert (`76d92bf3`)
- settings.py + log.py + hardcoded Werte zentralisiert (`bd9a06b1`)

### Tests
- Unit-Tests für kritische reine Funktionen (closes #78) (`5a86bb92`)

### Dokumentation
- Gitea-Versionsvergleich (#59) in README + COOKBOOK Kapitel 18 (`10a090fb`)
- Codesegment-Strategie (#72) in README + COOKBOOK Kapitel 17 (`116b63d7`)
- SEARCH/REPLACE (#58) in README + COOKBOOK Kapitel 16 (`6de6d1b1`)
- Diff-Validierung (#57) in README + COOKBOOK Kapitel 15 (`866a602f`)
- AST-Skelett (#68) in README Roadmap + COOKBOOK Kapitel 14 (`1f9db1ca`)
- README Roadmap #65 + Gemini-Warnung; COOKBOOK Kapitel 12 Querverweise (`c06fcae9`)
- Sitzungs-Protokoll 2026-03-23 — Änderungen + Gemini CLI Warnung (`4bfe8931`)
- README aktualisiert — Consecutive-Pass Gate, Betriebsmodi, Pflicht-Kette (`53c23453`)
- Feature-Übersicht in README erweitern (`d0dc2085`)
- Roadmap und Cookbook aktualisieren (`cee2ddb1`)
- README vollständig LLM-agnostisch formuliert (`49aa7aaa`)
- LLM-Anbindung (manuell vs. API) und Roadmap ausgebaut (`7626eb6b`)
- Feature-Übersicht mit Beschreibungen und Mini-Beispielen ausgebaut (`d3e1bb71`)
- README — Intro und Feature-Übersicht ergänzt (`dc1aeb0c`)
- konfigurierbare Excludes + contexts/open/ Struktur nachgezogen (`c7ceb8a5`)
- agent_start.py.md + evaluation.py.md nachgezogen (Kontext-Loader Fixes, Pfade) (`b25086e1`)
- README + COOKBOOK — zentrale Agent-Instanz, Migrationsrezept (#35) (`6fd0b794`)
- README + COOKBOOK — Kontext-Loader (Import-Analyse + grep) dokumentiert (#33) (`da428dc8`)
- agent_start.py.md — Kontext-Loader dokumentiert (#33) (`ae75ebab`)
- README + COOKBOOK — Auto-Issue strukturierter Body dokumentiert (#29) (`a0c81c81`)
- evaluation.py.md + agent_start.py.md — Issue #29 dokumentiert (closes #29) (`0b8c0d9e`)
- README + COOKBOOK — Server-Staleness-Check, --force, --restart-before-eval dokumentiert (`f595f400`)
- README + COOKBOOK aktualisiert — Prozess-Enforcement, Watch-Szenario 2, log_analyzer (closes #11) (`30f58dee`)
- COOKBOOK.md — 10 Rezepte mit Skynet-Referenz (closes #5) (`8dd50f42`)
- README — Watch-Modus, score_history, Auto-Baseline, History-in-Comments (`ca88ce6a`)
- README — Risiko ≥ 2 Ausnahme + steps sleep(2) + Eval-Fehler-Verhalten (`b80a991d`)
- README — Eval-System Abschnitt ergänzt (evaluation.py, agent_eval.json, steps, baseline) (`4f31887f`)
- evaluation.py.md + README Eval-Abschnitt (refs #1) (`7ca16b68`)
- README komplett überarbeitet (`0e3dad45`)
- Problemstellung + Lösungsansätze für LLM-Trigger vollständig dokumentiert (`be1b4f61`)
- README Auto-Modus Diagramm + --root Fix, Docstrings vervollständigt (`06098992`)

### Wartung
- add gemini_start.sh for Gemini CLI workflow (closes #61) (`9e8639dd`)
- contexts/ und gitea-agent.log zu .gitignore hinzufügen (`a96ce2bb`)

### Sonstiges
- Merge pull request 'Auto-Changelog-Generator (closes #39)' (#75) from feat/issue-39-auto-changelog-generator into main (`955ba08d`)
- Merge pull request '5 Gitea Versionsvergleich (closes #59)' (#74) from chore/issue-59-5-gitea-versionsvergleich into main (`bba525fc`)
- Merge pull request 'Flächendeckende Codesegment-Strategie — LLM lädt nur noch relevante Blöcke (closes #72)' (#73) from chore/issue-72-flaechendeckende-codesegment-strate into main (`0e3a3121`)
- Merge pull request '4 SEARCH/REPLACE (closes #58)' (#71) from chore/issue-58-4-search-replace into main (`9b9b6f8d`)
- Merge pull request '3 Diff-Validierung (closes #57)' (#70) from chore/issue-57-3-diff-validierung into main (`5ebeb5ae`)
- Merge pull request 'Titel: AST-Repository-Skelett — repo_skeleton.json bei --issue generieren (closes #68)' (#69) from chore/issue-68-titel-ast-repository-skelett-repo-s into main (`0995050b`)
- Merge pull request '2 AST-Skelett (closes #56)' (#67) from chore/issue-56-2-ast-skelett into main (`757ba664`)
- Merge pull request 'context_export.sh — LLM-agnostischer Kontext-Export + Dual-Repo-Unterstützung (closes #65)' (#66) from feat/issue-65-context-export-dual-repo into main (`f9fa27f3`)
- Merge pull request 'gemini_start.sh — Gemini CLI in Agent-Workflow integrieren (closes #61)' (#63) from fix/gemini-start-correct into main (`3a75fa28`)
- Merge pull request 'gemini_start.sh — Gemini CLI in Agent-Workflow integrieren (closes #61)' (#62) from chore/issue-61-gemini-start-sh-gemini-cli-in-agent into main (`d6c0fa82`)
- README.md aktualisiert (`8be81bdc`)
- Merge pull request 'feat: Betriebsmodi — Night / Patch / Idle (closes #52)' (#53) from feat/issue-52-betriebsmodi into main (`611e83b5`)
- Merge pull request 'feat: Auto-Issues erst nach N aufeinanderfolgenden Passes schließen (closes #50)' (#51) from feat/issue-50-consecutive-passes into main (`ac05027a`)
- README.md aktualisiert (`738c7e45`)
- Merge pull request 'feat: zentrale Agent-Instanz — Pfade aus PROJECT_ROOT/agent/ lesen (closes #35)' (#36) from feat/issue-35-zentrale-agent-instanz into main (`4f9c730d`)
- Merge pull request 'feat: Kontext-Loader — Import-Analyse + Keyword-Suche für files.md (closes #33)' (#34) from feat/issue-33-kontext-loader-import-analyse into main (`67f1598b`)
- Merge pull request 'feat: Auto-Issues mit strukturiertem Kontext (closes #29)' (#32) from feat/issue-29-auto-issue-strukturierter-kontext into main (`2030c2c1`)
- Merge pull request 'pr soll vor Eval prüfen ob Server-Code aktuell ist (closes #30)' (#31) from chore/issue-30-pr-soll-vor-eval-pr-fen-ob-server-c into main (`760f6372`)
- Merge pull request 'Docs: README + COOKBOOK aktualisieren — #6/#8 Details + log_analyzer Rezept (closes #11)' (#28) from docs/issue-11-docs-readme-+-cookbook-aktualisiere into main (`c377a0fe`)
- Merge pull request 'Audit-Fixes — 2 FAIL + 3 WARN aus Re-Audit #23 (closes #26)' (#27) from fix/issue-26-audit-fixes into main (`93d4e687`)
- Merge branch 'main' into fix/issue-26-audit-fixes (`3e4fc6c0`)
- Merge pull request '[Audit FAIL] Szenario 2 — Watch-Neustart-Trigger nicht implementiert (closes #19)' (#21) from fix/issue-19-szenario2-watch-neustart into main (`bb9af28e`)
- Merge branch 'main' into fix/issue-19-szenario2-watch-neustart (`6f745179`)
- Merge pull request '[Audit FAIL] Risiko 1 — Eval wird nicht übersprungen (kein Risk-Gate) (closes #18)' (#20) from fix/issue-18-risk-gate-eval-skip into main (`ac2dcfeb`)
- Merge branch 'main' into fix/issue-18-risk-gate-eval-skip (`c0d56c83`)
- Merge pull request '_build_metadata() — Token-Schätzung + aufklappbarer Block (closes #16)' (#17) from chore/issue-16-_build_metadata-—-token-sch-tzung-+ into main (`933eea13`)
- Merge branch 'main' into chore/issue-16-_build_metadata-—-token-sch-tzung-+ (`878c125d`)
- Merge pull request 'Abschluss-Kommentar — Eval-Ergebnis + Metadata + Session-Status (closes #13)' (#15) from chore/issue-13-abschluss-kommentar-—-eval-ergebnis into main (`4910ddca`)
- Merge branch 'main' into chore/issue-13-abschluss-kommentar-—-eval-ergebnis (`0a2c3e12`)
- Merge pull request 'Fix: --eval-after-restart Diskrepanz — COOKBOOK vs agent_start.py (closes #10)' (#12) from chore/issue-10-fix-eval-after-restart-diskrepanz-— into main (`835c262c`)
- Merge pull request 'fix: Output-Validierung — _validate_comment() + COMMENT_REQUIRED_FIELDS (closes #8)' (#9) from fix/issue-8-output-validierung-kommentare into main (`17d905c6`)
- Merge pull request 'Prozess-Enforcement — Kontext-Drift verhindern (closes #6)' (#7) from chore/issue-6-prozess-enforcement-—-kontext-drift into main (`dc366abb`)
- Merge pull request 'feat: evaluation.py — generisches Eval-System vor --pr (closes #1)' (#2) from feat/issue-1-evaluation-system into main (`afb8a7a8`)

## [Unreleased] — 2026-03-25

### Features
- Auto-Changelog-Generator — --changelog [VERSION] (closes #39) (`6fe6ef29`)
- Gitea-Versionsvergleich — get_file_contents, _ast_diff, _gitea_version_compare (closes #59) (`002ba078`)
- context_export.sh — Slice-Schleife (plain) + Slice-Anweisung (file/gemini) (`09a855b1`)
- --get-slice loggt session.json + _warn_slices_not_requested in preconditions (`c9f0a470`)
- files.md Skelett-Modus — kein Volltext mehr (`02be831e`)
- --build-skeleton + _update_skeleton_incremental + _load_skeleton_map (`878ec0d3`)
- SEARCH/REPLACE Protokoll — Parser, _apply_patch, cmd_apply_patch, --apply-patch/--dry-run (closes #58) (`d3fd8f5d`)
- Diff-Validierung — Scope-Warnung bei Änderungen außerhalb AST-Skelett (closes #57) (`88e6205c`)
- AST-Skelett — ClassDef/FunctionDef-Extraktion, repo_skeleton.md, --get-slice (closes #68) (`0955d0cb`)
- use repo_skeleton.json for files.md truncation (`e6ff1c79`)
- generate repo_skeleton.json in cmd_plan (`d1910e3a`)
- introduce find_relevant_files_advanced (`1259b4a0`)
- context_export.sh + Dual-Repo --self Flag + COOKBOOK.md LLM-Workflow (closes #65) (`bc8367d5`)
- Betriebsmodi — start_patch.sh, start_night.sh, stop_agent.sh, agent_status.sh (closes #52) (`3e4a8123`)
- Auto-Issues erst nach N aufeinanderfolgenden Passes schließen (closes #50) (`601fc854`)
- add agent_eval.template.json and tag check (`5993a82a`)
- implement patch mode and live dashboard (Issue #46) (`39216083`)
- implement systematic tag failure detection in watch mode (Issue #47) (`b65bcc70`)
- implement test generation via LLM prompt (Issue #44) (`2b5b8cc8`)
- implement performance benchmarking for eval (Issue #37) (`de88cbb9`)
- Add deterministic agent self-consistency check - Add agent_self_check.py for self checks - Run check in _check_pr_preconditions - Update COOKBOOK.md (`fb5d3306`)
- Kontext-Zusammenfassung als Gitea-Kommentar posten (`710d678c`)
- Kontext-Loader — Kommentare in Keyword-Suche einbeziehen (`e9f173fd`)
- Issue-Kommentare in starter.md aufnehmen (## Diskussion) (`080dae98`)
- konfigurierbare Excludes + contexts/open/ Struktur (`e81b7718`)
- zentrale Agent-Instanz — Pfade aus PROJECT_ROOT/agent/ lesen (closes #35) (`c8cb35b6`)
- Kontext-Loader — Import-Analyse + Keyword-Suche für files.md (closes #33) (`be8b1756`)
- Auto-Issues mit strukturiertem Kontext (closes #29) (`3481c6e9`)
- --pr prüft Server-Aktualität vor Eval + --force + --restart-before-eval (closes #30) (`10372c09`)
- _build_metadata() portiert (closes #74) (`c02a3a60`)
- _build_metadata() portiert — Abschluss + Fixup Kommentare (closes #74) (`186981b3`)
- Szenario 2 Watch-Neustart-Trigger implementieren (closes #19) (`04d342f2`)
- _build_metadata() — aufklappbarer Block + Token-Schätzung (closes #16) (`5fce1593`)
- Abschluss-Kommentar — Eval-Ergebnis + Metadata + Session-Status (closes #13) (`a923edc4`)
- Prioritätshinweis — technische Schranken vor Prompt-Regeln (refs #6) (`5955f2ac`)
- Prozess-Enforcement — 5 Maßnahmen gegen Kontext-Drift (closes #6) (`856c3bfd`)
- STARTER_MD_PFLICHTREGELN in settings.py (Maßnahme 2+3, refs #6) (`87cc28a9`)
- watch_interval_minutes aus agent_eval.json + README (closes #4) (`2c559d37`)
- cmd_watch — generischer Hook für tools/log_analyzer.py im Watch-Loop (`fb7f335f`)
- score_history letzte 5 Einträge in PR- und Watch-Kommentare (`b4ec8ab3`)
- --score-history Anzeige-Befehl — Eval-Verlauf als Tabelle im Terminal (`736cfa8b`)
- --watch Modus + score_history.json + Auto-Issues bei Score-Verlust (`8ed46cc7`)
- Baseline automatisch hochsetzen wenn Score besser wird (`172ee6a6`)
- steps-Mechanismus für mehrstufige Tests (refs #1) (`264b225e`)
- evaluation.py — generisches Eval-System vor --pr (closes #1) (`b9fc5989`)
- --fixup, Docs-Check, Auto-Cleanup + Revert-Hinweis (`323247d5`)
- Kontext-Dateien pro Issue (contexts/) (`5cafee8e`)
- Textbausteine zentralisiert in settings.py (`cd368efc`)
- Auto-Scan + working-bot Auth + generische Konfiguration via .env (`abb3b05b`)
- auto-modus — Gitea-States selbständig erkennen und verarbeiten (`c6b8db83`)
- initial gitea-agent MVP — LLM-agnostischer Issue-Workflow (`0b12bfe7`)

### Bugfixes
- CONTEXT_DIR in .env.agent — contexts statt contexts/open (verschachtelte done/ korrigiert) (`91eb9658`)
- --build-skeleton ohne Größen-Limit (max_size_kb=10000) — agent_start.py vollständig analysiert (`04638aaa`)
- stufe in cmd_plan nicht definiert (NameError) (`67cda8ee`)
- evaluation.py — Commit-Hash in score_history.json (closes #55) (`8eea1978`)
- CONTEXT_DIR aus .env.agent lesen — --self Modus funktioniert (`4e703ed8`)
- gemini_start.sh korrekt implementiert — Kontext-Loader statt Wrapper (closes #61) (`a27f30b4`)
- _sync_closed_contexts() beim Watch-Start — verwaiste open/-Ordner nach done/ verschieben (`8ff336b4`)
- Labels bei Auto-Issue-Schließung entfernen (`ecabd722`)
- _close_resolved_auto_issues verschiebt Context nach done/ (`781241e3`)
- Bot-Kommentar-Filter nach Username statt Keywords (`d7c6dd3c`)
- Kontext-Loader — Datei-Blacklist + Größen-Cap (50KB) (`d4b53d59`)
- branch_name() — Sonderzeichen robust filtern (regex statt char-Liste) (`3517e66e`)
- Keyword-Suche nur in Code-Dateien (nicht .md) (`c01ee514`)
- Kontext-Loader — Documentation/ excluden, AST depth=1 (`919cadc4`)
- _EXCLUDE_DIRS erweitert (Backup, vendor, contexts, .claude) (`b9d69bbb`)
- Path() fehlt in _resolve_path/_resolve_config — TypeError bei str project_root (`9232f4c8`)
- import time fehlte — Watch-Modus crashed (NameError) (`8f3e282a`)
- eval_line enthält 'Score:' in allen Zweigen — COMMENT_REQUIRED_FIELDS Validierung (`bc95cd25`)
- eval_line enthält Score auch bei Risikostufe-1-Skip (closes #11 Nebenfix) (`ebb44101`)
- changed=[] Fallback vor try-Block — verhindert NameError in _neustart_required() (`42cf01a9`)
- WARN 3 — doppelte _build_metadata entfernt + Agent-Metadaten (`679d53b1`)
- doppelte _build_metadata() entfernt + System-Infos → Agent-Metadaten (WARN 3) (`fedc4267`)
- baseline_score statt max_score + dynamischer Neustart-Hinweis (WARN 1+2) (`59602542`)
- _save_score_history() bei baseline_created — erster Lauf in score_history (closes #25) (`c559f24b`)
- COMMENT_REQUIRED_FIELDS[completion] — Eval + Score als Pflichtfeld (closes #24) (`af70545a`)
- Risk-Gate in cmd_pr() — Risiko 1 überspringt Eval (closes #18) (`da11d4a4`)
- Eval-Status und Session-Status auf separate Zeilen trennen (`dea0efda`)
- create_pr() gibt bestehenden PR zurück bei 409 (kein Crash bei Retry) (`71047b4c`)
- _format_history_block gibt Verlauf-Header zurück wenn keine History (closes #10 partial) (`85714c24`)
- --eval-after-restart implementieren — COOKBOOK/Code Diskrepanz beheben (closes #10) (`7fe798ab`)
- Output-Validierung — _validate_comment() + COMMENT_REQUIRED_FIELDS (closes #8) (`c448c355`)
- Eval-Prüfung nur wenn agent_eval.json existiert (refs #6) (`bf616363`)
- sleep(2) zwischen Steps — Jetson LLM-Cooldown bei mehrstufigen Tests (`c63b6138`)
- Eval-Fehler mit Gitea-Warnung + --summary Hinweis wenn leer (`1930f256`)
- _ping HEAD→GET, eindeutige eval_user-ID, prompt statt message (refs #1) (`aa2aea51`)
- Label-Kette + Metadaten-Block + help-wanted Korrektur (`f01df702`)

### Refactoring
- Konsolenausgabe auf Statusmeldungen reduziert (`76d92bf3`)
- settings.py + log.py + hardcoded Werte zentralisiert (`bd9a06b1`)

### Tests
- Unit-Tests für kritische reine Funktionen (closes #78) (`5a86bb92`)

### Dokumentation
- Gitea-Versionsvergleich (#59) in README + COOKBOOK Kapitel 18 (`10a090fb`)
- Codesegment-Strategie (#72) in README + COOKBOOK Kapitel 17 (`116b63d7`)
- SEARCH/REPLACE (#58) in README + COOKBOOK Kapitel 16 (`6de6d1b1`)
- Diff-Validierung (#57) in README + COOKBOOK Kapitel 15 (`866a602f`)
- AST-Skelett (#68) in README Roadmap + COOKBOOK Kapitel 14 (`1f9db1ca`)
- README Roadmap #65 + Gemini-Warnung; COOKBOOK Kapitel 12 Querverweise (`c06fcae9`)
- Sitzungs-Protokoll 2026-03-23 — Änderungen + Gemini CLI Warnung (`4bfe8931`)
- README aktualisiert — Consecutive-Pass Gate, Betriebsmodi, Pflicht-Kette (`53c23453`)
- Feature-Übersicht in README erweitern (`d0dc2085`)
- Roadmap und Cookbook aktualisieren (`cee2ddb1`)
- README vollständig LLM-agnostisch formuliert (`49aa7aaa`)
- LLM-Anbindung (manuell vs. API) und Roadmap ausgebaut (`7626eb6b`)
- Feature-Übersicht mit Beschreibungen und Mini-Beispielen ausgebaut (`d3e1bb71`)
- README — Intro und Feature-Übersicht ergänzt (`dc1aeb0c`)
- konfigurierbare Excludes + contexts/open/ Struktur nachgezogen (`c7ceb8a5`)
- agent_start.py.md + evaluation.py.md nachgezogen (Kontext-Loader Fixes, Pfade) (`b25086e1`)
- README + COOKBOOK — zentrale Agent-Instanz, Migrationsrezept (#35) (`6fd0b794`)
- README + COOKBOOK — Kontext-Loader (Import-Analyse + grep) dokumentiert (#33) (`da428dc8`)
- agent_start.py.md — Kontext-Loader dokumentiert (#33) (`ae75ebab`)
- README + COOKBOOK — Auto-Issue strukturierter Body dokumentiert (#29) (`a0c81c81`)
- evaluation.py.md + agent_start.py.md — Issue #29 dokumentiert (closes #29) (`0b8c0d9e`)
- README + COOKBOOK — Server-Staleness-Check, --force, --restart-before-eval dokumentiert (`f595f400`)
- README + COOKBOOK aktualisiert — Prozess-Enforcement, Watch-Szenario 2, log_analyzer (closes #11) (`30f58dee`)
- COOKBOOK.md — 10 Rezepte mit Skynet-Referenz (closes #5) (`8dd50f42`)
- README — Watch-Modus, score_history, Auto-Baseline, History-in-Comments (`ca88ce6a`)
- README — Risiko ≥ 2 Ausnahme + steps sleep(2) + Eval-Fehler-Verhalten (`b80a991d`)
- README — Eval-System Abschnitt ergänzt (evaluation.py, agent_eval.json, steps, baseline) (`4f31887f`)
- evaluation.py.md + README Eval-Abschnitt (refs #1) (`7ca16b68`)
- README komplett überarbeitet (`0e3dad45`)
- Problemstellung + Lösungsansätze für LLM-Trigger vollständig dokumentiert (`be1b4f61`)
- README Auto-Modus Diagramm + --root Fix, Docstrings vervollständigt (`06098992`)

### Wartung
- add gemini_start.sh for Gemini CLI workflow (closes #61) (`9e8639dd`)
- contexts/ und gitea-agent.log zu .gitignore hinzufügen (`a96ce2bb`)

### Sonstiges
- Merge pull request 'Auto-Changelog-Generator (closes #39)' (#75) from feat/issue-39-auto-changelog-generator into main (`955ba08d`)
- Merge pull request '5 Gitea Versionsvergleich (closes #59)' (#74) from chore/issue-59-5-gitea-versionsvergleich into main (`bba525fc`)
- Merge pull request 'Flächendeckende Codesegment-Strategie — LLM lädt nur noch relevante Blöcke (closes #72)' (#73) from chore/issue-72-flaechendeckende-codesegment-strate into main (`0e3a3121`)
- Merge pull request '4 SEARCH/REPLACE (closes #58)' (#71) from chore/issue-58-4-search-replace into main (`9b9b6f8d`)
- Merge pull request '3 Diff-Validierung (closes #57)' (#70) from chore/issue-57-3-diff-validierung into main (`5ebeb5ae`)
- Merge pull request 'Titel: AST-Repository-Skelett — repo_skeleton.json bei --issue generieren (closes #68)' (#69) from chore/issue-68-titel-ast-repository-skelett-repo-s into main (`0995050b`)
- Merge pull request '2 AST-Skelett (closes #56)' (#67) from chore/issue-56-2-ast-skelett into main (`757ba664`)
- Merge pull request 'context_export.sh — LLM-agnostischer Kontext-Export + Dual-Repo-Unterstützung (closes #65)' (#66) from feat/issue-65-context-export-dual-repo into main (`f9fa27f3`)
- Merge pull request 'gemini_start.sh — Gemini CLI in Agent-Workflow integrieren (closes #61)' (#63) from fix/gemini-start-correct into main (`3a75fa28`)
- Merge pull request 'gemini_start.sh — Gemini CLI in Agent-Workflow integrieren (closes #61)' (#62) from chore/issue-61-gemini-start-sh-gemini-cli-in-agent into main (`d6c0fa82`)
- README.md aktualisiert (`8be81bdc`)
- Merge pull request 'feat: Betriebsmodi — Night / Patch / Idle (closes #52)' (#53) from feat/issue-52-betriebsmodi into main (`611e83b5`)
- Merge pull request 'feat: Auto-Issues erst nach N aufeinanderfolgenden Passes schließen (closes #50)' (#51) from feat/issue-50-consecutive-passes into main (`ac05027a`)
- README.md aktualisiert (`738c7e45`)
- Merge pull request 'feat: zentrale Agent-Instanz — Pfade aus PROJECT_ROOT/agent/ lesen (closes #35)' (#36) from feat/issue-35-zentrale-agent-instanz into main (`4f9c730d`)
- Merge pull request 'feat: Kontext-Loader — Import-Analyse + Keyword-Suche für files.md (closes #33)' (#34) from feat/issue-33-kontext-loader-import-analyse into main (`67f1598b`)
- Merge pull request 'feat: Auto-Issues mit strukturiertem Kontext (closes #29)' (#32) from feat/issue-29-auto-issue-strukturierter-kontext into main (`2030c2c1`)
- Merge pull request 'pr soll vor Eval prüfen ob Server-Code aktuell ist (closes #30)' (#31) from chore/issue-30-pr-soll-vor-eval-pr-fen-ob-server-c into main (`760f6372`)
- Merge pull request 'Docs: README + COOKBOOK aktualisieren — #6/#8 Details + log_analyzer Rezept (closes #11)' (#28) from docs/issue-11-docs-readme-+-cookbook-aktualisiere into main (`c377a0fe`)
- Merge pull request 'Audit-Fixes — 2 FAIL + 3 WARN aus Re-Audit #23 (closes #26)' (#27) from fix/issue-26-audit-fixes into main (`93d4e687`)
- Merge branch 'main' into fix/issue-26-audit-fixes (`3e4fc6c0`)
- Merge pull request '[Audit FAIL] Szenario 2 — Watch-Neustart-Trigger nicht implementiert (closes #19)' (#21) from fix/issue-19-szenario2-watch-neustart into main (`bb9af28e`)
- Merge branch 'main' into fix/issue-19-szenario2-watch-neustart (`6f745179`)
- Merge pull request '[Audit FAIL] Risiko 1 — Eval wird nicht übersprungen (kein Risk-Gate) (closes #18)' (#20) from fix/issue-18-risk-gate-eval-skip into main (`ac2dcfeb`)
- Merge branch 'main' into fix/issue-18-risk-gate-eval-skip (`c0d56c83`)
- Merge pull request '_build_metadata() — Token-Schätzung + aufklappbarer Block (closes #16)' (#17) from chore/issue-16-_build_metadata-—-token-sch-tzung-+ into main (`933eea13`)
- Merge branch 'main' into chore/issue-16-_build_metadata-—-token-sch-tzung-+ (`878c125d`)
- Merge pull request 'Abschluss-Kommentar — Eval-Ergebnis + Metadata + Session-Status (closes #13)' (#15) from chore/issue-13-abschluss-kommentar-—-eval-ergebnis into main (`4910ddca`)
- Merge branch 'main' into chore/issue-13-abschluss-kommentar-—-eval-ergebnis (`0a2c3e12`)
- Merge pull request 'Fix: --eval-after-restart Diskrepanz — COOKBOOK vs agent_start.py (closes #10)' (#12) from chore/issue-10-fix-eval-after-restart-diskrepanz-— into main (`835c262c`)
- Merge pull request 'fix: Output-Validierung — _validate_comment() + COMMENT_REQUIRED_FIELDS (closes #8)' (#9) from fix/issue-8-output-validierung-kommentare into main (`17d905c6`)
- Merge pull request 'Prozess-Enforcement — Kontext-Drift verhindern (closes #6)' (#7) from chore/issue-6-prozess-enforcement-—-kontext-drift into main (`dc366abb`)
- Merge pull request 'feat: evaluation.py — generisches Eval-System vor --pr (closes #1)' (#2) from feat/issue-1-evaluation-system into main (`afb8a7a8`)
Alle nennenswerten Änderungen werden hier festgehalten.

## [Unreleased] — 2026-03-24

### Features
- Auto-Changelog-Generator — --changelog [VERSION] (closes #39) (`6fe6ef29`)
- Gitea-Versionsvergleich — get_file_contents, _ast_diff, _gitea_version_compare (closes #59) (`002ba078`)
- context_export.sh — Slice-Schleife (plain) + Slice-Anweisung (file/gemini) (`09a855b1`)
- --get-slice loggt session.json + _warn_slices_not_requested in preconditions (`c9f0a470`)
- files.md Skelett-Modus — kein Volltext mehr (`02be831e`)
- --build-skeleton + _update_skeleton_incremental + _load_skeleton_map (`878ec0d3`)
- SEARCH/REPLACE Protokoll — Parser, _apply_patch, cmd_apply_patch, --apply-patch/--dry-run (closes #58) (`d3fd8f5d`)
- Diff-Validierung — Scope-Warnung bei Änderungen außerhalb AST-Skelett (closes #57) (`88e6205c`)
- AST-Skelett — ClassDef/FunctionDef-Extraktion, repo_skeleton.md, --get-slice (closes #68) (`0955d0cb`)
- use repo_skeleton.json for files.md truncation (`e6ff1c79`)
- generate repo_skeleton.json in cmd_plan (`d1910e3a`)
- introduce find_relevant_files_advanced (`1259b4a0`)
- context_export.sh + Dual-Repo --self Flag + COOKBOOK.md LLM-Workflow (closes #65) (`bc8367d5`)
- Betriebsmodi — start_patch.sh, start_night.sh, stop_agent.sh, agent_status.sh (closes #52) (`3e4a8123`)
- Auto-Issues erst nach N aufeinanderfolgenden Passes schließen (closes #50) (`601fc854`)
- add agent_eval.template.json and tag check (`5993a82a`)
- implement patch mode and live dashboard (Issue #46) (`39216083`)
- implement systematic tag failure detection in watch mode (Issue #47) (`b65bcc70`)
- implement test generation via LLM prompt (Issue #44) (`2b5b8cc8`)
- implement performance benchmarking for eval (Issue #37) (`de88cbb9`)
- Add deterministic agent self-consistency check - Add agent_self_check.py for self checks - Run check in _check_pr_preconditions - Update COOKBOOK.md (`fb5d3306`)
- Kontext-Zusammenfassung als Gitea-Kommentar posten (`710d678c`)
- Kontext-Loader — Kommentare in Keyword-Suche einbeziehen (`e9f173fd`)
- Issue-Kommentare in starter.md aufnehmen (## Diskussion) (`080dae98`)
- konfigurierbare Excludes + contexts/open/ Struktur (`e81b7718`)
- zentrale Agent-Instanz — Pfade aus PROJECT_ROOT/agent/ lesen (closes #35) (`c8cb35b6`)
- Kontext-Loader — Import-Analyse + Keyword-Suche für files.md (closes #33) (`be8b1756`)
- Auto-Issues mit strukturiertem Kontext (closes #29) (`3481c6e9`)
- --pr prüft Server-Aktualität vor Eval + --force + --restart-before-eval (closes #30) (`10372c09`)
- _build_metadata() portiert (closes #74) (`c02a3a60`)
- _build_metadata() portiert — Abschluss + Fixup Kommentare (closes #74) (`186981b3`)
- Szenario 2 Watch-Neustart-Trigger implementieren (closes #19) (`04d342f2`)
- _build_metadata() — aufklappbarer Block + Token-Schätzung (closes #16) (`5fce1593`)
- Abschluss-Kommentar — Eval-Ergebnis + Metadata + Session-Status (closes #13) (`a923edc4`)
- Prioritätshinweis — technische Schranken vor Prompt-Regeln (refs #6) (`5955f2ac`)
- Prozess-Enforcement — 5 Maßnahmen gegen Kontext-Drift (closes #6) (`856c3bfd`)
- STARTER_MD_PFLICHTREGELN in settings.py (Maßnahme 2+3, refs #6) (`87cc28a9`)
- watch_interval_minutes aus agent_eval.json + README (closes #4) (`2c559d37`)
- cmd_watch — generischer Hook für tools/log_analyzer.py im Watch-Loop (`fb7f335f`)
- score_history letzte 5 Einträge in PR- und Watch-Kommentare (`b4ec8ab3`)
- --score-history Anzeige-Befehl — Eval-Verlauf als Tabelle im Terminal (`736cfa8b`)
- --watch Modus + score_history.json + Auto-Issues bei Score-Verlust (`8ed46cc7`)
- Baseline automatisch hochsetzen wenn Score besser wird (`172ee6a6`)
- steps-Mechanismus für mehrstufige Tests (refs #1) (`264b225e`)
- evaluation.py — generisches Eval-System vor --pr (closes #1) (`b9fc5989`)
- --fixup, Docs-Check, Auto-Cleanup + Revert-Hinweis (`323247d5`)
- Kontext-Dateien pro Issue (contexts/) (`5cafee8e`)
- Textbausteine zentralisiert in settings.py (`cd368efc`)
- Auto-Scan + working-bot Auth + generische Konfiguration via .env (`abb3b05b`)
- auto-modus — Gitea-States selbständig erkennen und verarbeiten (`c6b8db83`)
- initial gitea-agent MVP — LLM-agnostischer Issue-Workflow (`0b12bfe7`)

### Bugfixes
- CONTEXT_DIR in .env.agent — contexts statt contexts/open (verschachtelte done/ korrigiert) (`91eb9658`)
- --build-skeleton ohne Größen-Limit (max_size_kb=10000) — agent_start.py vollständig analysiert (`04638aaa`)
- stufe in cmd_plan nicht definiert (NameError) (`67cda8ee`)
- evaluation.py — Commit-Hash in score_history.json (closes #55) (`8eea1978`)
- CONTEXT_DIR aus .env.agent lesen — --self Modus funktioniert (`4e703ed8`)
- gemini_start.sh korrekt implementiert — Kontext-Loader statt Wrapper (closes #61) (`a27f30b4`)
- _sync_closed_contexts() beim Watch-Start — verwaiste open/-Ordner nach done/ verschieben (`8ff336b4`)
- Labels bei Auto-Issue-Schließung entfernen (`ecabd722`)
- _close_resolved_auto_issues verschiebt Context nach done/ (`781241e3`)
- Bot-Kommentar-Filter nach Username statt Keywords (`d7c6dd3c`)
- Kontext-Loader — Datei-Blacklist + Größen-Cap (50KB) (`d4b53d59`)
- branch_name() — Sonderzeichen robust filtern (regex statt char-Liste) (`3517e66e`)
- Keyword-Suche nur in Code-Dateien (nicht .md) (`c01ee514`)
- Kontext-Loader — Documentation/ excluden, AST depth=1 (`919cadc4`)
- _EXCLUDE_DIRS erweitert (Backup, vendor, contexts, .claude) (`b9d69bbb`)
- Path() fehlt in _resolve_path/_resolve_config — TypeError bei str project_root (`9232f4c8`)
- import time fehlte — Watch-Modus crashed (NameError) (`8f3e282a`)
- eval_line enthält 'Score:' in allen Zweigen — COMMENT_REQUIRED_FIELDS Validierung (`bc95cd25`)
- eval_line enthält Score auch bei Risikostufe-1-Skip (closes #11 Nebenfix) (`ebb44101`)
- changed=[] Fallback vor try-Block — verhindert NameError in _neustart_required() (`42cf01a9`)
- WARN 3 — doppelte _build_metadata entfernt + Agent-Metadaten (`679d53b1`)
- doppelte _build_metadata() entfernt + System-Infos → Agent-Metadaten (WARN 3) (`fedc4267`)
- baseline_score statt max_score + dynamischer Neustart-Hinweis (WARN 1+2) (`59602542`)
- _save_score_history() bei baseline_created — erster Lauf in score_history (closes #25) (`c559f24b`)
- COMMENT_REQUIRED_FIELDS[completion] — Eval + Score als Pflichtfeld (closes #24) (`af70545a`)
- Risk-Gate in cmd_pr() — Risiko 1 überspringt Eval (closes #18) (`da11d4a4`)
- Eval-Status und Session-Status auf separate Zeilen trennen (`dea0efda`)
- create_pr() gibt bestehenden PR zurück bei 409 (kein Crash bei Retry) (`71047b4c`)
- _format_history_block gibt Verlauf-Header zurück wenn keine History (closes #10 partial) (`85714c24`)
- --eval-after-restart implementieren — COOKBOOK/Code Diskrepanz beheben (closes #10) (`7fe798ab`)
- Output-Validierung — _validate_comment() + COMMENT_REQUIRED_FIELDS (closes #8) (`c448c355`)
- Eval-Prüfung nur wenn agent_eval.json existiert (refs #6) (`bf616363`)
- sleep(2) zwischen Steps — Jetson LLM-Cooldown bei mehrstufigen Tests (`c63b6138`)
- Eval-Fehler mit Gitea-Warnung + --summary Hinweis wenn leer (`1930f256`)
- _ping HEAD→GET, eindeutige eval_user-ID, prompt statt message (refs #1) (`aa2aea51`)
- Label-Kette + Metadaten-Block + help-wanted Korrektur (`f01df702`)

### Refactoring
- Konsolenausgabe auf Statusmeldungen reduziert (`76d92bf3`)
- settings.py + log.py + hardcoded Werte zentralisiert (`bd9a06b1`)

### Dokumentation
- Gitea-Versionsvergleich (#59) in README + COOKBOOK Kapitel 18 (`10a090fb`)
- Codesegment-Strategie (#72) in README + COOKBOOK Kapitel 17 (`116b63d7`)
- SEARCH/REPLACE (#58) in README + COOKBOOK Kapitel 16 (`6de6d1b1`)
- Diff-Validierung (#57) in README + COOKBOOK Kapitel 15 (`866a602f`)
- AST-Skelett (#68) in README Roadmap + COOKBOOK Kapitel 14 (`1f9db1ca`)
- README Roadmap #65 + Gemini-Warnung; COOKBOOK Kapitel 12 Querverweise (`c06fcae9`)
- Sitzungs-Protokoll 2026-03-23 — Änderungen + Gemini CLI Warnung (`4bfe8931`)
- README aktualisiert — Consecutive-Pass Gate, Betriebsmodi, Pflicht-Kette (`53c23453`)
- Feature-Übersicht in README erweitern (`d0dc2085`)
- Roadmap und Cookbook aktualisieren (`cee2ddb1`)
- README vollständig LLM-agnostisch formuliert (`49aa7aaa`)
- LLM-Anbindung (manuell vs. API) und Roadmap ausgebaut (`7626eb6b`)
- Feature-Übersicht mit Beschreibungen und Mini-Beispielen ausgebaut (`d3e1bb71`)
- README — Intro und Feature-Übersicht ergänzt (`dc1aeb0c`)
- konfigurierbare Excludes + contexts/open/ Struktur nachgezogen (`c7ceb8a5`)
- agent_start.py.md + evaluation.py.md nachgezogen (Kontext-Loader Fixes, Pfade) (`b25086e1`)
- README + COOKBOOK — zentrale Agent-Instanz, Migrationsrezept (#35) (`6fd0b794`)
- README + COOKBOOK — Kontext-Loader (Import-Analyse + grep) dokumentiert (#33) (`da428dc8`)
- agent_start.py.md — Kontext-Loader dokumentiert (#33) (`ae75ebab`)
- README + COOKBOOK — Auto-Issue strukturierter Body dokumentiert (#29) (`a0c81c81`)
- evaluation.py.md + agent_start.py.md — Issue #29 dokumentiert (closes #29) (`0b8c0d9e`)
- README + COOKBOOK — Server-Staleness-Check, --force, --restart-before-eval dokumentiert (`f595f400`)
- README + COOKBOOK aktualisiert — Prozess-Enforcement, Watch-Szenario 2, log_analyzer (closes #11) (`30f58dee`)
- COOKBOOK.md — 10 Rezepte mit Skynet-Referenz (closes #5) (`8dd50f42`)
- README — Watch-Modus, score_history, Auto-Baseline, History-in-Comments (`ca88ce6a`)
- README — Risiko ≥ 2 Ausnahme + steps sleep(2) + Eval-Fehler-Verhalten (`b80a991d`)
- README — Eval-System Abschnitt ergänzt (evaluation.py, agent_eval.json, steps, baseline) (`4f31887f`)
- evaluation.py.md + README Eval-Abschnitt (refs #1) (`7ca16b68`)
- README komplett überarbeitet (`0e3dad45`)
- Problemstellung + Lösungsansätze für LLM-Trigger vollständig dokumentiert (`be1b4f61`)
- README Auto-Modus Diagramm + --root Fix, Docstrings vervollständigt (`06098992`)

### Wartung
- add gemini_start.sh for Gemini CLI workflow (closes #61) (`9e8639dd`)
- contexts/ und gitea-agent.log zu .gitignore hinzufügen (`a96ce2bb`)

### Sonstiges
- Merge pull request '5 Gitea Versionsvergleich (closes #59)' (#74) from chore/issue-59-5-gitea-versionsvergleich into main (`bba525fc`)
- Merge pull request 'Flächendeckende Codesegment-Strategie — LLM lädt nur noch relevante Blöcke (closes #72)' (#73) from chore/issue-72-flaechendeckende-codesegment-strate into main (`0e3a3121`)
- Merge pull request '4 SEARCH/REPLACE (closes #58)' (#71) from chore/issue-58-4-search-replace into main (`9b9b6f8d`)
- Merge pull request '3 Diff-Validierung (closes #57)' (#70) from chore/issue-57-3-diff-validierung into main (`5ebeb5ae`)
- Merge pull request 'Titel: AST-Repository-Skelett — repo_skeleton.json bei --issue generieren (closes #68)' (#69) from chore/issue-68-titel-ast-repository-skelett-repo-s into main (`0995050b`)
- Merge pull request '2 AST-Skelett (closes #56)' (#67) from chore/issue-56-2-ast-skelett into main (`757ba664`)
- Merge pull request 'context_export.sh — LLM-agnostischer Kontext-Export + Dual-Repo-Unterstützung (closes #65)' (#66) from feat/issue-65-context-export-dual-repo into main (`f9fa27f3`)
- Merge pull request 'gemini_start.sh — Gemini CLI in Agent-Workflow integrieren (closes #61)' (#63) from fix/gemini-start-correct into main (`3a75fa28`)
- Merge pull request 'gemini_start.sh — Gemini CLI in Agent-Workflow integrieren (closes #61)' (#62) from chore/issue-61-gemini-start-sh-gemini-cli-in-agent into main (`d6c0fa82`)
- README.md aktualisiert (`8be81bdc`)
- Merge pull request 'feat: Betriebsmodi — Night / Patch / Idle (closes #52)' (#53) from feat/issue-52-betriebsmodi into main (`611e83b5`)
- Merge pull request 'feat: Auto-Issues erst nach N aufeinanderfolgenden Passes schließen (closes #50)' (#51) from feat/issue-50-consecutive-passes into main (`ac05027a`)
- README.md aktualisiert (`738c7e45`)
- Merge pull request 'feat: zentrale Agent-Instanz — Pfade aus PROJECT_ROOT/agent/ lesen (closes #35)' (#36) from feat/issue-35-zentrale-agent-instanz into main (`4f9c730d`)
- Merge pull request 'feat: Kontext-Loader — Import-Analyse + Keyword-Suche für files.md (closes #33)' (#34) from feat/issue-33-kontext-loader-import-analyse into main (`67f1598b`)
- Merge pull request 'feat: Auto-Issues mit strukturiertem Kontext (closes #29)' (#32) from feat/issue-29-auto-issue-strukturierter-kontext into main (`2030c2c1`)
- Merge pull request 'pr soll vor Eval prüfen ob Server-Code aktuell ist (closes #30)' (#31) from chore/issue-30-pr-soll-vor-eval-pr-fen-ob-server-c into main (`760f6372`)
- Merge pull request 'Docs: README + COOKBOOK aktualisieren — #6/#8 Details + log_analyzer Rezept (closes #11)' (#28) from docs/issue-11-docs-readme-+-cookbook-aktualisiere into main (`c377a0fe`)
- Merge pull request 'Audit-Fixes — 2 FAIL + 3 WARN aus Re-Audit #23 (closes #26)' (#27) from fix/issue-26-audit-fixes into main (`93d4e687`)
- Merge branch 'main' into fix/issue-26-audit-fixes (`3e4fc6c0`)
- Merge pull request '[Audit FAIL] Szenario 2 — Watch-Neustart-Trigger nicht implementiert (closes #19)' (#21) from fix/issue-19-szenario2-watch-neustart into main (`bb9af28e`)
- Merge branch 'main' into fix/issue-19-szenario2-watch-neustart (`6f745179`)
- Merge pull request '[Audit FAIL] Risiko 1 — Eval wird nicht übersprungen (kein Risk-Gate) (closes #18)' (#20) from fix/issue-18-risk-gate-eval-skip into main (`ac2dcfeb`)
- Merge branch 'main' into fix/issue-18-risk-gate-eval-skip (`c0d56c83`)
- Merge pull request '_build_metadata() — Token-Schätzung + aufklappbarer Block (closes #16)' (#17) from chore/issue-16-_build_metadata-—-token-sch-tzung-+ into main (`933eea13`)
- Merge branch 'main' into chore/issue-16-_build_metadata-—-token-sch-tzung-+ (`878c125d`)
- Merge pull request 'Abschluss-Kommentar — Eval-Ergebnis + Metadata + Session-Status (closes #13)' (#15) from chore/issue-13-abschluss-kommentar-—-eval-ergebnis into main (`4910ddca`)
- Merge branch 'main' into chore/issue-13-abschluss-kommentar-—-eval-ergebnis (`0a2c3e12`)
- Merge pull request 'Fix: --eval-after-restart Diskrepanz — COOKBOOK vs agent_start.py (closes #10)' (#12) from chore/issue-10-fix-eval-after-restart-diskrepanz-— into main (`835c262c`)
- Merge pull request 'fix: Output-Validierung — _validate_comment() + COMMENT_REQUIRED_FIELDS (closes #8)' (#9) from fix/issue-8-output-validierung-kommentare into main (`17d905c6`)
- Merge pull request 'Prozess-Enforcement — Kontext-Drift verhindern (closes #6)' (#7) from chore/issue-6-prozess-enforcement-—-kontext-drift into main (`dc366abb`)
- Merge pull request 'feat: evaluation.py — generisches Eval-System vor --pr (closes #1)' (#2) from feat/issue-1-evaluation-system into main (`afb8a7a8`)
